export default function SystemPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-mono">System</h1>
      <p className="mt-4 text-text-secondary">
        Page: <code>/system</code> &mdash; Day 20 scaffold; real content (kill-switch UI,
        read-only Risk Envelope tile, audit log table, reconciliation status, watchdog status,
        Account section for backup-code regen) lands Week 7 Wed.
      </p>
    </div>
  );
}
