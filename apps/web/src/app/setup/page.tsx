export default function SetupPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-mono">Setup</h1>
      <p className="mt-4 text-text-secondary">
        Page: <code>/setup</code> &mdash; Day 20 scaffold; real first-run WebAuthn registration
        ceremony + TOTP enrollment + 8 single-use backup codes lands Day 21 (Week 6 Tue) per IG
        line 376.
      </p>
    </div>
  );
}
