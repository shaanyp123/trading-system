export default function PerformancePage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-mono">Performance</h1>
      <p className="mt-4 text-text-secondary">
        Page: <code>/performance</code> &mdash; Day 20 scaffold; real content (equity curve via
        Recharts + monthly returns table + CSV export) lands Week 7 Wed.
      </p>
    </div>
  );
}
