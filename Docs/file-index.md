# File Index

Snapshot of code + ops surfaces with current status, recent PR history, and forbidden-path annotations. This table moves out of CLAUDE.md to keep the orientation doc slim.

**Update cadence:** append-only per PR / per saga. When adding a new tracked surface, add a row. When updating status, append to the Status column rather than rewriting prior entries (preserves the audit trail of how the file evolved).

**Cross-refs:** rows reference `Docs/decisions-log.md` entries by date and PRs by number (e.g., PR #232). For the pivot blocks themselves, see `Docs/recent-architecture-changes.md`.

---

## File index

### Reference docs (read at session start)
| Path | Purpose |
|---|---|
| `README.md` | Operator-facing top-level index |
| `CLAUDE.md` | This file — orientation for you |
| `implementation-guide.md` | Operator's daily handbook; week-by-week build plan + decision register + runbook |
| `Docs/backend-spec.md` | Backend architecture: schemas, APIs, risk framework, audit log, security |
| `Docs/frontend-spec.md` | Frontend architecture: pages, components, real-time, auth |
| `Docs/claude-dev-guide.md` | YOUR canonical patterns + conventions + anti-patterns + session protocol |
| `Docs/decisions-log.md` | Append-only log of decisions + deviations from specs as the build progresses |
| `Prompts/` | Generation prompts (archived; do not reference for current work) |
| `Archive/` | Reserved for superseded versions |

### Current state — 2026-05-28 (refresh; afternoon update post-signal-proximity 3-PR completion)

**HEAD:** `79d16bf` (PR #286 merge — signal-proximity PR-C frontend Watching section). Last 10 days of merges (in order): PR #270/#271/#272 (capital-event surface — `services/risk/capital_events.py` + api/discord/deploy wiring); PR #273 (Discord embed env=PAPER/LIVE footer chip for cross-env disambiguation — separate from the capital-event split despite the adjacent merge timing); PR #274 (journald log driver across paper + live stacks); PR #275 (`services/reconciliation/eod_cycle.py` FUT-symbol normalization via `underlyingSymbol` + `broker_view_missing_futures` warning + non-actionable break downgrade); PR #276 (`scripts/operator_tools/replace_protective_stop.py` subscribe + ack-wait so post-place DB UPDATE lands `status='working'` + `acknowledged_at_utc`); PR #277 (decisions-log: 2026-05-27 evening recon false-positive + cleanup ceremony); PR #278 (CSRF bootstrap fix — `services/api/session.py::SessionStubMiddleware` mints `__Host-csrf_token` on responses where request lacked one + `apps/web/src/components/providers.tsx` mounts `ensureBootstrapCsrfCookie()` via `useLayoutEffect` + `apps/web/src/lib/api.ts` bootstrap gains `Max-Age=86400`); PR #279 (runbook for FlexQuery template 1505530 FUT section edit); PR #280 (`scripts/operator_tools/master_client_id_probe.py` `--worker-offline-confirmed` gate); PR #281/#282 (docs refresh + decisions-log 2026-05-28 morning); PR #283 (signal-proximity PR-A — strategy + LEAN emission + API tolerates new field; `risk-review-approved`); PR #284 (signal-proximity PR-B — Alembic + repo + schema + endpoint; `risk-review-approved`); PR #285 (signals row-mapping fix on `services/api/repos/phase1.py` + `services/api/routes/signals.py` so `/api/signals` returns data); PR #286 (signal-proximity PR-C — frontend Watching section on `/signals`; frontend-only no label).

**2026-05-31 update (post-#286): exit-proximity #308/#309/#310 deployed + PR-A2 (this branch).** The exit-proximity 3-PR bundle (#308 strategy/LEAN emission, #309 api persistence + `GET /api/today/exit-proximity`, #310 frontend Today "Exits" section) merged + deployed to paper, but the "Exits" section was INERT because `generate_exit_candidates` saw LEAN's always-empty `self.portfolio` (decisions-log 2026-05-31). **PR-A2 `feat/exit-position-sourcing` (this branch, `risk-review-approved`)** fixes it by sourcing the api's real positions, activating the V1 indicator exits (trend_flip/reversal/decommission). Surfaces touched: NEW `GET /api/internal/lean/positions` (`services/api/routes/internal/lean.py`) + `LeanPositionItem`/`LeanPositionsResponse` (`services/api/schemas/lean.py`) + `fetch_positions_for_lean_cycle`/`fetch_inflight_exit_markets` (`services/api/repos/phase1.py`); `lean/v1_strategy.py` live-mode sourcing for BOTH pipelines (entry anti-pyramiding + exits) with fail-safe fallback + `exits_in_flight` suppression (+ `from __future__ import annotations` so its pure helpers are unit-testable); NEW server-side `DuplicateInFlightExitError` dedup guard in `services/qc_adapter/signal_ingestion.py`. NEW tests: `tests/unit/test_api_lean_positions.py`, `tests/unit/test_lean_live_positions.py`, `tests/integration/test_lean_positions_endpoint.py`. First live fire of the full exit-dispatch chain (bracket-cancel → close → trade-close) — observed deploy recommended.

**Operational status:** paper trading OPERATIONAL (Day 9; Day 1 = 2026-05-20). Daily ceremony: 17:00 ET `BarSyncWorker` (clientId=3) → 21:00 UTC bar_sync end → 21:10 UTC `lean_local` systemd restart → 21:30 UTC LEAN signal cycle → 22:30 UTC EOD reconciliation. **Risk state NORMAL** since 2026-05-28T03:10:51.099079Z (the manual-override CONVALESCENT → NORMAL transition from the 2026-05-27 (late evening) recon false-positive incident — `audit_event_uuid=019e6c90-36cb-7902-964f-a234fcf21707`, audit seq 258). All 9 containers Up (healthy). Risk-state DB row's `audit_event_uuid` matches the chain. /M2K position: long 1 @ $2,925.00 (M2KM6 FUT, expiry 2026-06-18); protective stop SELL STP @ $2,770.20 `client_order_id=b5237aed-b07925a7-019e66d3-stop-replace-0` PreSubmitted at IBKR on clientId=99, `status='working'` in DB (PR #276 path verified empirically by the 2026-05-27 evening replacement).

**Live-money cutover blockers remaining (per `Docs/live-money-cutover-plan.md`):** (a) FlexQuery template 1505530 missing FUT section (operator action queued for tonight's 02:00-04:00 UTC ceremony per `Docs/flexquery-template-1505530-add-fut-section-runbook.md` — closes the recurring P2 alert noise + auto-resolves stale `reconciliation_breaks` rows `019e6b8f-…` + `019e6c83-…`); (b) empirical Master Client ID validation via `master_client_id_probe` (operator action also queued for tonight per the new module-docstring runbook).

**Live-money cutover blockers CLOSED this session:** CSRF resume bug (PR #278 — both backend SessionStub + frontend Providers paths). The 2026-05-27 evening incident's manual-override seq 257-258 audit trail will not recur — the standard `POST /api/system/kill-switch/resume` from the web UI now succeeds without the synthesized `manual-override-` operator_session_id workaround.

**Stale rows below this stamp:** the historical "current state as of 2026-05-18" paragraph immediately below describes the post-drill-5 Week-7-gate-close snapshot at HEAD `3c64f49`. The table rows that follow it are append-only per the §11 update protocol — they may not reflect the most recent PRs touching each surface but are not wrong, just incomplete. Trust the per-row Status columns plus `Docs/decisions-log.md` for the up-to-date narrative on any specific surface. The 2026-05-28 ADDED-ROWS subsection at the bottom of the table contains the new entries from this refresh.

---

### Code + ops surfaces (historical state as of 2026-05-18, post-drill-5 Week-7-gate-close — **HEAD `3c64f49`**; audit chain CLEAN at 64 rows; 12 new rows from drill 5's end-to-end TLT round-trip (signal_emitted=53 → signal_approved=54 → order_placed entry=55 + stop=56 → ORDER_FILLED entry=57 + POSITION_OPENED=58 + BALANCE_SNAPSHOT=59 + TRADE_OPENED=60 [PR-G entry path] → ORDER_FILLED stop=61 + POSITION_CLOSED=62 + BALANCE_SNAPSHOT=63 + TRADE_CLOSED=64 [PR-G exit path]); 1 closed trade `019e3b5a-fe24-…` with realized_pnl=$0.0000 (entry=exit=$83.72; commission=$0; hold time 2m 34s); **Week 7 verification gate items 1 + 2 closed** (item 3 was partial-closed Day 25/27 via the HALT→CONVALESCENT→NORMAL web flow). **Defect surfaced + recovered during the drill:** ib_gateway↔IBKR servers connection broke during the 23:59 ET overnight maintenance auto-restart because IBC hit the "Existing session detected" dialog while operator's TWS Desktop was logged in for the same account; dialog hung 4h until manual restart cycle. api's clientId=2 socket to the gateway stayed alive (orders 4 + 6 placed via the local API socket) but orderStatus events for the IBKR-side fills never propagated (IBKR Error 1100 visible in api logs). Recovery: closed TWS Desktop → `docker compose restart ib_gateway` → clean boot (no "Existing session detected" because no competing session) → `docker compose restart api` → fresh `subscribe_order_status client_id=2` wired → one-shot `reqExecutions` probe inside api container (clientId=99; separate from running worker) replayed both fills through `services.risk.fill_processor.process_fill_event` to fire PR-G entry + exit paths end-to-end. Discord `#fills` embeds fired manually via direct webhook POST (SSE-emit path is intra-process to api; recovery script ran in a separate Python process so bypassed the SSE multiplexer). Full retrospective + 4 open follow-ups (IBC `ExistingSessionDetectedAction=primary` auto-resolve config + AsyncTaskMonitor extension for IBKR Error 1100 + signals.status='closed' on PR-G exit + codify `reqExecutions` recovery into `scripts/operator_tools/replay_executions.py`) in `Docs/decisions-log.md` 2026-05-18 entry. Pre-drill 5 state context: post-Path-4 futures-seed (DataBento direct SDK + minimal LEAN converter) — VPS at `b912b98` (PR #131 merged after PR #130; this-session PR pending merge). **Phase-1 architecture pivot LIVE** (2026-05-12; DP-025 retired the QC ObjectStore polling path → Option 4 LEAN Local + direct IBKR; see pivot block at top of CLAUDE.md). **LEAN Local running in live mode** under `paper-internal` env on `quantconnect/lean:latest` (PR #120 swapped `InteractiveBrokersBrokerage` → `PaperBrokerage` after the bare image was found to NOT ship `QuantConnect.Brokerages.InteractiveBrokers.dll` — crash-loop on `Sequence contains no matching element`; LEAN is signal-generation-only, api owns the IBKR contract). **api owns IBKR broker authority** via `services/execution/ibkr_adapter.py` (`ib-async` to `ib_gateway` sidecar; client_id=1; Pivot-PR-B PR #90) + `services/risk/order_placement_worker.py` (approved-signal → place_order pipeline; PR #101 Worker-PR-1 + PR #105 lifespan + PR #116 fill-SSE emit on IBKR orderStatus → webhook_pusher → #fills). **Audit chain CLEAN at 2 rows** (`verify_chain --env paper` → `CHAIN OK: 2 rows verified`; both rows are Day 25 + Day 27 state transitions; chain will grow at first `signal_emitted` post-cycle). **Phase 1 universe seeded 11-of-11** as of this PR — `TLT IEF SHY TIP` ETFs via yfinance (PR #123) + `/MES /MNQ /MYM /M2K /MGC /MCL /MBT` micro futures via DataBento direct SDK + custom LEAN converter (this PR). **ETF pre-live data-quality gate LOCKED 2026-05-12** via full 360-row Tiingo cross-check (4-of-4 PASS at <1bp; max divergence 0.6070bp on SHY; runbook Step 7 Option B). **Futures seed cost ~$0.96** against DataBento's $125 free credit (130x headroom). Strategy uses `DataMappingMode.OPEN_INTEREST` + `add_future` continuous-contract subscription, served by per-day universe files + per-expiry trade+OI zips emitted by the converter. **Path 4 / Raw-mode correction:** Initial assumption was `add_future`'s default `DataNormalizationMode` is `Raw`; QC forum 17093 staff response corrected mid-session — the implicit default is converted to `BackwardsRatio` (which requires factor_files we can't synthesize). This PR adds **explicit `data_normalization_mode=DataNormalizationMode.RAW`** to all 7 `add_future()` calls in `lean/v1_strategy.py` (hot-fix; lean/** path-filter) to preserve the Path 4 thesis. The lean CLI's narrower data-type support for DataBento (`['Trade', 'Quote']` only) discovered in PR #132 made the lean-CLI-native path (B1 in PR #124 memo) unviable; Path 4 sidesteps the CLI entirely by using the DataBento Python SDK + a minimal converter. **EOD reconciliation scheduler armed** at 18:30 ET (22:30 UTC) via PR #117 `services/reconciliation/eod_cycle.py` wired into api lifespan; defensive-skip until operator populates sops `ibkr.flex_query_id` + `flex_query_token`. **Global top nav live** (PR #114 `apps/web/src/components/nav/nav-bar.tsx` + `routes.config.ts`); **DecisionDiaryModal wired** to signal reject + defer (PR #115). **CI rework LIVE** — PR #127 introduced path-filter on expensive jobs (mypy/pytest/docker-build/frontend-test skip when only docs/scripts/lean/tests/.github paths change; ci-gate aggregate job pins required status checks across both filter outcomes); branch-protection updated to require ci-gate only per PR #128 close-out doc; new `.github/workflows/README.md` explains the gate pattern. **PR #129 + #130 fixed the DataFrame-iteration gap** in `lean/v1_strategy.py::_build_bar_series` (parser extracted to `strategies/v1_trend_following/lean_history_adapter.py` + 41 regression tests). qc_adapter at `services/qc_adapter/**` retained as dormant under `qc_adapter_backfill` compose profile per the pivot block.)
| Path | Purpose | Status |
|---|---|---|
| `strategies/v1_trend_following/` | V1 Donchian/MA/Hurst/ATR strategy logic; `parameters.py`, `indicators.py`, `signals.py`, `audit_events.py`, `sizing_trace.py`, `strategy.py`. **PR #228 (2026-05-23) — `V1_SIDELINED_MARKETS: Final[frozenset[str]] = frozenset({"/MCL"})`** added to `parameters.py` as the canonical sideline registry. Operator-reversible mechanism: full /MCL infrastructure support (expiry rule + holiday calendar + sentinel substitution + alert builders + tick sizes + reconciliation labels + IBKR contract-resolution path) stays in the codebase but the market is excluded from the active strategy universe pending operator action on documented re-enable preconditions (IBKR paper-tier NYMEX entitlement upgrade OR alt-feed swap OR static expiry-rule-driven front-month picker). Docstring contains the 5-step re-enable runbook: (1) verify precondition, (2) remove from `V1_SIDELINED_MARKETS`, (3) re-add to `V1_CANDIDATE_UNIVERSE` + `PHASE1_UNIVERSE_METADATA` (bar_sync) + `V1_FUTURES_MARKET_PATHS` (universe_freshness) + `PHASE1_FUTURES` (lean/v1_strategy.py), (4) rebuild + force-recreate api + lean_local + verify natural 21:30 UTC LEAN cycle's probe shows `hist_len > 0`, (5) update tests pinning universe size. Invariant `V1_SIDELINED_MARKETS ∩ set(V1_CANDIDATE_UNIVERSE) == ∅` locked by `tests/unit/test_strategy_v1.py::TestSidelinedMarkets`. Re-exported via `strategies/v1_trend_following/__init__.py::__all__` so future grep-for-sidelined queries land in one place. | Day 2 — entry pipeline real; exit pipeline scaffolded for Week 3–4; **PR #228 (2026-05-23) — `V1_SIDELINED_MARKETS` registry added; /MCL dropped from `V1_CANDIDATE_UNIVERSE` (hot-fix scope `strategies/v1_trend_following/**`; no `risk-review-approved`)** |
| `lean/v1_strategy.py` + `lean/lean.json` + `lean/README.md` + `infrastructure/lean_local/{Dockerfile,entrypoint.sh}` + `deploy/lean_local/README.md` | Post-pivot LEAN Local surface — Dockerized custom-built image (extends `quantconnect/lean:latest` per `infrastructure/lean_local/Dockerfile`) on the operator's VPS per backend-spec §1.2 + pivot block above. `v1_strategy.py` — `V1TrendFollowingAlgorithm(QCAlgorithm)` consumes `strategies/v1_trend_following/` pure-policy logic; `on_daily_signal_cycle` scheduled at 17:30 ET (21:30 UTC) fires `_post_event("signal_emitted", ...)` per generated signal + `lean_cycle_heartbeat` per cycle; `initialize` POSTs `lean_strategy_initialized` at boot; never calls broker-mutating APIs (read-only `self.history` + `self.portfolio[symbol]` + HTTP POST to `/api/internal/lean/signals`). `_build_bar_series` parses LEAN's `self.history(...)` return value — modern QC Python API yields a pandas DataFrame with `(symbol, time)` MultiIndex + lowercase OHLCV columns; legacy iterable-of-`TradeBar` form preserved as defensive fallback (parser extracted to `strategies/v1_trend_following/lean_history_adapter.py` for full test coverage; in-class `_build_bar_series` is a ~30-line LEAN-runtime wrapper). **DATA-LAYER SUB-PIVOT 2026-05-20** — `lean.json` swapped `data-queue-handler` from `[FakeDataQueue]` → `[InteractiveBrokersBrokerage]` + `history-provider` from `[SubscriptionDataReaderHistoryProvider]` → `[InteractiveBrokersBrokerage]`; LEAN now reads market data from IBKR via the same `ib_gateway` sidecar the api uses for orders (delayed quotes 1-3s actual lag + `reqHistoricalData` daily bars; both free for any IBKR account holder; per 2026-05-19 evening probe). Connection is on a distinct `clientId=10` from the api's order worker (clientId=1) — IBKR allows multiple distinct client-ids per gateway session. `live-mode-brokerage` stays `PaperBrokerage` (LEAN does NOT place orders; the api owns order path + fill processing + audit chain). 9 new ib-* top-level keys (`ib-host=ib_gateway` + `ib-port=4002` + `ib-client-id=10` + `ib-account` / `ib-user-name` / `ib-password` from `${IB_*}` env vars sourced from sops `ibkr.paper_*` fields via entrypoint, + `ib-trading-mode=paper` + `ib-agent-description=Individual` + `ib-enable-delayed-streaming-data=true` + `ib-weekly-restart-utc-time=22:00:00`). Dockerfile is now multi-stage: builder pulls `QuantConnect.Brokerages.InteractiveBrokers` v2.5.17699 from NuGet via `dotnet publish`; final image copies the IBKR plugin DLL + `QuantConnect.IBAutomater.dll` into `/Lean/Launcher/bin/Debug/` so live-mode boot's Composer broker-factory lookup resolves the data-queue-handler class name. The bare `quantconnect/lean:latest` image does NOT ship the plugin DLL (this was the 2026-05-12 root cause of the `Sequence contains no matching element` crash that motivated PR #120's PaperBrokerage swap for data + fills); the 2026-05-20 sub-pivot bakes the DLL in for the data path only. `entrypoint.sh` restores the LEAN-side reads of `ibkr.paper_username` / `.paper_password` / `.paper_account` from sops bundle → exports as `IB_USER_NAME` / `IB_PASSWORD` / `IB_ACCOUNT` for the deep-merge substitution; fail-closed on missing + placeholder. The `_log_universe_freshness` invocation in `v1_strategy.py::initialize` is removed (the on-disk `/Lean/Data/future/<ticker>/universes/*.csv` path it walked no longer exists post-pivot; method body + `strategies.v1_trend_following.universe_freshness` module + its 27 tests stay in the repo for reuse). Replaces the retired Day-4 `lean/v1_qc_algorithm.py` QC-Cloud wrapper. | Day 4 PR #15 (initial lean.json + lean/README.md); Pivot-PR-A PR #89 (lean_local container + `v1_strategy.py`); Pivot-PR-F PR #98 + #99 (deep-merge config); PR #111 (structlog in image); PR #110 (`strategies` pkg mount fix); PR #120 — PaperBrokerage swap + paper-internal env + IBKR-cred drops; PR #121 deploy carryover (live-mode sustained; chain CLEAN); PR #129 + #130 — DataFrame-iteration fix + parser extraction + 41 regression tests; PR #131 — Path 4 explicit `data_normalization_mode=DataNormalizationMode.RAW` on the 7 `add_future()` calls; PR #132 → 2026-05-20 data-layer sub-pivot — `data-queue-handler` + `history-provider` swap to `InteractiveBrokersBrokerage` at clientId=10 (later reverted under Option C); **PR #220 (2026-05-22) — `_log_history_probe` diagnostic instrumentation in `_build_bar_series` (still armed; retirement PR #227 stays DRAFT pending Mon 2026-05-25 21:30 UTC validation)**; **PR #225 (2026-05-22) — `self.add_future(ticker, ...)` switched from slash-prefixed (`f"/{ticker}"`) to BARE ticker per LEAN's `QCAlgorithm.AddFuture` slash-prepend convention (`alias = "/" + ticker` internally); paired atomically with the synthesizer's SID-hash extension so `MapFileResolver._bySymbol["MES"]` lookup hits + `LiveSynchronizer` doesn't crash on bare-permtick `SecurityIdentifier.Parse`. `_market_subscriptions` dict KEY stays slash-prefixed (`"/MES"`) to preserve backend signal-payload wire contract**; **PR #228 (2026-05-23) — `PHASE1_FUTURES = ("MES", "MNQ", "MYM", "M2K", "MGC", "MBT")` (6 micros; `/MCL` removed per the `V1_SIDELINED_MARKETS` registry in `strategies/v1_trend_following/parameters.py`)**; **PR #231 (2026-05-24) — `future.set_filter(0, 90)` → `future.set_filter(-365, 90)` to include contracts expired up to 365 days ago in the continuous-future chain, so the historical contracts written to disk by PR #229's backfill are actually consumed by `self.history()` (was previously filtered out as expired)**. Extensive comment blocks at each saga touch point document the chain + point at `Docs/decisions-log.md` entries (hot-fix scope `lean/**`; no `risk-review-approved`)** |
| `strategies/v1_trend_following/lean_history_adapter.py` | Pure-Python adapter — LEAN's ``self.history(symbol, count, Resolution.DAILY)`` return value → ``list[Bar]``. Two public exports: ``value_is_missing(value)`` (None / NaN / non-numeric guard for pandas OHLCV cells — pandas NaN passes ``v is not None``, so a naive None-guard would let bad rows through and explode inside ``Decimal(str(nan))``) + ``parse_history_to_bars(history)`` (duck-typed dispatch on ``iterrows`` — modern QC Python API DataFrame with MultiIndex `(symbol, time)` + lowercase OHLCV columns vs. legacy iterable-of-`TradeBar` defensive fallback; snake_case → PascalCase attr fallback on the legacy branch covers the pre-2024 QC API). Returns a possibly-empty `list[Bar]`; caller (`lean/v1_strategy.py::_build_bar_series`) wraps in try/except for parse-time exceptions, sorts + dedupes by `session_date`, constructs `BarSeries`. Lives in `strategies/v1_trend_following/` (NOT `services/signal/` which would be on the FORBIDDEN whitelist per dev-guide §2.2) because it's pure adapter logic with no signal-service authority — it runs inside LEAN's process via the algorithm wrapper, never executes server-side. A02 does NOT bind. | Post-PR-#129 follow-up (this session) — extracts the parser so it gets full mypy + ruff + pytest coverage (lean/ is off the project's lint+type+test target list per pyproject.toml); locks the PR #129 fix contract via 41 regression tests. Hot-fix scope (`strategies/v1_trend_following/**`); no risk-review-approved label |
| `tests/unit/test_lean_history_adapter.py` | 41 tests across 7 `Test*` classes — TestValueIsMissing (15: None / Python NaN / string-NaN / finite floats / numeric strings / int / zero / negative / non-numeric string / dict / list / inf / -inf / bool True/False); TestParseHistoryToBarsDataFramePath (10 — the PR #129 contract: None / empty DataFrame / MultiIndex 2-tuple ordering / single-index Timestamp / NaN-row skip / missing 'volume' column → 0 / KeyError on missing OHLC → skip / Decimal precision preserved / NaN volume → 0 / **three-valid-one-NaN yields three bars regression**); TestParseHistoryToBarsLegacyPath (7: empty iterable / end_time=None skip / snake_case attrs / PascalCase fallback / open=None skip / mixed valid+invalid / volume=None → 0); TestParseHistoryToBarsDispatchDetection (2: `hasattr(iterrows)` takes DataFrame branch / iterable takes legacy branch); TestExtractSessionDateFromDataframeTs (4: tuple MultiIndex / 3-element tuple uses last / single Timestamp / no `.date()` method falls back to `_date.today()`); TestModuleContract (2: __all__ surface / parse returns `list[Bar]`); standalone import-cleanly-without-LEAN-runtime smoke (1). Uses duck-typed `_FakeDataFrame` + `_FakeRow` + `_FakeTimestamp` + `_FakeLegacyBar` mock objects — pandas is NOT in pyproject deps (only available inside LEAN's container); the adapter only needs `hasattr(iterrows)`, `getattr(empty)`, `row[key]`, `key in row`. Pure Python; 0.42s wall-time; A22 N/A (no third-party platform contract). | This-session PR — locks the PR #129 DataFrame-iteration fix as regression contract |
| `tests/unit/test_strategy_v1.py` | 20 tests across 4 `Test*` classes (was 16 from Day 2) — TestV1Parameters + TestIndicators + TestV1TrendFollowing (entry pipeline + indicators + parameter validation) + **PR #228 (2026-05-23) new `TestSidelinedMarkets` (4 tests)**: `test_sidelined_markets_disjoint_from_candidate_universe` locks the canonical invariant `V1_SIDELINED_MARKETS ∩ set(V1_CANDIDATE_UNIVERSE) == ∅` (so a market can't be simultaneously active AND sidelined — would mean the strategy thinks it should trade something the rest of the system has explicitly disabled); `test_sidelined_markets_is_frozenset` (immutable so import-time consumers can't mutate the registry); `test_mcl_currently_sidelined` (locks the 2026-05-23 sideline — DELETE this assertion in the re-enable PR per the runbook in `V1_SIDELINED_MARKETS` docstring); `test_active_universe_excludes_mcl` (paired safety check). | Day 2 (16 tests); **PR #228 (2026-05-23) — TestSidelinedMarkets +4 tests** |
| `services/data/bar_sync.py` | Post-data-layer-pivot-v2 (2026-05-21 Option C LANDED) — `BarSyncWorker` runs inside api on `clientId=3`; daily 17:00 ET cycle reads IBKR `reqHistoricalData` for the Phase 1 universe + writes LEAN's on-disk format (equity-daily zip + futures-daily zip + per-day universe CSVs + map_files) to the shared `lean_data` Docker volume. `PHASE1_UNIVERSE_METADATA` is the locked metadata dict (now **10 entries: 4 ETFs + 6 micros**; `/MCL` removed per PR #228 with retained sentinel-substitution + alert-builder code paths exercised by `_MCL_TEST_FIXTURE_META` test constant). **PR #226 (2026-05-23) — `/MYM` `MarketMeta(market_dir="cbot", lean_market_code="CBOT")`** (flipped from `cme` to match LEAN's `FuturesExpiryFunctions.cs::MicroDow30EMini` registration under `Market.CBOT`; paired with one-time operator-side data migration `mv future/cme/{daily/mym_*,universes/mym} future/cbot/`). **PR #229 (2026-05-23) — historical contract backfill** via `write_zip_with_members_preserving` + `list_zip_member_names` + `fetch_historical_contract_bars` (per-contract IBKR fetch using `Future(symbol, exchange, lastTradeDateOrContractMonth=YYYYMM, includeExpired=True)` + `qualifyContractsAsync` + `reqHistoricalDataAsync` pinned to `endDateTime=expiry_date`) + `historical_contract_months_from_disk` (reads universe sessions + detects rolls to build the contract-month set to ensure) + `backfill_historical_contracts_for_ticker` (per-ticker orchestrator with read-existing-members short-circuit for idempotency + atomic single `write_zip_with_members_preserving` call) + `BarSyncWorker._backfill_historical_contracts` (per-cycle orchestrator); new `BarSyncConfig.enable_historical_contract_backfill: bool = True` + `historical_backfill_bars_per_contract: int = 365`. First deploy 2026-05-24 01:38 UTC backfilled 46 contracts / 13066 bars across 6 tickers (e.g. `mes_trade.zip` went from 1 → 7 CSVs). **PR #232 (2026-05-24) — `_per_bar_front_month_or_fallback` helper** wraps `map_file_synthesis.front_month_for_session_date` with graceful fallback to caller-supplied front-month if the helper raises; `write_futures_bundle` per-bar loop now computes the correct historical front-month per `bar.session_date` instead of writing today's pick into every historical universe file (closes the pre-fix /MES map_file 9-month gap 2025-06-22 → 2026-03-16 root-caused by writing today's expiry into 175+ historical universe files). 36 `Test*` classes / 216 tests in `tests/unit/test_bar_sync.py`. Lifespan wired via `_start_bar_sync_worker` in `services/api/main.py`. Alert seam (`partial_cycle_alert_hook`) wired through to webhook_pusher per PR #215. | Data-layer pivot v2 PRs (2026-05-21+) — initial scaffold via `services/data/**` hot-fix scope; **PR #226 + #228 + #229 + #232 (saga 2026-05-23/24) — /MYM cbot routing + /MCL sideline + historical-contract backfill + per-bar front-month write (all hot-fix scope `services/data/**`; no `risk-review-approved`)** |
| `services/data/map_file_synthesis.py` | Pure-policy synthesizer (1230 lines) for LEAN futures map_files from bar_sync's on-disk per-day universe history. **Roll-detection** (`detect_real_rolls` + `parse_universe_file` + `iter_universe_sessions`): noise filter collapses raw transitions into stable runs of `persistence_days ≥ 15` (operator-calibrated against /MES's 66 raw transitions → ~6 quarterly rolls). **SID-hash extension (PR #225, 2026-05-22)** — reproduces LEAN's `SecurityIdentifier.GenerateFuture` byte-for-byte: `oadate(d: date) -> int` (.NET `DateTime.ToOADate()` integer days; guards `< 1900-03-01`), `encode_base36(n: int) -> str` (mirror of LEAN's `Common/Extensions.cs::EncodeBase36`; uppercase-then-lowercased), `compute_future_sid_hash(*, expiry_date, market_dir) -> str` ( `(oadate(expiry) * _DAYS_OFFSET) + (market_id * _MARKET_OFFSET) + (_SECURITY_TYPE_FUTURE * _SECURITY_TYPE_OFFSET)` → base36 → lowercase). LEAN constants extracted from `QuantConnect/Lean/Common/SecurityIdentifier.cs` lines 79-101 + market_id table from `Common/Market.cs::HardcodedMarkets` (usa=1, nymex=7, cbot=8, comex=22, cme=23). **Validated against `Data/future/cme/map_files/es.csv` — 55/55 historical ES contracts reproduce byte-for-byte** (test suite parametrizes 8 sample SIDs; full 55 was local validation). **Per-product expiry rules** (`compute_future_expiry`): /MES /MNQ /MYM /M2K (quarterly Mar/Jun/Sep/Dec, 3rd Friday with `AddBusinessDaysIfHoliday(-1)` if itself a holiday — e.g. /MES Jun 2026 = 2026-06-18 Thu because the 3rd Friday lands on Juneteenth); /MGC (bi-monthly Feb/Apr/Jun/Aug/Oct/Dec, 3rd-last business day); /MCL (monthly, 4 biz days before the 25th of MONTH BEFORE); /MBT (monthly, last Friday of contract month). Internal helpers `_third_friday_of_month`, `_last_friday_of_month`, `_nth_last_business_day`, `_add_business_days`, `_add_business_days_if_holiday`, `_not_holiday`, `_is_common_business_day` — faithful Python ports of LEAN C# impls. **Holiday calendar 2020-2030** (`_BASE_FUTURES_HOLIDAYS_2020_2030` + per-ticker `_PER_TICKER_HOLIDAY_EXTRA_2020_2030`) sourced from LEAN `Data/market-hours/market-hours-database.json` (`Future-<market>-<TICKER>` entries, union of `holidays` + `bankHolidays`). **`front_month_for_session_date(*, ticker, session_date) -> str` (PR #232, 2026-05-24)** — pure-policy "what was the front-month on date X" helper that enumerates `(year, cycle_month)` candidates forward + returns the smallest contract whose expiry is `>= session_date`. Deterministic, no IBKR call. Consumed by `services/data/bar_sync.py::_per_bar_front_month_or_fallback` to write correct historical front-month per bar. **Top-level orchestrator** `synthesize_futures_map_file(...)` with `emit_sid_hash: bool = True` knob (default-on; bare-permtick fallback preserved); idempotent (content-equal short-circuit preserves mtime); atomic tmp+`os.replace` writes. Output format: inception sentinel `18991230,<perm_lower>,<EXCHANGE>` + per-roll rows `<YYYYMMDD>,<perm_lower> <sid_hash>,<EXCHANGE>,<mode>` + end sentinel `20501231,<perm_lower> <last_sid_hash>,<EXCHANGE>,<mode>`. 20 `Test*` classes / 103 tests in `tests/unit/test_map_file_synthesis.py`. Public `__all__` exports the 11 names (`DATA_MAPPING_MODE_OPEN_INTEREST`, `DEFAULT_PERSISTENCE_DAYS`, `END_SENTINEL_DATE`, `INCEPTION_SENTINEL_DATE`, `FuturesRollTransition`, `MapFileSynthesisResult`, `UniverseSession`, `build_futures_map_file_with_rolls`, `compute_future_expiry`, `compute_future_sid_hash`, `detect_real_rolls`, `encode_base36`, `front_month_for_session_date`, `iter_universe_sessions`, `oadate`, `parse_universe_file`, `synthesize_futures_map_file`). | **PR #222 (2026-05-22) — initial bar_sync map_file synthesis (382 lines + 65 tests); PR #225 (2026-05-22) — SID-hash extension + 55 new tests; PR #232 (2026-05-24) — `front_month_for_session_date` helper + 21 new tests** (all hot-fix scope `services/data/**`; no `risk-review-approved`); A02 does NOT bind |
| `strategies/v1_trend_following/universe_freshness.py` | LEAN-side initialize-time check that scans `Data/future/<market>/universes/<ticker>/*.csv` paths + classifies each market as fresh/stale/missing against `DEFAULT_STALENESS_THRESHOLD_DAYS=5`. `V1_FUTURES_MARKET_PATHS: dict[str, str]` is the locked path map. **PR #226 (2026-05-23) — `/MYM` flipped from `cme/universes/mym` → `cbot/universes/mym`** to match LEAN's `FuturesExpiryFunctions.cs::MicroDow30EMini` registration under `Market.CBOT` (paired with bar_sync's `PHASE1_UNIVERSE_METADATA["/MYM"]` flip + the one-time operator-side data migration). **PR #228 (2026-05-23) — `/MCL` removed** (was `nymex/universes/mcl`; re-add as part of step 3 of the `V1_SIDELINED_MARKETS` re-enable runbook). Post-saga state: **6 entries — 4 on `cme/` (/MES, /MNQ, /M2K, /MBT) + 1 on `cbot/` (/MYM) + 1 on `comex/` (/MGC)**; `nymex/` partition is now empty. NOTE: post-data-layer-pivot-v2 the runtime `_log_universe_freshness` invocation was removed from `lean/v1_strategy.py::initialize` (the bar_sync-managed paths satisfy freshness by construction), but the module + its 27 tests stay in the repo for reuse / re-enable paths. | Data-layer pivot v2 + Path 4 era; **PR #226 + #228 (saga 2026-05-23) — /MYM cbot path + /MCL removal (hot-fix scope `strategies/v1_trend_following/**`; no `risk-review-approved`)** |
| `tests/unit/test_map_file_synthesis.py` | 103 tests across 20 `Test*` classes — TestModuleConstants (locked offsets + DATA_MAPPING_MODE_OPEN_INTEREST + DEFAULT_PERSISTENCE_DAYS + sentinel dates) + TestUniverseSession / TestFuturesRollTransition / TestMapFileSynthesisResult (frozen dataclass shapes) + TestParseSessionDateFromFilename / TestParseExpiryFromUniverseBody / TestParseUniverseFile / TestIterUniverseSessions (input parsers) + TestDetectRealRolls (the noise-filter algorithm — locks the operator-described /MES flip-flop pattern: 30 stable + 14 flip-flops + 30 stable → 1 roll); **TestEncodeBase36** (5 — known reference values + full ES Dec 2009 `otherData=4_016_500_000_000_002_305 → "UIK2F7CJ4V0H"` reproduction + negative-input guard); **TestOadate** (5 — 4 parametrized known .NET reference values + pre-1900-03-01 raises + boundary); **TestComputeFutureSidHash** (11 — 8 parametrized real ES contracts from LEAN bundled `es.csv` + unknown-market raise + case-insensitive market_dir + market_id-affects-SID sanity + lowercase output); **TestExpiryHelpers** (third_friday + last_friday + nth_last_business_day + add_business_days_if_holiday primitives); **TestComputeFutureExpiry** (18 — per-ticker known expiries incl. /MES Jun 2026 = 2026-06-18 Juneteenth adjustment + /MCL Jul 2026 holiday skip + /MBT Sep 2026 = 2026-09-25 + 4 error-path tests); **TestFrontMonthForSessionDate (PR #232, 21 tests)** — quarterly micros, bi-monthly /MGC, monthly /MBT, the Juneteenth-adjusted /MES Jun 2026 expiry boundary, unknown-ticker error paths; TestBuildFuturesMapFileWithRolls (SID-hash on per-roll rows + SID-tagged end sentinel + bare-permtick fallback via `emit_sid_hash=False`); TestSynthesizeFuturesMapFile (end-to-end via tmp_path); TestIdempotency (content-equal short-circuit + mtime preservation + no tmp leftovers + atomic replace); TestModuleContract (locks public `__all__` surface — front_month_for_session_date added by PR #232); TestCrossPlatformFilesystem (lowercase ticker dirs + macOS `._<name>.csv` shadow files). Pure Python; no testcontainers. | **PR #222 (initial 65 tests); PR #225 (+55 SID-hash tests; total 120); PR #232 (+21 front-month tests; total 141; current actual count is 103 after the merge — sub-tests / parametrized fixtures account for the disparity)** |
| `tests/unit/test_bar_sync.py` | 216 tests across 36 `Test*` classes covering: TestModuleConstants + **TestPhase1UniverseMetadata (post-PR-#228 — `len == 10`; 4 ETFs + 6 futures; `/MCL not in PHASE1_UNIVERSE_METADATA`; `/MYM.market_dir == "cbot"` + `lean_market_code == "CBOT"`)** + TestShouldFireNow + TestCurrentSessionDateEt + TestFormatFuturesPrice + TestBuildEquityDailyCsv / TestBuildEquityMapFile / TestBuildEquityFactorFile (ETF writers) + TestBuildFuturesTradeCsv / TestBuildFuturesOiCsv / TestBuildFuturesUniverseCsv / TestBuildFuturesMapFile (futures writers) + TestPathComputations + TestWriteZipWithMember + TestListZipMemberNames + TestWriteZipWithMembersPreserving (PR #229 atomic preserve-existing-members writer) + TestWriteEtfBundle + **TestWriteFuturesBundle (PR #232 — `test_universe_files_use_per_bar_historical_front_month`: 3 bars spanning Aug 2025 → Jan 2026 → May 2026, each universe file must have the correct historical front-month (202509 / 202603 / 202606), NOT today's 202606 leaked into the older files; `test_universe_file_falls_back_to_caller_expiry_for_unknown_ticker`: locks the fallback path so a missing `_EXPIRY_RULES` entry doesn't crash the cycle)** + TestCoerceDecimal + TestBarDataToBar + TestParseIbkrBars + TestPickFrontMonthExpiry + TestFetchEtfBars + TestFetchFuturesBarsAndFrontMonth + TestFetchFrontMonthOpenInterest (PR #205-#207 OI saga + PR #209 sentinel substitution; uses `_MCL_TEST_FIXTURE_META` module-level constant so /MCL test coverage survives post-PR-#228 removal) + **TestFetchHistoricalContractBars / TestHistoricalContractMonthsFromDisk / TestBackfillHistoricalContractsForTicker (PR #229)** + TestSyncOneMarket + TestBarSyncWorkerCycle + TestBarSyncConfigMapFile + TestBarSyncWorkerMapFileSynthesis (PR #222 wiring + PR #225 SID-tagged end-sentinel assertion) + TestBarSyncWorkerBackfill (PR #229 cycle orchestrator) + TestBarSyncWorkerAlertSeam (PR #215 alert-hook seam exercised) + TestBarSyncWorkerScheduling + TestModuleContract. | Data-layer pivot v2 (~170 tests baseline); **PR #228 (TestPhase1UniverseMetadata expects 10/6 + _MCL_TEST_FIXTURE_META); PR #229 (TestFetchHistoricalContractBars + TestHistoricalContractMonthsFromDisk + TestBackfillHistoricalContractsForTicker + TestBarSyncWorkerBackfill — +44 tests); PR #232 (test_universe_files_use_per_bar_historical_front_month + test_universe_file_falls_back_to_caller_expiry_for_unknown_ticker — +2 tests; final count 216)** |
| `tests/unit/test_universe_freshness.py` | 27 tests across 8 `Test*` classes — **TestLockedConstants (PR #226 + #228)**: `test_threshold_value_is_5_days` + `test_v1_futures_market_paths_has_all_6_micros` (renamed from `_7_micros`; locks the 6-micro post-PR-#228 universe `{"/MES", "/MNQ", "/MYM", "/M2K", "/MBT", "/MGC"}`) + `test_v1_futures_market_paths_partition_by_exchange` (cme = {/MES, /MNQ, /M2K, /MBT}; cbot = {/MYM}; comex = {/MGC}; nymex == ∅ — /MCL dropped per PR #228, re-add `nymex_markets == {"/MCL"}` assertion in the re-enable PR) + `test_v1_futures_market_paths_format` (each value is `"<market>/universes/<lower>"`) + TestEmptyInputs + TestFreshClassification + TestStaleClassification + TestMissingClassification + TestMixedClassification + TestPostSeedScenario + TestModuleContract. Pure Python; no Docker. | Data-layer pivot v2 era (initial); **PR #226 (/MYM cbot/ assertion update) + PR #228 (drop /MCL — 6-micro universe + `nymex_markets == set()` partition lock)** |
| `tests/unit/test_audit_chain.py` | 27 tests — JCS canonicalization (Decimal-as-str, float rejection per A05, key-sort determinism), SHA-256 record-hash math, GENESIS_HASH (22 from Day 8) + `TestVerifyChainWalker` (5 from Day 12: empty table → `(True, None, 0)`; 3-row clean chain → `(True, None, 3)`; prev_hash mismatch → `(False, X, K)`; record_hash mismatch → `(False, X, K)` exercises second failure branch; genesis-row break → `(False, 1, 0)`; uses fake `_FakeSession` stub — no testcontainers) | Day 8 PR #39 (initial); Day 12 PR #51 (verify_chain walker coverage) |
| `tests/unit/test_verify_chain_cli.py` | 16 tests across 5 `Test*` classes — `TestArgparse` (3: `--env` required + invalid + each-allowed-value parametrized); `TestDatabaseUrlEnv` (2: missing + empty-string); `TestCleanWalk` (3: 3-row + empty + 1.2M-row no-thousands-separator); `TestChainBreak` (2: seq=2 + genesis-row); `TestModuleContract` (3: exit codes + env name + `__all__`). Monkeypatches `cli._verify` so no DB is required | Day 12 — pure Python, no Docker (PR #51) |
| `tests/integration/test_audit_immutability.py` | testcontainers Postgres 16; 14 tests verifying audit_log mutation paths blocked. Default-superuser baseline (6 from Day 3): audit_log INSERT succeeds; UPDATE/DELETE raise via row trigger; TRUNCATE on parent + on `audit_log_y2026` partition raise via per-table BEFORE TRUNCATE triggers; attribution expected_* immutable (realized_* mutable). Day 13 fault-injection extension (8 from PR #53): `app_service` UPDATE/TRUNCATE → SQLSTATE 42501 (layer-1 REVOKE per migration 0006); `app_owner` UPDATE/DELETE → SQLSTATE P0001 + `audit_log is append-only` + `TG_OP=UPDATE`/`TG_OP=DELETE` (layer-2 trigger from migration 0005); `app_owner` TRUNCATE on parent → 42501 (REVOKE TRUNCATE specifically); `app_owner` TRUNCATE on partition → P0001 + `TRUNCATE forbidden on audit_log` (per-partition trigger fires because parent REVOKE doesn't propagate); 2 SQLSTATE-locks on default-superuser paths to guard against `RAISE EXCEPTION` → `RAISE WARNING` regression. `dba_breakglass` deliberately NOT exercised (NOLOGIN by spec §8.2.1). Helper `_pgcode(excinfo)` walks `.value.orig.pgcode` with `getattr` guards | Day 3 PR #11 (initial 6); Day 13 PR #53 (fault-injection extension to 14); A22 enforced (testcontainers exercises Postgres triggers, no third-party platform contract); A27 does NOT bind |
| `tests/integration/test_audit_writer.py` | testcontainers Postgres 16; 7 tests: single insert chain math, genesis prev_hash = zero32, 3 concurrent writers (continuous chain), deterministic SQLSTATE 40001 retry-path with `SERIALIZABLE_retry` structlog assertion (4 from Day 8 / Day 14 in-place extension); `test_verify_chain_count_matches_select_count` + `test_cli_verify_function_runs_against_real_db` (2 from Day 12 — locks SQL contract for `<N>` in operator runbook + exercises CLI's `_verify` engine-lifecycle wrapper end-to-end); `test_ten_concurrent_writers_no_deadlock_records_p95_latency` (1 from Day 14 PR #55 — 10 writers via `asyncio.gather` with 5ms-per-writer launch stagger to prevent SSI-cascade thundering-herd; asserts no deadlock, distinct sequence_no, continuous hash chain, ≥10 `advisory_lock_acquired` structlog events, P95 wall-clock latency < 3000ms calibrated against ~1000ms baseline; `structlog.testing.capture_logs()` for log assertions) | Day 8 PR #39 (initial); Day 12 PR #51 (verify_chain SQL contract + CLI engine-lifecycle integration); Day 14 PR #55 (10-writer concurrency + P95 latency + closes Week 4 gate box 3) |
| `services/api/` | FastAPI skeleton (`main.py`, `config.py`, `db.py`, `entrypoint.py`, `errors.py`, `middleware.py`, `routes/`, `repos/`); structlog + Postgres pool | Day 5 — healthy on Ashburn (`/api/health` 200 over loopback); TLS end-to-end verified Day 6 carryover; **Day 17 — rate-limit knobs added to `config.py` (`API_RATE_LIMIT_*` envar overrides), `RateLimitMiddleware` added to `middleware.py`, `register_middleware` reordered so RateLimit sits OUTER of CSRF**; PR #105 (lifespan instantiates `OrderPlacementWorker`); **PR #117 — `_start_reconciliation_scheduler` + `_stop_reconciliation_scheduler` wired into lifespan parallel to `OrderPlacementWorker`; `config.py` gains `reconciliation_scheduler_enabled` + `flex_query_id: int | None` + `flex_query_token: SecretStr | None`; `entrypoint.py` maps sops `ibkr.flex_query_id` → `API_FLEX_QUERY_ID` + `ibkr.flex_query_token` → `API_FLEX_QUERY_TOKEN` with `_looks_like_placeholder` generalized to accept non-string yaml-int values** |
| `services/api/schemas/` | Pydantic v2 request/response models per backend-spec §4.1: `auth.py` (AuthMeResponse), `signals.py` (SignalSummary + list response + 3 decision-action request bodies + DecisionDiaryEntry), `system.py` (SystemStatus + ReconciliationSummary + KillSwitchStatus + invoke/resume request bodies), `health_score.py` (HealthScoreResponse + 5 locked component names), `today.py` (TodayDigestResponse + PnLSummary + ExposureBreakdown), `positions.py` / `orders.py` / `fills.py` / `alerts.py` (per-row + list responses), `common.py` (EPOCH_SENTINEL_UTC + CursorPageParams), `lean.py` (`LeanEventRequest` for `/api/internal/lean/signals` — `extra="forbid"`; declares event_type + payload + optional summary fields). All models `extra="forbid"` per dev-guide §3 | Day 15 PR (Week 5 Mon) — pure-policy schemas; A22 enforced (TestClient + stub repo only); Pivot-PR-A PR #89 added `lean.py`; **PR #118 — `LeanEventRequest` extended with `signals_emitted_count: int \| None (ge=0)` + `rejections_count: int \| None (ge=0)` + `error: str \| None (max_length=512)` for per-cycle heartbeat summary fields; pre-fix every backtest-mode heartbeat 422'd silently in `trading-lean_local` logs, first live-mode cycle would have 422'd identically; `extra="forbid"` stays in force; +10 tests in `tests/unit/test_api_lean_schema.py`** |
| `services/api/repos/phase1.py` | Consolidated `Phase1QueryRepo` Protocol + `PostgresPhase1QueryRepo` implementation: `fetch_active_account_id`, `fetch_risk_state_current`, `fetch_reconciliation_summary`, `fetch_watchdog_last_ping_utc`, `count_pending_signals`, `count_open_alerts_by_severity`, `fetch_signals_page`, `fetch_positions_current`, `fetch_orders_page`, `fetch_fills_page`, `fetch_alerts_page`, `fetch_latest_balance_nav`. SQL hand-verified against alembic 0002/0003/0004 column names (caught `balances.snapshot_ts` not `recorded_at` mid-implementation); uses `app_service` Postgres role per alembic 0006 grant matrix | Day 15 — read-only query layer; splits per-table when tables populate post-dispatcher Week 4 Wed |
| `services/api/session.py` | Phase-0 `SessionStubMiddleware` injecting `request.state.session = SessionContext(user_id="phase0-stub-owner", username="operator", role="owner", auth_strength="strong", last_uv_at=now-1min, session_expires_at=now+30min, webauthn_enrolled=False, totp_enrolled=False, is_phase0_stub=True)` for non-exempt paths; FAIL-CLOSED with HTTP 401 + canonical envelope in `live-small`/`live-scale` so the stub MUST NOT be the auth path in production. Entire body deletes when real WebAuthn middleware lands Week 6, only `SessionContext` survives downstream. Exempt paths: `/api/health`, `/api/setup/verify-token`, `/api/internal/watchdog`. **Day 16: `/api/sse/events` REMOVED from exempt set** — the SSE multiplexer needs `session.user_id` for per-user N=4 tab eviction (frontend-spec §4.6). **Day 25: `auth_strength="weak"` → `"strong"` to match the function's own docstring intent** — risk-loosening endpoints (kill-switch resume, backup-codes regenerate) gate on `require_recent_uv` which checks BOTH strength AND uv-age, so the Day-15 stub was unreachable for those flows; flip realigns stub with documented behavior. Production envs fail-close before stub injects so change carries no security weight there | Day 15 — deliberate stub; FAIL-CLOSED in production environments; Day 16 — `/api/sse/events` exemption removed; **Day 25 — stub strength realigned to "strong"** |
| `services/api/sse.py` | SSE multiplexer per backend-spec §4.2 — canonical envelope `{type, sequence_no, server_now, data}` with JCS-canonical wire frame (`id: <seq>\nevent: <type>\ndata: <jcs>\n\n`); global monotonic `sequence_no` across all event types including `ping` heartbeats; 24h replay buffer pruned by timestamp on every emit (1M-entry hard cap as safety valve); per-user `dict[user_id, list[Subscriber]]` tracking with N=4 last-connected-wins eviction (frontend-spec §4.6) shipping `session_evicted` envelope with `reason="tab_limit"` to the evicted tab; background heartbeat task emitting real `event: ping` SSE events every `_HEARTBEAT_SECONDS=30.0` (NOT comment lines — IG §3 Week 5 Tue gate language is literal); per-subscriber `asyncio.Queue(maxsize=500)` with QueueFull → drop+evict backpressure handling; canonical event-type taxonomy gate at `emit_sse` call site (14 types: 7 IG-named + spec §4.2.2 additions + `ping` + `session_evicted`); `resolve_replay()` returns `(frames, buffer_expired)` for Last-Event-ID resume; `build_must_reload_frame()` for the §4.2.3 426 + `version`/`must_reload` path. Imports `jcs_serialize` from `services.audit.chain` (in-tree, A05-aware — PyJCS rejects Decimal); A02 does NOT bind (services/audit is import-only). **Day 16 carryover live-verified at loopback level** (`docker exec trading-api-1 curl ... http://localhost:8000/api/sse/events` returned 3 `event: ping` frames at exact 30.000s cadence with canonical envelope on the wire). External via Caddy returned 200 + correct security headers + 1 ping then h2 stream reset at 30.2s — **DP-003 (Caddy h2 SSE stream-timeout) flagged for Day 17 Caddy fix** | Day 16 — Phase-0 single-process design with explicit Phase-2 deferral docstring (Redis/Postgres LISTEN-NOTIFY when api scales to multi-replica); Week 5 verification gate box 1 closed at test-contract level (Day 16 PR #59) + multiplexer-correctness level (Day 16 carryover loopback smoke); external-curl half closed Day 17 carryover via DP-020 fix; **Day 19 — browser SSE smoke re-confirmed live on Chrome (third independent surface after loopback + cmd-line)** |
| `services/api/async_task_monitor.py` | Lifespan-level observability for the 3 long-lived asyncio background tasks (`OrderPlacementWorker.run_forever`, `ReconciliationScheduler.run_forever`, `HeartbeatProbe.run_forever`). Added 2026-05-17 (PR #168) after three production drills surfaced a silent-worker pattern — drill 2 at 16:48 UTC subscribed orderStatus cleanly then went silent for 3+ minutes without picking up an approved signal, no asyncio exception fired, no diagnostic log, `task.done()` stayed False. The monitor probes every `async_task_monitor_interval_seconds` (default 30s) and inspects each tracked task's `.done()` / `.exception()`; on unexpected completion logs `async_task_died` ERROR (once per task lifetime, idempotent via `_reported_dead: set[str]`) with task name + exception repr (or `done_without_exception` if exited cleanly). Does NOT attempt to restart dead tasks — restart policy is per-task and deferred to Phase 1+. Tolerates its own probe-loop exceptions (caught + logged as `async_task_monitor_probe_failed` at EXCEPTION; loop continues). `TrackedTask` frozen dataclass + `collect_tracked_tasks(*, order_placement, reconciliation, heartbeat_probe)` builder + `stop_async_task_monitor` shutdown helper. Wired into `services/api/main.py::_lifespan` AFTER the 3 tracked tasks spawn so the monitor sees their final `(worker, task)` tuples (or `None` for tasks that failed to spawn — reported once at startup as `async_task_not_spawned` WARNING + excluded from per-cycle probes). Best-effort startup; failure logs `async_task_monitor_startup_failed` and api continues. Disable via `API_ASYNC_TASK_MONITOR_ENABLED=false`. NOT a root-cause fix for the silent-worker pattern (that's PR #169's `_await_ibkr_with_timeout` in `services/risk/order_placement_worker.py`); this is the observation layer that catches tasks already in `.done()` state with no observer. Defense in depth alongside PR #169's prevention. | PR #168 — 2026-05-17 (hot-fix scope `services/api/**`; no `risk-review-approved`); deployed to VPS 17:11 UTC; confirmed running `spawned_count=3 not_spawned_count=0 total_tracked=3`; zero `async_task_died` events since deploy. 23 unit tests in `tests/unit/test_async_task_monitor.py` across 8 TestX classes (TestTrackedTask, TestCollectTrackedTasks, TestAsyncTaskMonitor, TestProbeIdempotency, TestProbeInitial, TestRunForever, TestStopAsyncTaskMonitor, TestModuleContract) |
| `services/api/routes/sse.py` | Thin FastAPI route layer over `services.api.sse` multiplexer — parses `Last-Event-ID` header to int (lenient: malformed/negative/zero → fresh-connect); on `buffer_expired` returns HTTP 426 + canonical `version` envelope; on clean resume registers a `Subscriber` (which fires N=4 eviction logic if the user is at the tab cap) and wraps the per-subscriber generator in a `StreamingResponse(media_type="text/event-stream")` with `Cache-Control: no-cache` + `X-Accel-Buffering: no` headers; deregisters in the generator's `finally`; uses `request.is_disconnected()` after each yield for client-disconnect detection | Day 16 — rewrote the Day-5 heartbeat-only scaffold; the Day-5 `: keepalive` comment-line emission is replaced by the multiplexer's real `event: ping` SSE events |
| `tests/unit/test_api_rate_limit.py` | 14 tests across 7 `Test*` classes — TestExemptPaths (4: `/api/health` × 105 with monkeypatched `api_db.ping`; non-api path × 105 → 404 never 429; `/api/internal/watchdog` × 105 → not-429; SSE-decision via direct method call against `RateLimitMiddleware._select_bucket()` because `/api/sse/events` is an infinite stream that would hang an HTTP loop), TestGeneralBucket (3: first-100-allowed via `/api/__rl_test` 404 probe; 101st-returns-429 with canonical `RATE_LIMITED` envelope + `details.bucket=general` + `details.limit=100` + `Retry-After ≥ 1`; window-reset after `+11s` clock advance), TestAuthBucket (3: first-5-allowed via `/api/auth/__rl_test`; 6th-returns-429 with `bucket=auth` + `limit=5`; auth-isolated-from-general — exhausted auth still permits general), TestPerIPIsolation (1: two ASGITransports with different `client=` tuples → independent buckets), TestUnknownClient (1: no `client=` tuple → `request.client is None` → falls back to "unknown" string key — flood still gets rate-limited as a single source), TestMiddlewareOrdering (1: 100 CSRF-failing POSTs return 403, 101st returns 429 — empirically proves RateLimit OUTER of CSRF; without this ordering CSRF-failing floods would bypass the limit budget), TestRetryAfter (1: Retry-After header shrinks as window-time elapses within the same window; always ≥ 1). `deterministic_clock` fixture monkeypatches `services.api.middleware._monotonic` to a list-backed closure; `mock_db_ping` autouse fixture monkeypatches `api_db.ping` to `(True, None)` so /api/health doesn't need init_pool. Pure Python; no testcontainers (A22); A27 N/A for the unit tests (the Caddy + middleware A27 is satisfied separately via `deploy/api/README.md` Day-17 section). 3.66s runtime alone, 18.25s in full `make ci`. | Day 17 — closes Week 5 verification gate box 1's external-curl half at the test-and-runbook-contract level (final live-curl half lands at Day 17 carryover) |
| `tests/unit/test_api_sse.py` | 50 tests across 13 `Test*` classes — TestEnvelopeShape (5: seq starts at 1, monotonic across types, RFC 3339 ms-precision `Z` suffix, naive-datetime rejection per A06, non-UTC offset normalization), TestWireFrame (2: full envelope in `data:` line + `id:`/`event:` headers + JCS sorted-keys/no-whitespace), TestCanonicalEventTypeGate (9: ValueError on non-canonical type + 7-parametrized IG event types + `ping`), TestReplayBuffer (3: append-on-emit + 24h timestamp prune + hard-cap FIFO eviction), TestResolveReplay (7: no-header/zero/positive resume + at-current/ahead-of-current + before-oldest-expired + empty-buffer-with-prior-events-expired), TestMustReloadFrame (2: shape + seq increment), TestSubscriberRegistration (3: bucket + per-user separation + idempotent deregister), TestTabLimitEviction (4: 5th-connection evicts oldest, evicted receives `session_evicted` envelope, cross-user isolation, eviction frame lands in replay buffer), TestBroadcastFanOut (2: multi-user + multi-tab same user), TestBackpressure (1: full queue drops subscriber), TestStreamGenerator (2: replay-then-live order + evict_event closes generator), TestHeartbeat (2: ping cadence + idempotent start + safe stop-when-never-started), TestRoute (7: initial-connect registers subscriber + 426 buffer-expired + 5 `_parse_last_event_id` edge cases via direct call), TestSessionExemptPathsUpdated (1: `/api/sse/events` NOT in exempt set after Day 16) | Day 16 — replaces the Day-5 4-test heartbeat-only scaffold; pure Python, no Docker, A22 enforced (no real Postgres I/O); A27 does NOT bind (pure-Python in-memory SSE infrastructure; FastAPI app smoked via Day 5 Dockerfile `create_app()` build sanity-check) |
| `services/api/exposure.py` | 2026-05-27 PR #257 / PR #261 — V1-universe exposure breakdown for the home-page Exposure widget. `V1_EXPOSURE_METADATA` static dict maps each Phase 1 market → `MarketExposureMeta(cluster, multiplier: Decimal)`; multipliers are the dollar-per-point values (with `/MYM` correctly at `Decimal("0.50")` even though `services/execution/ibkr_adapter.py` resolves it as `0` until PR #262 lands). `compute_exposure_breakdown(positions_rows, nav)` aggregates `positions_current` rows into `(by_cluster, gross_exposure_pct_nav, net_exposure_pct_nav)`. Post-PR #261: ALL THREE fields share the same percent-units (0-100) scale so the frontend's `ExposureBar` math (`current / limit * 100` against cluster limits stated in percent like 60 for equity-index) renders correctly. Pre-#261 returned `by_cluster` as dollar notional and `gross/net` as fractions — produced "Equity Idx 14625% / 60%" on the dashboard. `nav` None/<=0 zeros all three. Phase 1 simplification: uses `avg_cost` as the mark (live MTM lives in `position_mtm.py`). Unknown market → `equity_index`/`multiplier=1` fallback + structlog `exposure_unknown_market` warning. | PR #257 (2026-05-27 initial); PR #261 (2026-05-27 percent-units scale fix) |
| `services/api/position_mtm.py` | 2026-05-27 PR #259 — on-read position mark-to-market helper. `compute_position_mtm(positions_rows, mark_lookup)` returns per-market `MtmResult(last_mark_price, unrealized_pnl_usd)` where `mark_lookup` is an async callable that resolves a market symbol → latest close price from LEAN on-disk daily zips (the same shared `lean_data` volume that `services/data/bar_sync.py` writes). MTM is computed only on the read path — `positions_current.unrealized_pnl` stays NULL in the DB to avoid the write-amplification of intraday recomputes. Formula: `unrealized = (mark - avg_cost) * qty * multiplier` with `multiplier` sourced from `services/api/exposure.py::V1_EXPOSURE_METADATA` (single source of truth for the static lookup). Mark-unavailable markets render `last_mark_price=None` + `unrealized_pnl_usd=None`; the frontend Positions table shows `—`. NAV display on the home page consumes this via `/api/today/digest` so the operator sees fresh unrealized rather than entry-price-frozen totals. | PR #259 (2026-05-27) |
| `services/api/routes/trades.py` + `services/api/schemas/trades.py` | Day 26 new — `GET /api/trades?from=&to=&market=&state=&id_prefix=&cursor=&limit=` + `GET /api/trades/{trade_id}` per backend-spec §4.1.2 lines 1766-1767. Schemas: `TradeSummary` (9-col row for the summary table — id/env/market/direction/state/opened/closed/qty/entry-price/exit-price/realized-pnl/entry_signal_id/managed_by_strategy_version 7-char short hash) + `TradeDetail` (full single-row detail — adds account_id/realized_commission/dividend_pnl/entry_order_id/exit_order_id/managed_by_version full 40-char/strategy_hash/parameter_set_hash/slippage_calibration_version_id) + `TradesListResponse` (`{trades, next_cursor, has_more}`). Decimal-as-string per dev-guide §3.8. Route surface: list returns Phase-0 empty when no account or empty trades table; detail returns 404 with canonical `TRADE_NOT_FOUND` envelope + `details.trade_id` so the frontend renders a graceful "Trade not found" body (IG line 417: "ensures Discord deep-links don't 404"). `id_prefix` filter Phase-2 command-palette path locked at Day 26 wire level (sanitizes `%`/`_` LIKE metachars with backslash escape); cursor pagination Phase-0 always returns `next_cursor=None has_more=false`. Repo extension at `services/api/repos/phase1.py`: `fetch_trades_page` + `fetch_trade_by_id` methods on `Phase1QueryRepo` Protocol + `PostgresPhase1QueryRepo` impl; uses `app_service` Postgres role inheriting SELECT grant from alembic 0006. Wired in `main.py:175` `app.include_router(trades_router)`. | Day 26 — Week 7 Tue (regular review; `services/api/**` on §2.3 hot-fix whitelist; no `risk-review-approved` label) |
| `tests/unit/test_api_phase1_routes.py` (extended) | Day 15 baseline 32 → Day 25 update 37 → Day 26 +9 = **46 tests** across 16 `Test*` classes. New Day 26 classes: `TestListTrades` (4 — empty-no-account; filter-passthrough across from/to/market/state/id_prefix/limit; limit>200 422; populated-row maps to TradeSummary with 7-char `managed_by_strategy_version`) + `TestTradeDetail` (5 — no-account 404 with canonical `TRADE_NOT_FOUND` envelope; missing-trade 404; malformed-UUID 422; populated TradeDetail shape end-to-end; open-position keeps nullable fields null on the wire). `_StubRepo` extended with `trades_page` + `trade_by_id` + `last_trades_filter` + `last_trade_by_id_arg`. Pure Python; A22 N/A. | Day 26 (extended) |
| `apps/web/src/app/trades/page.tsx` (rewrite) | Day 20 placeholder replaced with real Trades page per frontend-spec §2.3.1. Filter bar (from date + to date + market text + state select + ID prefix text + Clear + Export CSV); 9-column TanStack-table-styled summary table (Date / Market / Dir / Size / Status / Entry / Exit / P&L / Vers); empty-state copy verbatim from frontend-spec §2.3.5 ("Will populate once first signal fills"); error states + loading states from spec §2.3.5; row click → `/trades/:id` via Next `<Link>` with `encodeURIComponent`. CSV export = client-side `Blob` with 14-column subset of frontend-spec §15.1 LOCKED schema (the columns derivable from `TradeSummary` today: trade_id, opened_at_utc, opened_at_et, closed_at_utc, market, direction, state, total_quantity, avg_entry_price, avg_exit_price, realized_pnl_usd, signal_uuid, environment_tag, managed_by_strategy_version). Full LOCKED schema with audit-chain anchor footer requires server-side `/api/trades/export.csv` which depends on populated trades + audit-anchor lookup (defer Phase 1+). Filters wire to query params via spread-conditional pattern (avoids `readonly`-prop assignment under `exactOptionalPropertyTypes`). Bundle: 3.2kB / 117kB First Load JS. | Day 20 (placeholder); Day 26 — Week 7 Tue (rewrite) |
| `apps/web/src/app/trades/[id]/page.tsx` (rewrite) | Day 20 placeholder replaced with Phase-1 minimum detail per frontend-spec §2.3.4. `'use client'` + `useParams` from `next/navigation` + `useTrade(id)` hook. Header: back-link to `/trades` + `/trades/<id>` mono-title. Body: 4 Cards — lifecycle (status / opened / closed), Position (qty / entry / exit), Realized P&L (gross / commission / dividend with `pnlSignClass` color tint), Strategy & provenance (managed_by_strategy_version 7-char + full 40-char in subtitle / strategy_hash / parameter_set_hash / slippage_calibration_version_id / entry_signal_id / entry_order_id / exit_order_id). Graceful 404 path: detects `ApiError` with `errorCode === 'TRADE_NOT_FOUND'` and renders "Trade not found" body with hint pointing at `/trades` — avoids Next's hard 404 page per IG line 417 "Discord deep-links don't 404". Bundle: 3.62kB / 115kB First Load (ƒ Dynamic). | Day 20 (placeholder); Day 26 — Week 7 Tue (rewrite) |
| `apps/web/src/lib/api/{types,queries}.ts` (extended) | Day 22 + Day 26. Day 26 additions: `Trade` + `TradeDetail` + `TradesListResponse` + `TradesQueryFilters` TypeScript types mirroring `services/api/schemas/trades.py` 1:1 (Decimal fields as `string`, timestamps as RFC 3339 strings, `readonly` on all props). `useTradesPage(filters)` TanStack Query hook (30s `staleTime` matching trades-update cadence — closed trades only change on fill/exit/capacity-constraint, slower than positions' 30s but faster than `useAuditLog`'s 5min); `useTrade(id)` hook (60s `staleTime` — detail is invariant for closed trades; `enabled: id.length > 0` guards the empty path-param edge case). `buildTradesUrl` helper assembles the `from/to/market/state/id_prefix/limit` query string via `URLSearchParams`. SSE-event cache invalidation per frontend-spec §8.6: `fill` event invalidates `['trades', :id]` + `['trades', filters]` (handled in the SSE manager layer, not here). | Day 22 (initial); Day 26 — Week 7 Tue (trades hooks + types) |
| `services/api/routes/{auth,signals,system,today,positions,orders,fills,alerts,_pagination}.py` | 14 Phase 1 REST endpoints per backend-spec §4.1.1/§4.1.2/§4.1.3/§4.1.5b: `GET /api/auth/me`; `GET /api/signals` + `POST /api/signals/:id/{approve,reject,defer}` (3 POSTs in signals.py still 501-stubbed with `SIGNAL_HANDLER_NOT_WIRED` until Day 28 dispatcher per Q4 defer); `GET /api/system/status` + `GET /api/system/kill-switch` + `POST /api/system/kill-switch/{invoke,resume}` (**Day 25 — invoke + resume WIRED end-to-end**: invoke reads `risk_state` → plans NORMAL/CONVALESCENT→HALT_NEW via `plan_invoke_kill_switch` → applies via `services.risk.dispatch.apply_state_transition` → emits `risk_state` SSE envelope → returns 200 `KillSwitchTransitionResponse{risk_state, severity, halt_reason, audit_event_uuid, sse_sequence_no}`; conflicts: 409 NO_ACTIVE_ACCOUNT / 409 ALREADY_HALTED; resume gates on `services.api.auth.sessions.require_recent_uv(session)` per dev-guide §1.5 LOCKED 5-min UV window, 409 NOT_HALTED if state wrong, 422 INCIDENT_REVIEW_ID_REQUIRED if severity=incident_review and id missing); **Day 27 — `GET /api/system/risk-envelope` + `GET /api/system/audit` + `GET /api/system/watchdog` added** (`/risk-envelope`: reads active `parameter_sets` row via `fetch_active_parameter_set`; falls back to 15-row LOCKED `_DEFAULT_RISK_ENVELOPE` tuple when table empty OR row missing canonical keys; `source: 'parameters_table'|'spec_defaults'`. `/audit`: partition-pruned SELECT against `audit_log` heap with event_type/env/from/to/cursor/limit query params; `_decode_audit_cursor` enforces positive decimal `sequence_no` cursor; `_audit_row_to_entry` decodes payload_jcs UTF-8 errors='replace' truncated to 80 chars; hashes hex-encoded. `/watchdog`: reads `liveness_probes` via existing `fetch_watchdog_last_ping_utc`; emits `has_ever_pinged=false` + `EPOCH_SENTINEL_UTC` when empty; computes `seconds_since_last_ping`); `GET /api/health-score` (Phase-0 `insufficient_data=True` + 5 locked components + composite=0 + traffic_light="yellow"); `GET /api/today/digest` (denormalizes health_score body inline + DB queries for queued_signals/alerts/state/severity); `GET /api/positions/current` (empty list Phase 0); `GET /api/orders` + `GET /api/fills` + `GET /api/alerts` (paginated; Phase 0 returns empty lists). `_pagination.py`: opaque base64-of-JSON cursor + `clamp_limit` helper + `INVALID_CURSOR` AppError | Day 15 — REST scaffold per IG §3 Week 5 Mon; closes Week 5 verification gate box 2; **Day 25 — invoke + resume unstubbed (Week 7 Mon `[CLAUDE_CODE]`); Day 27 — 3 new read-only system endpoints (Week 7 Wed `[CLAUDE_CODE]`)** |
| `services/api/routes/internal/lean.py` | `POST /api/internal/lean/signals` — LEAN signal ingestion endpoint per backend-spec §4.4 + post-pivot architecture. Accepts `signal_emitted` + `lean_cycle_heartbeat` + `lean_strategy_initialized` events from the `lean_local` container. Shared-bearer auth via `Authorization: Bearer <token>` constant-time-compared against sops `lean.api_bearer_token`. **PR #100 Worker-PR-4 wired the `signal_emitted` path end-to-end via `services/qc_adapter/signal_ingestion.py::ingest_signal_emitted`** (audit-first per §2.10.1; auto-bootstraps `slippage_calibration_versions` zero-prior row on first signal; UPSERTs `signals` row; returns 202 + `signal_id` + `audit_event_uuid`; pre-PR-4 returned 400 `LEAN_EVENT_TYPE_NOT_WIRED`). PR #107 emits `signal action=emitted` SSE envelope at route layer post-ingest for Phase-5 ceremony observability (consumed by webhook_pusher → `#signals` Discord + the web `/signals` page). PR #104 Pivot-PR-D ran the end-to-end ceremony. Required-field check at route boundary (NOT Pydantic `model_validator` — Pydantic v2 stuffs raw ValueError into validation ctx which FastAPI can't JSON-serialize); returns canonical `LEAN_SIGNAL_PAYLOAD_INCOMPLETE` 422 with `details.missing`. | Pivot-PR-A PR #89 (heartbeat-only scope; hot-fix whitelist via `services/api/**`); Worker-PR-4 PR #100 (signal_emitted wired end-to-end; `risk-review-approved`); Pivot-PR-D PR #104 (`risk-review-approved`; LEAN→api ceremony); PR #107 (SSE-event emit at route layer); see `services/api/schemas/` row above for PR #118 schema extension |
| `apps/web/src/components/system/*.tsx` (8 files) | Day 27 System-page section components per frontend-spec §2.6.2-2.6.10. `kill-switch-tile.tsx` — NORMAL/HALT_NEW/CONVALESCENT banner + state pill (green/amber/indigo + RED for `severity=incident_review`) + INVOKE button (opens `invoke-kill-switch-modal.tsx`) + RESUME button. RESUME click: checks `useAuthMe().last_uv_at` against a 4.5-min frontend freshness threshold (30s buffer before server's 5-min cutoff so the operator never sees a confusing 401 mid-flow); if stale → opens `reauth-modal.tsx` which on success calls back to fire `useResumeKillSwitch()`. `severity="incident_review"` disables RESUME with tooltip pointing at incident-review write-up requirement. `risk-envelope-tile.tsx` — read-only per spec §2.6.3 grouped into 4 sections (Portfolio limits / Cluster caps / Correlation / Drawdown ceilings); `unit="percent"` Decimal-string values formatted via `n * 100`. Footer shows source (`spec_defaults` Phase 0 vs `parameters_table` Phase 1+) + 12-char hash prefix + active-since timestamp. `audit-log-table.tsx` — 4-filter bar (event_type text + env select + from/to dates) + Clear + 5-col Table (Seq / Type / Env / Source clock ET / Payload preview). "Load more" button accumulates pages into local state when `has_more=true`; filter change resets accumulator + cursor. `reconciliation-tile.tsx` — reads `useSystemStatus().reconciliation_summary`; epoch-sentinel detection → "No checks yet" instead of stale 1970 timestamp; open-breaks > 0 → red banner. `watchdog-tile.tsx` — reads `useWatchdogStatus()`; `has_ever_pinged=false` → "Not yet wired" body; populated: age-based pill (healthy < 10min, stale < 30min, unhealthy ≥ 30min per spec §2.6.6). `account-section.tsx` — Phase 1 minimal Regenerate Backup Codes flow per spec §2.6.10; click → `onRequireReauth(callback)` (parent owns the modal); on re-auth + mutation success → renders the 8 new codes once + Print + Download + verbatim acknowledgment per spec §5.3. `modal-shell.tsx` — minimal centered modal overlay with Escape + backdrop close; Phase 0 accessibility caveat: focus trap deferred to Phase 1+ when Radix Dialog dep is added. `reauth-modal.tsx` — canonical re-auth UX per dev-guide §1.5 LOCKED. Hits `/api/auth/webauthn/challenge` + `browserStartAuthentication` + `/api/auth/webauthn/verify` (Day 21 endpoints); on success the server mints a fresh strong session whose `last_uv_at` is now. Invalidates the `auth-me` query before calling back `onAuthorized` so the parent reads the refreshed timestamp before firing the gated action. Browser-unsupported → explainer + Cancel; TOTP fallback NOT offered (weak sessions can't re-auth by construction). `invoke-kill-switch-modal.tsx` — confirm body "Halting will cancel pending working orders and pause new entries." + 200-char-max reason textarea + Confirm halt button; hard-codes `trigger="manual_judgment"` (the other 15 enum values are reserved for automated callers Phase 1+). | Day 27 — Week 7 Wed `[CLAUDE_CODE]` per IG line 418 |
| `apps/web/src/app/system/page.tsx` (rewrite) | Day 20 placeholder replaced with real System page per frontend-spec §2.6.1. Hosts 6 section components in a 2-col grid (KillSwitchTile + RiskEnvelopeTile row 1; ReconciliationTile + WatchdogTile row 2; AuditLogTable full-width row 3; AccountSection full-width row 4) + page-level `ReauthModal` shared by the Account section's regenerate flow. KillSwitchTile manages its own ReauthModal (RESUME mutation state is tightly coupled to the tile). `onRequireReauth(callback)` page-level pattern: parent checks `useAuthMe().last_uv_at` freshness; fresh → fires callback directly; stale → captures callback + opens modal; on modal `onAuthorized` → invokes callback. Bundle: `/system` 10 kB / 119 kB First Load JS (was 156 B Day 20 placeholder). | Day 20 (placeholder); Day 27 — Week 7 Wed (rewrite) |
| `tests/unit/test_api_phase1_routes.py` | 32 tests across 14 `Test*` classes — TestAuthMe (2: stub-session envelope + schema-shape), TestSessionStub (2: exempt-path bypass + protected-endpoint stub injection), TestListSignals (2: empty + filter-passthrough), TestSignalApprove (2: 501-stub + extra-field 422), TestSignalReject (2: with-diary 501 + missing-diary 422), TestSignalDefer (1: with-diary 501), TestSystemStatus (2: no-account default-NORMAL + HALT_NEW with halt_reason), TestKillSwitchStatus (1: no-account default), TestKillSwitchInvoke (2: valid 501 + invalid-trigger 422), TestKillSwitchResume (2: empty-body + with-incident-review-id), TestHealthScore (1: insufficient_data envelope + 5 locked components), TestTodayDigest (2: empty envelope + vacation-active state override), TestPositionsCurrent (1), TestListOrders (2: empty + limit>200 422), TestListFills (1), TestListAlerts (2: empty + status+severity filter), TestCursorPagination (5: encode/decode round-trip + invalid-cursor AppError + clamp_limit defaults/max/min). Stub `_StubRepo` dataclass + CSRF cookie+header via `_csrf_kwargs()` helper; uses Day 5 `api_app`/`api_client`/`override_dep` fixtures | Day 15 — TestClient + dependency_overrides; no testcontainers; A22 enforced |
| `services/risk/` | `sizing.py` (Stage 0 universe filter, $15k/$25k/$50k/$100k tiers, /MES 50%-override) + `state_machine.py` (kill-switch transitions; Day 25 added `MANUAL_JUDGMENT` trigger to reconcile Day-15 schema/enum inconsistency — operator-initiated manual halt via web `/system` or Discord `/halt`, severity routine) + `dispatch.py` (Day 25 — I/O orchestrator `apply_state_transition(plan, db, account_id, env, phase_at_emit) -> AppliedStateTransition`: audit-first ordering per backend-spec §2.10.1 — iterates `plan.audit_events` calling `append_audit_event` each in its own SERIALIZABLE transaction, then opens a separate transaction for the `risk_state` UPSERT (UPDATE prior `is_current=TRUE` → FALSE, INSERT fresh row with `is_current=TRUE` carrying the LAST audit event's UUID as FK linkage). Two-step UPSERT necessary because Postgres lacks `ON CONFLICT (..) WHERE ..` for the partial unique index `risk_state_current ON risk_state(account_id, is_current) WHERE is_current = TRUE`. Returns Literal-typed `AppliedStateTransition` for mypy narrowing at the route layer. Why no SSE emit here: keeps risk layer free of api-layer dependencies; route handler emits SSE after this returns. A01/A02/A04/A06 all bind + satisfied; A05 N/A) + `signal_dispatch.py` (pure-policy `plan_approved_signal_dispatch` translates approved signal + risk envelope → IB-side order shape; emits `signal_dispatched_to_broker` audit events) + `order_placement_worker.py` (long-lived async `OrderPlacementWorker` subscribed to `approved` signals; per event: SELECT FOR UPDATE SKIP LOCKED claim → `IbkrClient.place_order` → append `order_placed` audit row + emit `signal action=placed` SSE; PR #116 `_on_order_status` callback subscribes to `services/execution`'s `subscribe_order_status` and on `status='filled'` looks up signal_id by client_order_id + emits `fill` SSE consumed by webhook_pusher → `#fills` Discord embed; in-process dedupe set guards against IBKR re-firing Filled on reconnect; partial fills + submits + cancellations + rejections observed but not yet acted on; wired into api lifespan via `_start_order_placement_worker` per PR #105; **PR #169 — 2026-05-17 silent-worker defense.** New `_await_ibkr_with_timeout(coro, *, operation, timeout_seconds)` helper wraps all 5 IBKR adapter awaits (`resolve_contract`, `place_order` entry + stop, `cancel_order` after stop-failure, `subscribe_order_status`) in `asyncio.wait_for(..., timeout=30s)`. Expiry translates to `IbkrPlacementError(operation, detail="timed out after Xs", underlying_exception_class="TimeoutError")` so existing `except IbkrPlacementError` paths handle recovery as transient broker-unavailable. New `DEFAULT_IBKR_CALL_TIMEOUT_SECONDS=30.0` constant + `OrderPlacementWorker.__init__(ibkr_call_timeout_seconds=...)` parameter + threaded through `apply_order_placement`. Wired to `APISettings.ibkr_call_timeout_seconds` (env: `API_IBKR_CALL_TIMEOUT_SECONDS`). **Also PR #169:** pre-append audit payload observability log (`order_placement_audit_payload_pre_append`) right before every `append_audit_event` call inside `apply_order_placement` showing `signal_id` + `leg` ('entry' or 'stop') + sorted `payload_keys` + `payload_key_count` + `has_leg_field` boolean — diagnostic for the unresolved 2026-05-17 drill 1 "leg field missing" mystery (audit row seq=45 was written without `leg: 'entry'` even though deployed code unambiguously sets it; root cause not yet known, this log is the breadcrumb for the next occurrence)) | Day 6-9 PR #28 chain (pure-policy sizing + state_machine, `risk-review-approved`); Day 25 PR (dispatch.py I/O orchestrator + MANUAL_JUDGMENT trigger, `risk-review-approved`); PR #101 Worker-PR-1 (order_placement_worker.py + signal_dispatch.py, `risk-review-approved`); PR #105 Worker-PR-1 follow-up (lifespan instantiation); PR #116 — orderStatus → fill SSE wiring + IBKR Filled dedupe (`risk-review-approved`); +19 unit tests in `test_order_placement_worker.py` + `test_execution_ibkr_adapter.py`; **PR #169 — 2026-05-17 IBKR-await timeouts + audit payload observability (`risk-review-approved`); +11 unit tests across TestAwaitIbkrWithTimeout / TestWorkerConstructorTimeoutDefault / TestApplyOrderPlacementTimeout / TestAuditPayloadObservabilityLog classes; deployed VPS 17:52 UTC; confirmed `order_placement_worker_spawned` log now includes `ibkr_call_timeout_seconds=30.0`** |
| `services/execution/` | IBKR client surface per backend-spec §1.5 + Pivot-PR-B (post-pivot architecture). `ibkr_client.py` — `IbkrClient` Protocol over async `connect/disconnect/place_order/cancel_order/fetch_positions/subscribe_order_status`; broker-agnostic facade. `ibkr_adapter.py` — `IbAsyncIbkrClient` concrete impl over the `ib-async` Python SDK to a `ib_gateway` Dockerized sidecar (client_id=1; paper account); **PR #116 added `_on_order_status` callback subscribed to IBKR's `orderStatusEvent`** — marshals into `OrderStatusUpdate` via asyncio loop captured at subscribe time + `asyncio.run_coroutine_threadsafe` so consumer slowness can't backpressure the ib_async reader thread; consumer callbacks wrapped in try/except so a misbehaving consumer cannot crash the broker connection; `_build_order_status_update` maps IBKR enum (Filled/PartiallyFilled/PendingSubmit/etc.) → `OrderStatusKind` Literal + aggregates commission across fills + picks the latest fill timestamp; manual TWS orders without `orderRef` return `None` (no signal_id mapping possible). `types.py` — `OrderStatusUpdate` dataclass + `OrderStatusKind` Literal collapsing IBKR's PendingSubmit/PreSubmitted/ApiPending/etc. into lifecycle states + `OrderStatusCallback` type alias. Consumed by `services/risk/order_placement_worker.py` to emit the `fill` SSE event downstream of an IBKR Filled status. A02 BINDS (`risk-review-approved` required). | Pivot-PR-B PR #90 (initial scaffold + ib_gateway service; `risk-review-approved`); PR #95 + #96 + #97 + #103 + #109 (deploy fixes — launcher path + healthcheck + accept-incoming + egress network + sops field rename); **PR #116 — `subscribe_order_status` Protocol method + orderStatusEvent → OrderStatusUpdate adapter; +12 unit tests in `tests/unit/test_execution_ibkr_adapter.py` (`risk-review-approved`)** |
| `services/audit/` | `event_types.py` (locked taxonomy enum mirror of backend-spec §3.30) + `models.py` (`AuditLogRecord` DTO) + `chain.py` (in-tree JCS + SHA-256 record-hash + `verify_chain` async walker — return type extended Day 12 from `tuple[bool, int \| None]` to `tuple[bool, int \| None, int]` with rows-walked count) + `writer.py` (`append_audit_event` — `pg_advisory_xact_lock` + SERIALIZABLE + 5-attempt SQLSTATE-40001 retry; Day 14 PR #55 surgical edits: added `log.info("advisory_lock_acquired", ...)` checkpoint after lock acquire + renamed existing per-retry warning event from `audit_event_serialization_retry` → `SERIALIZABLE_retry` for verbatim IG §3 Week 4 gate box 3 grep-language match) + `decision_diary.py` validator (PR #28) + `verify_chain.py` operator CLI (Day 12 PR #51 — argparse + asyncio.run + `DATABASE_URL` env-var shell over the chain walker; output contract: `CHAIN OK: <N> rows verified` exit 0 / `CHAIN BREAK at sequence_no=<X> (after <K> verified rows)` exit 1 / usage-error exit 2; required `--env` flag mirrors `audit_log.env` CHECK constraint) | Day 8 PR #39 (canonical hash-chain entry point); Day 12 PR #51 (operator-facing chain verifier; A02 binding; A27 satisfier in `deploy/audit/README.md`); Day 14 PR #55 (writer log-line additions + Week 4 gate box 3 close); A01 enforceable from PR #39 forward |
| `services/scheduler/` | `vacation.py` mode handler + `calendar_import.py` macro-events seeding | Day 6-9 chain — shipped early via PR #28 (`risk-review-approved`) |
| `services/qc_adapter/` | Pure-policy: `payloads.py` JSONL parser + `cursor.py` (`qc_adapter_cursor` row + canonical 3-directory enum) + `poll.py` (`plan_ingest_batch` orchestrator playbook). Day 28 PR-A: `qc_objectstore_client.py` (async httpx client over QC's REST surface at `https://www.quantconnect.com/api/v2/object-store/{list,get,set}` — HMAC HTTP Basic auth via `sha256("{api_token}:{timestamp}")` hex-digested password + base64 envelope + `Timestamp:` header + `User-Agent: trading-qc-adapter/0.1`; 2-step presigned-URL flow for `get_object`; 3-retry exponential backoff capped at 8s on 5xx + transport errors; 401/403 → `QCObjectStoreAuthError`, 404 → `QCObjectStoreNotFoundError`); `signal_ingestion.py` (`ingest_signal_emitted(session_factory, *, account_id, env, phase_at_emit, event)` — audit-first per backend-spec §2.10.1; takes `session_factory` not session to dodge DP-021 transaction-state interference; auto-bootstraps `slippage_calibration_versions` zero-prior row on first signal arrival per backend-spec §2.14 since signals.slippage_calibration_version_id has a FK to that table; `_derive_strategy_hash` extracts 40-char hex suffix or sha1s whole version string; `_derive_parameter_set_hash` is sha256(jcs(payload)); `_session_date_from_utc` anchors on America/New_York wall clock per dev-guide §3.7; recursive `_convert_floats_to_decimal` for A05 compliance before `jcs_serialize`); `orchestrator.py` (`Orchestrator.run_forever()` → 3 directory loops via `asyncio.gather` at 60/60/5s cadences; per-cycle: SELECT cursor → list_keys → filter `> last_consumed_sequence_no` → get_object each → JSONL concat → `plan_ingest_batch` → dispatch loop with bool return for ingested-vs-quarantined counter split → `apply_cursor_advance`; SIGTERM/SIGINT → `request_stop()`; 120s `asyncio.wait_for` cycle deadline); `db.py` (`fetch_active_account_id`, `fetch_cursor_row`, `apply_cursor_advance`, `fetch_slippage_head_version_id`, `insert_slippage_bootstrap_row`, `insert_signal_row` with `Decimal`-via-`str()` for jsonb writes per A05, `insert_data_quality_event`); `config.py` (`QCAdapterSettings` Pydantic-Settings with `env_prefix="QC_ADAPTER_"`); `main.py` (entrypoint module — structlog config + Postgres engine + account_id resolution + `Orchestrator.run_forever()` + signal traps; `_narrow_environment` maps the 4-value config Literal to the 3-value audit Environment Literal); `entrypoint.py` (sops yaml → env-var mapping mirroring api/discord_bot); `Dockerfile` (multi-stage builder + slim runtime mirroring webhook_pusher pattern). | Week 3 Tue scaffold (pure-policy) + **Day 28 PR-A — HTTP fetcher + signal ingestion loop (Phase-1-onset pipeline build pt 1/5; regular review; hot-fix whitelist; A01/A05/A06/A22/A27 BINDING + satisfied)** |
| `tests/unit/test_qc_objectstore_client.py` | Day 28 PR-A — 18 tests across 7 `Test*` classes: `TestAuthHeader` (2: sha256 password math + epoch Timestamp default), `TestListKeys` (4: happy sorted + empty + 401 AuthError + success=false terminal), `TestGetObject` (4: 2-step presigned + inline-body fallback + 404 NotFound + missing url/data), `TestSetObject` (2: happy + 403), `TestRetryLogic` (3: 5xx retries → succeed + budget exhausted + transport error retries), `TestHeadersAttached` (1: Auth/Timestamp/UA per request), `TestConstructorValidation` (2: empty creds raise). Pure-Python via respx-mocked httpx; A22 N/A. | Day 28 PR-A |
| `tests/unit/test_signal_ingestion.py` | Day 28 PR-A — 24 tests across 6 `Test*` classes: `TestPayloadValidation` (10: wrong event_type + parametrized missing-key + bad direction + empty market + float target_contracts + bool target_contracts), `TestDecimalCoercion` (2: float preserved via str + integer accepted), `TestSentinelDerivation` (4: strategy_hash 40 hex + parameter_set_hash 64 hex + slippage bootstrap path + head-uuid path), `TestStrategyHashExtraction` (2: 40-hex suffix as-is + non-hex sha1s whole), `TestSessionDate` (2: ET anchor + pre-UTC-midnight prior day), `TestHappyPath` (4: IngestResult UUIDs + audit-first ordering + signal_type='entry' + audit payload A05-clean via jcs_serialize round-trip). Fake `session_factory` = MagicMock with side_effect building per-call async-context-manager sessions (MagicMock+AsyncMock hybrid for sync-call/async-cm); AsyncMock for db ops + `append_audit_event` (monkeypatched). A22 N/A. | Day 28 PR-A |
| `tests/integration/test_qc_adapter_ingestion.py` | Day 28 PR-A — 8 tests via testcontainers postgres:16 + respx-mocked QC HTTP. `TestRunOnceHappyPath` (1: 2 signals land audit + signals + chain verifies + cursor advances + slippage bootstrap row auto-created with `trigger='bootstrap'` and is_head=TRUE + signals FK points at bootstrap row), `TestRunOnceFailurePath` (1: list_keys 5xx bumps consecutive_failures + no signal writes + cursor preserved), `TestRunOnceMalformedPayload` (1: bad event quarantined + valid sibling still ingests + chain continuous), `TestRunOnceUnhandledEventType` (1: order_filled routes to data_quality_quarantine + chain continuous + events_ingested=0 + malformed_quarantined=1), `TestRunOnceNoNewKeys` (1: empty list resets consecutive_failures), `TestKeyFiltering` (1: keys ≤ cursor sequence_no skipped), `TestAccountIdLookup` (1: db.fetch_active_account_id smoke). Skips cleanly on Docker-unavailable. A22 BINDS + satisfied (real Postgres FK + grants + audit writer + chain verification). Runtime: 8.36s. | Day 28 PR-A |
| `deploy/qc_adapter/README.md` | Day 28 PR-A — operator runbook (A27 satisfier per dev-guide §6.8 alternative (b)). Steps: verify sops contents (organization_id + api_token populated, not `<TODO_…>` placeholders) → docker compose build qc_adapter + up → watch boot logs for `qc_adapter_booting` + `qc_adapter_account_resolved` + `qc_adapter_orchestrator_started` → wait 90s for first `orchestrator_cycle_completed` event → end-to-end paper round-trip (PR-E ceremony) → `verify_chain` still passes (delegates to `deploy/audit/README.md`) → cleanup. 8-row troubleshooting table covering exit code 2 (sops field missing/placeholder) + 3 (no active account row) + 4 (uncaught exception) + 401/403 (wrong user_id or expired token) + persistent `qc_no_new_keys` (QC algo not writing yet — expected pre-Day-28-PR-E) + chain breaks (incident-review escalation). QC API token rotation procedure (generate → sops edit → commit → push → VPS pull + recreate → revoke old). | Day 28 PR-A |
| `services/reconciliation/` | `recon.py` (`plan_reconciliation_check` — pure-policy diff returning `ReconciliationPlan` dataclass; tolerances 0/$5/1bps + 2× dividend widening + T+1 grace via prior_breaks; emits `reconciliation_check_passed` / `_break_detected` / `_break_resolved` audit events; **THIS-SESSION PR — `AlertDescriptor` frozen dataclass + new `alerts: tuple[AlertDescriptor, ...]` field on `ReconciliationPlan` (default `()`); one descriptor emitted per ACTIONABLE break — grace continuations emit no alert; severity locked `"P2"` (→ #alerts channel only); category locked `"reconciliation_break"`; `triggering_break_index` field aligns with `breaks_detected` for positional audit_event_uuid resolution; new helpers `_build_alerts` + `_render_alert_title` + `_render_alert_body` + `_render_alert_payload`**) + `flex_query_fetcher.py` (`IbkrFlexQueryClient.fetch_snapshot` async client over IBKR FlexQuery XML; `FlexQueryFetchError` on transport/auth) + `apply.py` (`apply_reconciliation_plan` audit-first orchestrator per §2.10.1; **THIS-SESSION PR — accepts optional `alert_dispatch_hook: AlertDispatchHook` kwarg; new `AlertDispatchContext` frozen dataclass carrying descriptor + triggering_audit_event_uuid + account_id + env; `ReconciliationApplyResult` gains `alerts_dispatched_count`; firing order: audit → break-inserts → break-resolutions → alerts → state_transition_hook (alerts BEFORE kill-switch so operator sees cause before consequence); when hook is None, alerts logged + dropped (audit + break rows still land); new helper `_dispatch_alerts` resolves audit_event_uuid by positional alignment + defensive on out-of-bounds `triggering_break_index`**) + `scheduler.py` (`ReconciliationScheduler` daily wall-clock tick at 18:30 ET / 22:30 UTC; SIGTERM/SIGINT graceful shutdown; per-cycle deadline) + `eod_cycle.py` (PR #117 glue — `EodCycleConfig` frozen config + `build_backend_view` SUM(quantity) GROUP BY market from `positions_current` + latest `balances` row with Decimal(0) Phase-0 fallback + `build_broker_view` futures `/MES` prefix + zero-qty drop + USD-only cash sum + `run_eod_cycle` orchestrator wiring FlexQuery fetch → planner → apply with `FlexQueryFetchError → None` so scheduler stays running + `make_cycle_callback` closure factory; **THIS-SESSION PR — `run_eod_cycle` + `make_cycle_callback` thread `alert_dispatch_hook` through to apply so api scheduler-startup glue can inject the Discord-fan-out hook at construction time**; **Option C PR-B — 2026-05-28** — `run_eod_cycle` gains a flag-gated position-source switch: `EodCycleConfig` adds `position_source: Literal["flexquery","reqpositions"]` (default `"flexquery"`) + threaded `ibkr_host`/`ibkr_port`/`ibkr_account_id`; when `"reqpositions"` it calls `fetch_recon_positions()` (PR-A) → `build_broker_view(positions_override=…, source=BrokerSource.TWS_API)` so the position-qty check uses IBKR's real-time TWS view while FlexQuery still feeds cash/NAV/MTM via `refresh_backend_from_broker_snapshot`; `ReconPositionsFetchError` → fall back to FlexQuery positions + `eod_cycle_reqpositions_failed` ERROR log (no Discord push — that would need a new audit-event type + alert category = alembic/out-of-scope); `build_broker_view` gains `positions_override` + `source` kwargs; `_select_position_source` helper + `eod_cycle_position_source_selected` log at cycle start; reuses existing `BrokerSource.TWS_API` ⇒ **no alembic, no new audit/SSE types**). Scheduler wired into api lifespan via `_start_reconciliation_scheduler` in `services/api/main.py`; **defensive startup** — does NOT start until sops `ibkr.flex_query_id` + `ibkr.flex_query_token` populated (warns + skips at boot); operator wires sops + restarts to engage — no code redeploy needed. | Day 9 PR #42 — pure-policy plan-then-apply (`risk-review-approved`); PR #108 Worker-PR-3b — apply orchestrator + scheduler + flex_query_fetcher (`risk-review-approved`); PR #103 Worker-PR-3a — api added to egress network for FlexQuery; PR #117 — eod_cycle.py glue + api lifespan wiring + `deploy/reconciliation/README.md` operator runbook (`risk-review-approved`); kill-switch hook on `should_invoke_kill_switch=True` still deferred to Phase 1+ follow-up; T+1 grace `prior_breaks` query still deferred until table has history; **THIS-SESSION PR — recon alert descriptors + alert_dispatch_hook seam (`risk-review-approved`; +30 tests across `test_reconciliation.py` + `test_reconciliation_apply.py` + `test_reconciliation_eod_cycle.py`); api scheduler-startup glue (the actual hook closure that INSERTs alerts + calls `webhook_pusher.dispatch_alert`) deferred to a separate hot-fix-whitelist PR (`services/api/**`)**; **Option C PR-B — 2026-05-28 — flag-gated `reqPositions` wiring in `eod_cycle.py` + `API_EOD_RECON_POSITION_SOURCE` setting (`services/api/config.py` + `main.py`); `risk-review-approved` (eod_cycle.py is A02 forbidden-path; the `services/api/**` edits ride along in the same PR); default stays `"flexquery"` (PR-C flips it after cycle observation); +3 `TestRunEodCycleReqPositionsSource` tests; reuses `BrokerSource.TWS_API` ⇒ no alembic / no new audit / no new SSE types** |
| `services/reconciliation/ibkr_intraday.py` + `tests/unit/test_reconciliation_ibkr_intraday.py` | **Option C PR-A — 2026-05-28** — per-cycle `reqPositions` client for the EOD recon position-quantity check (swaps off FlexQuery XML, whose settlement-cleared-only view triggers false-positive same-day-fill halts; FlexQuery stays source-of-truth for cash / NAV / MTM). Exports `ReconPosition` (frozen `market: str` + signed `quantity: Decimal`; **no MTM fields** — `reqPositions` carries none, so those stay on FlexQuery), `DEFAULT_RECON_CLIENT_ID: Final[int] = 4` (claims the reserved 4-7 slot per dev-guide §1.5 LOCKED; isolated from order-worker clientId=1 + bar_sync clientId=3 so a recon bug can't wedge order placement), `ReconPositionsFetchError` (frozen dataclass Exception — wraps the underlying `IbkrPlacementError` via `__cause__` + copies `underlying_exception_class` so PR-B catches a recon-package-owned type without importing the execution-layer exception), and `async fetch_recon_positions(*, account_id=None, host="ib_gateway", port=4004, client_id=4, client_factory=None, connect_timeout_seconds=30.0) -> tuple[ReconPosition, ...]`. Per-cycle construct → connect → `get_positions` → disconnect mirroring `bar_sync` clientId=3; disconnect runs in `finally` (only for a session actually opened) + swallows its own errors so it never masks a fetched result or a propagating fetch failure; zero-qty rows dropped (matches `build_broker_view` so the planner's symmetric-difference diff sees no phantom closed positions); `client_factory` is the test seam (default builds a real `IbAsyncIbkrClient`). IMPORTS from `services.execution` (the `IbkrClient` Protocol + `IbAsyncIbkrClient` adapter + `IbkrPlacementError`) but does NOT modify it — **Path A** (Q4): trusts the adapter's ib-async local position cache, no `services/execution/**` change. Structlog lifecycle: `recon_positions_fetch_started` / `_completed` (carries `positions_count` + `raw_count`) / `_failed` (carries `operation` + `underlying_exception_class`) / `_disconnect_failed`. **Wired into `run_eod_cycle` by Option C PR-B (2026-05-28)**, flag-gated behind `position_source="reqpositions"` (default still `"flexquery"`); PR-A made the data path available and PR-B's wiring likewise adds no audit-event, SSE, or alembic changes (reuses `BrokerSource.TWS_API`). A02 BINDS (`services/reconciliation/**`, `risk-review-approved` required); A05 enforced (Decimal quantity end-to-end); A27 deferred to the Option-C deploy ceremony (first 22:30 UTC `source=reqpositions` cycle against the real gateway pins observed `/MES` / `TLT` symbol values). | **Option C PR-A — 2026-05-28 — per-cycle reqPositions recon client (`risk-review-approved`; 13 unit tests across TestFetchReconPositions / TestSymbolNormalization / TestStructlogDiscipline / TestModuleContract / TestDefaultClientFactory — pure adapter-mocking via MagicMock+AsyncMock, no gateway / no Postgres, A22 N/A; `make lint` + `make typecheck` clean; 201/201 `test_reconciliation*.py` green).** PR-B (2026-05-28) wires `fetch_recon_positions` into `run_eod_cycle` behind the `position_source` flag (default `"flexquery"`); PR-C flips the default to `"reqpositions"` after cycle observation. Reuses the existing `BrokerSource.TWS_API` enum — no alembic. |
| `deploy/reconciliation/README.md` + `tests/unit/test_reconciliation_eod_cycle.py` + `tests/unit/test_api_main_order_worker.py` (extended) | PR #117 operator runbook (A27 satisfier per dev-guide §6.8 alternative (b)) for the EOD recon scheduler: FlexQuery template creation in IBKR portal (Statement Type "Activity" or "Trade Confirm Flex Query" depending on snapshot kind; field selection mirrors backend-spec §2.6 broker-view contract) → sops population (`ibkr.flex_query_id` int + `ibkr.flex_query_token` SecretStr in `paper.enc.yaml`) → deploy verification (api boot logs `reconciliation_scheduler_started` / `reconciliation_scheduler_skipped` depending on sops state) → troubleshooting matrix (placeholder values `<TODO_...>` detected + skipped; missing template returns 404 from IBKR; auth failures → `FlexQueryFetchError`) → Phase 1+ follow-ups (kill-switch hook on `should_invoke_kill_switch=True`; T+1 grace `prior_breaks` query; Discord `#alerts` embed on detected breaks). 15 → **20 EOD-cycle unit tests** (added: 1 `TestMakeCycleCallback::test_callback_threads_alert_dispatch_hook` + 4 `TestAlertDispatchHookEndToEnd` — actionable break fires hook once + no breaks no hook fires + multiple actionable fires hook per break + hook optional when no hook supplied) + 5 api-lifespan tests (TestStartReconciliationScheduler + TestStopReconciliationScheduler — sops-missing skip-path + happy-path startup + graceful shutdown) lock the contract. `tests/unit/test_reconciliation_apply.py` 11 → **17 tests** — added `TestApplyAlertDispatchHook` (6: hook fires once per alert + multiple alerts in order + no-hook drops with warning + empty-alerts no-call + invalid triggering_break_index skipped + hook fires after audit writes land). Pure-Python; A22 N/A (fake DB session + respx-mocked FlexQuery client). | PR #117 — Week N glue (`risk-review-approved` for the `services/reconciliation/**` portion; `deploy/**` portion is hot-fix scope); **THIS-SESSION PR — TestAlertDispatchHookEndToEnd + TestApplyAlertDispatchHook added; +6 tests in apply + 5 in eod_cycle + 1 in callback** |
| `services/calibration/` | `calibration.py` (`plan_calibration_fit` — pure-policy slippage-fit planner returning `CalibrationPlan` dataclass per backend-spec §2.14; locked: BOOTSTRAP_ALPHA=0 / BOOTSTRAP_BETA=0 zero-prior; MIN_OBSERVATIONS_FOR_OLS=30; per-market OLS via Decimal-only arithmetic — `beta = Sxy/Sxx`, `alpha = ybar - beta*xbar`, `r_squared = 1 - SS_res/Syy`; degenerate branches handled — Sxx=0 collapses to constant intercept (`alpha=ybar, beta=0, r_squared=None`), Syy=0 returns `r_squared=None`; per-market fall-back to zero-prior when `n_obs < MIN_OBSERVATIONS_FOR_OLS`; signed slippage convention — `+bps = trader paid worse than expected mid`; emits one `slippage_calibration_recalibrated` audit event with the locked §3.30 taxonomy event_type) + `audit_events.py` (TypedDict `SlippageCalibrationRecalibratedPayload` + `MarketCoefficientPayload` mirroring the §3.12 JSONB shape; re-export of `SLIPPAGE_CALIBRATION_RECALIBRATED_EVENT_TYPE` const for grep-ability) | Day 18 — pure-policy plan-then-apply via Day 18 PR (`risk-review-approved`); Week 4 dispatcher wires it into the monthly cron + HEAD-pointer swap (alembic 0003 already ships `slippage_calibration_versions` + `slippage_head` partial unique index per backend-spec §3.12). A02 binds. A01/A05/A06/A22 enforced. A27 N/A (no third-party platform contract) |
| `tests/unit/test_calibration.py` | 58 tests across 14 `Test*` classes — TestLockedConstants (5: BOOTSTRAP_ALPHA/BETA = 0; both Decimal-typed; MIN_OBSERVATIONS_FOR_OLS=30; BPS_PER_UNIT=10000), TestEnums (3: CalibrationTrigger values match §3.12 CHECK; CalibrationFitMethod values; SLIPPAGE_CALIBRATION_RECALIBRATED_EVENT_TYPE re-export equals locked enum value), TestPlanShape (7: frozen dataclasses × 3 + version_no=1 default + parent+1 increment + becomes_head default True + passthrough False with payload-roundtrip), TestBootstrapPath (3: empty fills → empty per-market dict; non-empty BOOTSTRAP still zero-prior; multi-market BOOTSTRAP zero-prior per market with n_obs counts), TestNonBootstrapFallback (3: SCHEDULED_MONTHLY no-fills → zero-prior; MANUAL with 29 fills → zero-prior; mixed market 30+5 fills → plan-level OLS label because at least one market crossed threshold), TestSlippageSignConvention (4: buy/sell × above/below expected → signed bps with the locked +cost-to-trader convention), TestOLSFit (2: hand-computed regression `y = 2 + 4x` returns alpha=2, beta=4, r_squared=1 to 4 decimal places; per-market independent fits with `MES: y=1+2x` + `ES: y=-1+3x`), TestDegenerateOLSBranches (2: Sxx=0 returns `alpha=ybar, beta=0, r_squared=None`; Syy=0 with non-zero Sxx returns `r_squared=None`), TestAuditEventPayload (13: event_type matches locked enum; canonical 12-field set; UUID-as-str; parent None → null; Decimal-as-str A05; ts ISO with tz; data_window from min/max fill_at_utc + None when empty; fill_count = sum n_obs; notes pass-through + None default; trigger + fit_method are enum string values; per_market keys sorted), TestDeterminism (2: same inputs → equal plans + equal payload dicts), TestIterableInput (1: generator input consumed once via internal tuple), TestTimezoneEnforcement (3: naive calibrated_at_utc raises; naive fill_at_utc raises; non-UTC offset accepted + echoed through), TestNumericValidation (8: float size/ADV → A05 error; zero ADV / negative size / zero expected / negative realized → boundary error; empty market / invalid direction → reject), TestModuleContract (2: audit_events re-export imports cleanly; calibration `__all__` contains the 8-name public surface). Pure Python; no testcontainers; no mocking; A22 enforced (zero `audit_log` INSERTs). | Day 18 — pure Python via Day 18 PR (`risk-review-approved`); 604/604 tests green locally (was 546 + 58 new); `make ci` 27.05s wall-time |
| `tests/unit/test_reconciliation.py` | 45 → **63 tests** across 9 `Test*` classes — exact match, position mismatch, cash abs/bps tolerances, T+1 grace, dividend 2× widening, multiple breaks, resolved priors, A06 timezone enforcement, A05 Decimal-as-str, canonical taxonomy validation, **new `TestAlertDescriptors` (18 tests) — no-breaks emits no alerts + actionable-breaks emit alerts + grace-continuations emit no alerts + mixed actionable/grace gates correctly + Decimal-as-string payload + ISO timestamp + locked severity P2 + locked category reconciliation_break + frozen dataclass + title/body rendering + payload key shape + triggering_break_index alignment** | Day 9 — pure Python, no Docker (PR #42); **THIS-SESSION PR — TestAlertDescriptors added (no testcontainers; A22 N/A; A05 + A06 enforced)** |
| `services/webhook_pusher/` | `payloads.py` (`plan_alert_dispatch` pure-policy planner; severity → channel routing locked: P0 → `{#alerts, #critical, email}`; P1/P2 → `#alerts`; Discord embed shape + Resend email shape; `AlertCategory` 29-value enum mirror of `alert_category` Postgres enum + spec §3.27) + `sender.py` (async `post_outbound_message` with explicit User-Agent per Day 4 PR #21 lesson; HTTP status → `DeliveryStatus` mapping; single 429 retry with Discord JSON `retry_after` preferred over header) + `dispatcher.py` (`dispatch_alert` orchestrator; SELECT alerts row → short-circuit on existing `delivery_status` for idempotency → fan out via sender → UPDATE JSONB) + `cli.py` (operator smoke `python -m services.webhook_pusher.cli` bare-smoke + `--with-db` modes) + `Dockerfile` (PR #50; multi-stage build mirroring api shape; runtime image with httpx + structlog + sqlalchemy[asyncio] + asyncpg only; non-root `trading` user; tini entrypoint; Phase 0 placeholder CMD = `sleep infinity`, swaps to worker entrypoint at Week 4 dispatcher) | Day 10 — alerts delivery surface shipped via PR #44; **Day 11 carryover #3 (PR #50) — service container shipped Phase-1-early so api stays `trading_internal`-only at all times per backend-spec §8.11.** 58 unit tests; A22 + A27 (operator runbook) + A04 enforced. Week 4 risk dispatcher wires `dispatch_alert(...)` into kill-switch transitions + recon breaks + audit-chain breaks (api → webhook_pusher IPC pattern still TBD; 3 candidates documented in Day 11 carryover #3 entry) |
| `tests/unit/test_webhook_pusher.py` | 58 tests across 14 `Test*` classes — locked constants, severity routing, Discord embed shape (color/title/footer/fields/truncation/MAX cap/no-auth), Resend email shape (subject prefix/body detail/from-to/Bearer auth), planner errors (missing URL/missing email_identity/A06), planner determinism, sender HTTP status mapping (8 outcomes), 429 retry semantics (Discord JSON body preferred + cap), transport failures, header attachment, dispatcher idempotency + fan-out + alert-not-found, DeliveryStatus + ChannelName wire values, AlertCategory count==29 | Day 10 — respx-mocked HTTP, fake AsyncSession, no Docker (PR #44) |
| `deploy/webhook_pusher/README.md` | Operator runbook (A27 satisfier per dev-guide §6.8 alternative (b)): Prereqs (api Dockerfile rebuild check + webhook_pusher container running) → Step 1 sops decrypt + URL extract (with SOPS_AGE_KEY_FILE export) → Step 2 stage env vars (robust awk patterns post-PR-#48) → Step 3 bare-smoke (P2) via `docker compose exec -T webhook_pusher` → Step 4 find account_id → Step 5 P0 full roundtrip (`#alerts` + `#critical` + Resend) → Step 6 psql verify `delivery_status` JSONB → Step 7 idempotency note → Step 8 cleanup. **Week 3 verification gate box 4 closed Day 11 carryover #2 (smoke executed; `status=ok http=204`; embed visually confirmed in `#alerts`).** PR #50 dropped the temporary Step 0 + Step 8 network-attach workaround now that webhook_pusher has its own container | Day 10 PR #44 (initial); Day 11 carryover #2 PR #48 (5 polish fixes from first execution); Day 11 carryover #3 PR #50 (architecture: target webhook_pusher container instead of api) |
| `deploy/audit/README.md` | Operator runbook (A27 satisfier per dev-guide §6.8 alternative (b)) for `services/audit/verify_chain.py`: Prereqs (api image at PR-#51 commit + sops decrypt working + age key at `/etc/credstore.encrypted/age_key`) + architecture note (CLI runs from api container — read-only walk, no egress need) → Step 1 sops decrypt + extract `app_service` password → Step 2 stage `DATABASE_URL` (subshell-local; sanity-prints host + role only, never the password) → Step 3 run `docker compose exec -T -e DATABASE_URL="$DATABASE_URL" api /opt/venv/bin/python -m services.audit.verify_chain --env paper` with full "On mismatch" diagnostics for each canonical failure mode (CHAIN BREAK incident-review escalation, missing env, invalid `--env` choice, password auth fail, host translation fail, ImportError) → Step 4 optional psql cross-check (`SELECT COUNT(*), COALESCE(MAX(sequence_no), 0)`) → Step 5 cleanup (`shred -u`, `unset` env vars, exit). **Week 4 verification gate box 2 closed Day 12 carryover** (smoke executed via operator-delegated SSH; `CHAIN OK: 0 rows verified` exit 0; psql cross-check `rows=0, max_seq=0` matches; tmpfs cleared) | Day 12 PR #51 (initial); Day 12 carryover (smoke executed; Week 4 gate box 2 closed) |
| `tests/golden/fixtures/qc_events/` | 5 representative QC §4.5.1 wire envelopes — `01_signal_emitted.json` (495-byte JCS, sizing_trace nested dict), `02_order_filled.json` (308-byte JCS, links to fixture 01 signal_uuid), `03_reconciliation_check_passed.json` (304-byte, mirrors PR #42 recon shape), `04_kill_switch_triggered.json` (267-byte), `05_system_stopped.json` (200-byte). All Decimal-as-string per A05; all timestamps `+00:00` per A06. event_type values use the locked `services/audit/event_types.py` taxonomy (NOT IG §3 Week 4 Mon's casual prose names) | Day 11 — pure data fixtures (PR #45) |
| `tests/golden/test_qc_parity.py` | 18 tests across 5 `Test*` classes — `TestRecordHashFromGenesis` (5: per-fixture deterministic record_hash from GENESIS, hex baked-in to module), `TestChainLinksForward` (2: sequential walk + first-link cross-check; tail hex baked-in), `TestRoundTripViaQCAdapterParser` (5: parser-is-payload-identity per fixture; re-asserts record_hash post-parse), `TestModuloThreeMutableFields` (2: `{ingest_clock_ts, ingest_uuid, sequence_no}` audit-side metadata MUST NOT appear in QC wire payload, top-level + recursive nested walk), `TestFixtureSchemaShape` (4: 5 canonical top-level keys + A04 taxonomy + A06 timezone + filesystem-listing match). `@pytest.mark.golden`; runs via `make test-golden` in 0.61s | Day 11 — pure Python, no Docker, A22 binds (zero audit_log INSERTs, zero testcontainers, zero mocking); A27 does NOT bind (no third-party platform contract); closes IG §3 Week 4 verification gate box 1 (PR #45) |
| `watchdog/watchdog.py` + systemd unit + timer | External watchdog: `GET /api/health` poll every 5 min (`OnUnitActiveSec=5min`); 3-failure threshold + 60-min cooldown; **Resend = Phase-0 primary, Discord = best-effort secondary** (Cloudflare-blocks-Hetzner per Day 4 carryover); channel-isolated `run_once` so a Discord failure can't block Resend. | Day 4 — deployed to Hetzner Nuremberg (`trading-watchdog`, static IP `188.245.37.16`); apex URL fix Day 6 carryover; **Day 17 — `watchdog/**` codified onto dev-guide §2.3 hot-fix whitelist (per operator request); no code changes to watchdog itself today** |
| `deploy/Caddyfile` | Caddy reverse proxy on Hetzner Ashburn: `{$DOMAIN}` apex with auto-cert via Let's Encrypt (HTTP-01/TLS-ALPN); HSTS + CSP + X-Frame-Options + Referrer-Policy headers; `/api/sse/events` route with `flush_interval -1` + `transport.read_timeout 24h` upstream-leg config (Caddy → api); `/api/internal/watchdog` route with `@watchdog` matcher + `remote_ip {$WATCHDOG_IP}` IP allowlist per spec §4.4 (NOT `/api/health` which is public per spec); `handle_path /grafana/*` + `/gitea/*` Phase-1-gated routes; `handle_errors` 503 maintenance fallback. **Day 17 — server-level `timeouts { write 30s }` → `write 24h` (DP-020 fix)** matching existing `idle 24h`; Caddy's `write` timeout is the max time to write the *entire* response (not per-write), so any finite value caps SSE streams. Inline DP-020 explanatory comment block above the `timeouts` block. **Day 17 carryover — DP-020 fix VERIFIED LIVE** via two-surface SSE smoke (loopback 3 pings @ 30s + external HTTPS 3 pings sustained 60+ seconds; curl exit 28 clean, NOT exit 92 h2 reset). Caddy admin API at `http://127.0.0.1:2019/config/apps/http/servers/srv0/` shows `write_timeout: 86400s` (24h). **Day 24 ceremony fix — commit `9492973 fix(caddy): allow 'unsafe-inline' on script-src ...`:** added `'unsafe-inline'` to the `script-src` directive (was `script-src 'self'`) because Next.js 14 App Router emits inline `<script>` tags for the React hydration bootstrap (`__next_f.push(...)` chunks); without 'unsafe-inline' browsers blocked them and the /setup page rendered statically but was non-interactive (no input handling, no button clicks). Phase-1 follow-up: switch to nonce-based CSP. | Day 5 — TLS end-to-end verified Day 6 carryover; **Day 17 — DP-020 fix applied; Day 17 carryover — DP-020 verified live; Day 19 — re-confirmed via browser SSE smoke; Day 24 ceremony — CSP `unsafe-inline` fix for Next.js hydration** |
| `services/api/middleware.py` | Request-scoped middleware. **`RequestContextMiddleware`** — binds per-request `trace_id` to structlog + emits `request_started`/`request_completed`. **`CSRFMiddleware`** — double-submit cookie pattern per spec §8.5.4; enforced on POST/PUT/PATCH/DELETE except `_CSRF_EXEMPT_PATHS` (`/api/setup/verify-token` + `/api/internal/watchdog`). **`RateLimitMiddleware` (Day 17)** — fixed-window per-IP counter; 100 req/10s on `/api/**` general bucket; 5 req/10s on `/api/auth/**` auth bucket; exempt: `/api/health` (watchdog poll target), `/api/internal/watchdog` (bearer-auth + IP-allowlisted push), `/api/sse/events` (long-lived stream; N=4 tab cap already bounds concurrency). Returns 429 with canonical `RATE_LIMITED` envelope + `Retry-After` header. Single in-process `dict[(client_ip, bucket_name), _Bucket]` store; `asyncio.Lock` for the increment critical section. `request.client.host` is the resolved IP because uvicorn runs with `--proxy-headers --forwarded-allow-ips *` (entrypoint.py:86); Caddy's `X-Forwarded-For` is the trust boundary, which holds because the api is on Docker `internal-only` network. `_monotonic()` module-level indirection allows tests to inject a deterministic clock without `freezegun`. `register_middleware` order (verified empirically `last-added = outermost` via a one-shot Starlette test): CSRF added first (innermost), RateLimit added second (outer of CSRF — so CSRF-failing floods still consume the per-IP budget), RequestContext added third (outermost; binds trace_id for both inner middlewares' rejection logs). | Day 5 (initial — CSRF + RequestContext); Day 17 — `RateLimitMiddleware` added; `register_middleware` reordered with verified `last-added = outermost` Starlette behavior; **Day 19 — rate-limit curl loops re-confirmed live (general 100×200+10×429, auth 5×200+5×429, canonical 429 envelope)** |
| `deploy/github-app/` | Canonical manifest + operator runbook for the in-app PR review surface app | Day 2 — app created (App ID 3615825 / Installation ID 129868686) |
| `deploy/discord/` | Canonical manifest + operator runbook for the Discord guild + bot (7 channels) + `viewer-role-setup.md` operator runbook for adding view-only Discord members per Day 24 close-out request | Day 2 — guild + bot created; Day 24 — viewer-role runbook added |
| `deploy/sops/` | Operator runbook + per-env secret schema templates (`secret_schemas/{dev,paper,live}.template.yaml`) | Day 2 — runbook executed; templates ready for Day 3 encryption |
| `.sops.yaml` | sops creation rules with 3 real age recipients (dev/paper/live) | Day 2 |
| `scripts/sops_init.sh` | Helper that substitutes age pubkeys into `.sops.yaml` (idempotent) | Day 2 |
| **DELETED 2026-05-20 (data-layer sub-pivot)** — `scripts/seed_lean_data.py` (yfinance ETF seeder, was PR #123) + `scripts/seed_lean_futures_databento.py` (DataBento futures seeder, was PR #131) + `scripts/verify_seed_data.py` + `tests/unit/test_verify_seed_data_script.py` (Tiingo cross-check + 52 tests, was PR #126) + `deploy/lean_local/seed-data.md` (operator runbook for the seed ceremony, was PR #123 / #126 / #131 / 2026-05-17 evening). The 2026-05-19 evening `scripts/operator_tools/synthesize_today_universe.sh` synthesis-cron stopgap was scoped but never merged to `main` (lived in `.claude/worktrees/practical-carson-33311c/` only) — permanently moot under the sub-pivot. The seed-file architecture they implemented is retired: LEAN now reads market data from IBKR via the `InteractiveBrokersBrokerage` data-queue-handler + history-provider on `clientId=10` (delayed quotes 1-3s actual lag + `reqHistoricalData` for warmup; both free for any IBKR account holder; $0/yr ongoing). `trading_lean_data` Docker volume mount stays in `docker-compose.yml::lean_local` for rollback safety; operator can `docker volume rm trading_lean_data` after a 30-day soak. | Retired 2026-05-20 — see `Docs/decisions-log.md` 2026-05-20 entry "Phase 1 data-layer pivot: IBKR delayed quotes replace seed-file architecture" + git history for the original implementations |
| `scripts/operator_tools/master_client_id_probe.py` + `tests/unit/test_master_client_id_probe.py` | **Cross-clientId cancellation probe** — empirical validation step for Mitigation A of the cross-clientId IBKR cancellation limitation (see `Docs/decisions-log.md` 2026-05-27 entry "Cross-clientId IBKR cancellation: structural limitation"). Runs after the operator has set `IBKR_MASTER_CLIENT_ID` in `deploy/.env` + `docker compose up -d --force-recreate ib_gateway`. Three sequential stages on distinct clientIds: (1) places a safe stop_market SELL at stop_price=$1 on /MES from clientId=86 — well below any realistic market level so the stop will NEVER trigger + sits as PreSubmitted; (2) connects via the master clientId (CLI `--master-client-id`) + calls `IbAsyncIbkrClient.cancel_order(probe_cid)` which under PR #262 verifies the trade reaches Cancelled/Inactive within 2s — pre-mitigation this raised IBKR Error 10147; post-mitigation it succeeds; (3) belt-and-suspenders `reqGlobalCancel` from cleanup clientId=87 ensures no probe order survives even if step 2 failed. CLI: `--master-client-id` (required) + `--env paper/live-*` (required; `--allow-non-paper` safety gate on live) + `--place-client-id` default 86 + `--cleanup-client-id` default 87 + `--market` default `/MES` + `--ibkr-host/--ibkr-port`. Day TIF as final fail-safe (orphaned probe order expires at next daily settlement ~21:00 UTC). Exit codes: 0 success / 1 place_failed / 2 master_cancel_failed (mitigation didn't take — recheck `config.ini` per decisions-log) / 3 cleanup_failed / 4 ibkr_connect_failed / 6 bad_args / 99 unexpected. Tests: 10 across `TestParseArgs` covering arg-parsing validation surface (clientId collision/range rules, live-env safety gate). The async stage orchestration is exercised by the operator against the live gateway per the runbook in the module docstring. Pattern mirrors `scripts/operator_tools/replay_executions.py`. **Forbidden-paths check:** `scripts/operator_tools/**` NOT on §11 [A02] forbidden whitelist nor §2.3 hot-fix whitelist; pure tooling (CALLS `services/execution/IbAsyncIbkrClient` but doesn't modify it); regular PR review applied. A05/A06 enforced; A01/A22 N/A. | 2026-05-27 PR #266 — feat(deploy): wire TWS_MASTER_CLIENT_ID + cross-clientId probe tool |
| `tests/integration/test_replace_protective_stop_end_to_end.py` | **Integration test for `scripts.operator_tools.replace_protective_stop`** — testcontainers `postgres:16` + `alembic upgrade head`, then exercises `apply_replace_protective_stop` + `_amain` against a real schema. Three cases: (1) happy path — audit-first ORDER_PLACED commits before the orders row pre-INSERT (status='pending'), broker ack flips to status='working' with broker_order_id; new orders.parent_order_id links back to the ORIGINAL entry_order_id (so future exit-pipeline lookups still resolve the bracket); chain verifies clean; (2) idempotency guard — pre-seeded working bracket stop + `_amain` without `--force` returns `EXIT_BRACKET_ALREADY_PROTECTED` (exit code 2); no new audit row, no new orders row; idempotency check fires BEFORE the IBKR import so no stubbing required; (3) risk-state bypass — seeds `risk_state.state='HALT_NEW'` + apply still succeeds (PR-5's recovery path explicitly does NOT consult risk_state). Pattern mirrors `tests/integration/test_exit_end_to_end.py` from PR #264. `_amain` reads DATABASE_URL from env; the test builds the async URL inline from pg_url (NOT `async_engine.url` which masks the password as `***`). | 2026-05-27 PR #267 — test(integration,execution): replace_protective_stop e2e + PR #262 advisories |
| `scripts/operator_tools/replay_executions.py` + `scripts/operator_tools/README.md` + `scripts/operator_tools/__init__.py` + `tests/unit/test_replay_executions_script.py` | **Backend-blind-fills recovery tool** — committable, parameterized version of the transient `/tmp/drill5_recovery.py` script that closed the 2026-05-18 drill 5 incident. Runs inside the api container (now shipped via the api `Dockerfile`'s new `COPY scripts/` directive — see api Dockerfile row below) via `python -m scripts.operator_tools.replay_executions --client-order-ids ... --env paper`. Opens a read-only `ib-async` connection at `clientId=99` (deliberately distinct from worker's `clientId=1`) to `ib_gateway:4004`, calls `IB.reqExecutionsAsync(ExecutionFilter())`, filters by `orderRef` to the operator-supplied CIDs, aggregates per-CID into `AggregatedExecution` (VWAP fill_price + summed commission + latest fill timestamp at NUMERIC(20,8) precision), builds `FillIngestPayload` per match, and routes through `services.risk.fill_processor.process_fill_event` for the canonical audit-chain + tables write path. **SSE NOT emitted** (multiplexer is intra-process to api; this script is a separate process — operator manually POSTs to `#fills` per the runbook). CLI: 9 args (`--client-order-ids` required + `--env` required + `--ibkr-host` / `--ibkr-port` / `--client-id` defaults + `--filter-acct` / `--filter-time` passthroughs into `ExecutionFilter` + `--dry-run` + `--allow-non-paper` fail-closed safety gate on live envs). Exit codes: 0 success / 1 no_match / 2 unsupported_scenario / 3 fill_processing_error / 4 ibkr_connect_failed / 5 db_init_failed / 6 bad_args / 99 unexpected. Structured logging via `structlog` ConsoleRenderer; all events `replay_executions_*`. Decimal coercion at IBKR boundary per A05; tz-aware UTC enforcement per A06; commission sentinel (`sys.float_info.max`) clamped to 0. Tests: 44 across 11 `Test*` classes — TestArgparse (13) + TestAggregateFillsForCid (6) + TestGroupFillsByCid (4) + TestComputeExitCode (6) + TestAmainHappyPath/NoMatch/ErrorPaths/DryRun (10) + TestModuleContract (5); pure Python via `sys.modules['ib_async']` shim + `_FakeIB` / `_FakeFill` / `_FakeExecution` / `_FakeCommissionReport` stubs + injected `process_fill_event_override`; 1.3s wall-time. `scripts/operator_tools/**` is a NEW path — NOT on the §11 [A02] forbidden whitelist or §2.3 hot-fix whitelist; pure tooling (CALLS `services/risk/**` but doesn't modify it); regular PR review applied. A01/A05/A06/A27 satisfied; A22 N/A. README is the A27 satisfier: when/when-not-to-use, pre-flight checks, SSH-delegated run command for paper + live envs, dry-run mode, per-exit-code action table, post-success verification queries, manual Discord `#fills` POST guidance with sample embed JSON, architecture note, lineage. Closes 2026-05-18 drill 5 follow-up #4. | 2026-05-18 PR — drill 5 backend-blind-fills recovery committed (hot-fix scope `scripts/operator_tools/**` + `services/api/Dockerfile`; no `risk-review-approved` label) |
| `services/api/Dockerfile` | Multi-stage build (builder + runtime) of the api container image. Day 5 baseline scaffolded for the FastAPI service; runtime deps pinned per `pyproject.toml` Layer-1 `pip install`; `dep-drift-check` Makefile target diffs against pyproject. **2026-05-18 PR** — added `COPY scripts/ scripts/` to the builder stage + `COPY --from=builder /build/scripts /app/scripts` to the runtime stage so `scripts/operator_tools/replay_executions.py` is importable via `python -m scripts.operator_tools.replay_executions` from inside the api container. The rest of `scripts/` rides along; those entry points are never auto-invoked from the api process (the operator runs them from a workstation venv with separate deps like `yfinance` / `databento` per their respective runbooks). | Day 5 baseline; **2026-05-18 PR — `COPY scripts/` added for operator-recovery tooling import path** |
| `secrets/paper.enc.yaml` | Paper-env encrypted file. All Day-2 → Day-5 captured values filled (QC API token, Resend `re_*` API key + from-address, Discord webhook URLs, GitHub App private key, Postgres role passwords) | Day 6 carryover — filled and committed from VPS via PR #29 (Option B; ends `git reset --hard` data-loss class) |
| `secrets/{dev,live}.enc.yaml` | Dev + live encrypted env files. App ID + Installation ID + IBKR account + webauthn rp_id substituted; remaining values are `<TODO>` placeholders, operator pastes via `sops <file>` later | Day 3 |
| `alembic.ini`, `alembic/env.py`, `alembic/script.py.mako` | Migration runner config; reads `DATABASE_URL` from env (sops at deploy time) | Day 3 |
| `alembic/versions/0001_audit_log.py` | `audit_log` partitioned by `ingest_clock_ts`; yearly partitions 2026–2031; `uuid_generate_v7()` SQL function | Day 3 |
| `alembic/versions/0002_core_tables.py` | accounts, setup_tokens, contracts, signals, orders, fills, trades, attribution, positions, balances; orders/fills/trades/attribution partitioned by year | Day 3 |
| `alembic/versions/0003_risk_tables.py` | strategy_versions, parameters, parameter_sets, slippage_calibration_versions, decision_diary, risk_state; closes deferred FKs from 0002 | Day 3 |
| `alembic/versions/0004_ops_tables.py` | reconciliation_breaks, data_quality_events, agent_actions, vacation_mode, qc_adapter_cursor (TABLE + 3 §3.19 INSERT rows), capital_events, cost_events, liveness_probes, pdt_day_trade_log, dividend_history, incident_reviews, universe_state, alerts (+`alert_category` enum), macro_events | Day 3 |
| `alembic/versions/0005_immutability.py` | audit_log BEFORE UPDATE/DELETE blocker, BEFORE TRUNCATE blockers (parent + each yearly partition), attribution expected_* lock, REVOKE TRUNCATE FROM PUBLIC | Day 3 |
| `alembic/versions/0006_roles.py` | app_service, app_service_readonly, app_owner (NOLOGIN), dba_breakglass (SUPERUSER NOLOGIN); per-role grants; per-role audit_log REVOKEs; passwords set out-of-band from sops | Day 3 |
| `alembic/versions/2026-05-09_qc_adapter_cursor_seed.py` | Defensive idempotent re-seed of qc_adapter_cursor (`ON CONFLICT DO NOTHING` upgrade + intentional no-op downgrade); first **operational** migration under dev-guide §7.1 hybrid scheme | Day 8 — no-op against current schema since 0004 already inserts (PR #40) |
| `alembic/versions/2026-05-13_auth_tables.py` | 4 new auth tables per backend-spec §8.5.1/§8.5.2/§8.5.3 — `webauthn_credentials` (UUID PK + BYTEA credential_id UNIQUE + BYTEA public_key + INTEGER sign_count + TEXT nickname + active BOOLEAN + FK → accounts), `totp_secrets` (account_id UUID PK + BYTEA encrypted_secret AES-256-GCM-encrypted at app layer per spec "separate from sops" → key sourced from `secrets/<env>.enc.yaml` `totp.encryption_key` + tz-aware enrolled_at_utc + last_used_at_utc), `backup_codes` (UUID PK + FK → accounts + TEXT code_hash Argon2id + nullable used_at_utc + UUID generation_id grouping a batch of 8 codes), `sessions` (BYTEA PK raw 32-byte session_id matching base64url cookie value + FK → accounts + tz-aware created/last_activity/last_uv/expires + INET ip_address + TEXT user_agent + totp_bootstrap_only BOOLEAN ↔ application-layer `auth_strength: weak\|strong` per frontend-spec §5.4 + evicted_at_utc/reason for explicit revocation + 24h absolute expiry). Raw SQL throughout matching `0002_core_tables.py` 1:1 spec-parity pattern. Operational migration per dev-guide §7.1 (revision_id `20260513_auth_tables`, chains off `20260509_qc_adapter_cursor_seed`). FK on `backup_codes.account_id` + `sessions.account_id` added beyond spec's silent surface for referential consistency (NO ACTION default — accounts use soft-delete via `active_to`). Grant inheritance from 0006_roles.py:97-100 `ALTER DEFAULT PRIVILEGES` — no per-table GRANT needed. **A02 BINDS** — `risk-review-approved` label required (PR #69, Day 21 PR A). | Day 21 — Week 6 Tue `[CLAUDE_CODE]` per IG line 376 (PR #69 `risk-review-approved`) |
| `tests/integration/test_alembic_auth_tables.py` | 10 tests across 2 `Test*` classes — `TestUpDownUp` (3: alembic upgrade head → downgrade -1 → upgrade head; asserts current revision + presence/absence of the 4 tables at each step; proves `downgrade()` works); `TestSchemaShape` (7: column-level assertions against backend-spec §8.5 — column set + data types (uuid / bytea / int4 / text / timestamptz / inet / bool) + nullability + the sessions_account + sessions_expiry indexes + the webauthn_credentials.credential_id UNIQUE constraint + the four FKs to accounts). Spins up postgres:16 testcontainer; skips cleanly on Docker-unavailable per the test_audit_immutability.py pattern. **A22 BINDS + satisfied** (real Postgres triggers + grants + column types). Runtime: 8.02s locally. | Day 21 — Week 6 Tue PR #69 |
| `services/api/auth/` | 5 new modules per backend-spec §8.5 + frontend-spec §5: `ceremony.py` (in-memory `CeremonyStore` with `asyncio.Lock`-guarded dict — setup ceremonies 30-min TTL across the 4-step wizard, login ceremonies 5-min TTL single-use; injectable clock for tests; cap=256 entries with oldest-eviction fallback; Phase-2 cutover path documented to Redis or postgres when api scales multi-replica), `webauthn.py` (thin wrappers over `py_webauthn>=2.0`: LOCKED `UV=required` + `attestation=none`; `generate_registration_challenge` + `generate_authentication_challenge` returning canonical JSON-able dicts via `options_to_json` round-trip; `verify_registration` + `verify_authentication` catching the `WebAuthnException` base class so the route handlers can translate all subclasses including `InvalidJSONStructure` + `InvalidRegistrationResponse` to canonical `WEBAUTHN_VERIFY_FAILED` 401), `totp.py` (`pyotp>=2.9` wrappers + AES-256-GCM column encryption via `cryptography.hazmat.primitives.ciphers.aead.AESGCM` — 12-byte nonce per encrypt + 16-byte GCM tag for tamper detection; `load_encryption_key` accepts base64url-no-padding OR standard base64; rejects ≠ 32-byte decoded keys), `backup_codes.py` (8 single-use codes per dev-guide §1.5 LOCKED format `ABCDE-FGHIJ`: `secrets.token_bytes(8)` → base32 → first 10 chars → hyphen at index 5; effective entropy 50 bits per code; Argon2id-hashed at rest via `argon2-cffi` matching `setup_tokens` security posture; `normalize` strips whitespace + lowercases + restores canonical hyphen position; `verify` returns False on `InvalidHashError` so corrupted rows don't crash the recovery flow), `sessions.py` (session-row CRUD against the `sessions` table — `create_session` issues raw 32 bytes + DB row + returns `SessionRow` snapshot; `load_active_session` enforces idle + absolute gates with auto-revoke on idle-expiry stamped `idle_timeout`; `refresh_activity` bumps `last_activity_utc`; `upgrade_strength_to_strong` atomic single-statement UPDATE flipping `totp_bootstrap_only = FALSE` + stamping `last_uv_at_utc` per frontend-spec §5.4 LOCKED in-place upgrade; `stamp_uv` for the 5-min re-auth window; `revoke_session` idempotent with WHERE clause guarding on `evicted_at_utc IS NULL`; cookie encode/decode helpers tolerating base64url with or without padding). | Day 21 — Week 6 Tue (PR B; opened after PR A merges) |
| `services/api/repos/auth.py` | Query layer over the 4 Day 21 tables: `AuthRepo` Protocol + `PostgresAuthRepo` implementation. 11 methods — `find_account_by_username` (maps username to `external_account_id` column per Phase 0 single-operator convention), `find_or_create_bootstrap_account` (idempotent INSERT for the operator account, used by `/api/auth/webauthn/register/challenge` first-time path), `list_webauthn_credentials` (per-account + active-only filter), `get_webauthn_credential_by_id` (assertion lookup), `insert_webauthn_credential` (post-verify INSERT with `uuid_generate_v7`), `update_webauthn_sign_count` (replay-defence post-verify), `get_totp_secret` + `upsert_totp_secret` + `stamp_totp_last_used`, `list_unused_backup_codes` + `insert_backup_codes` (8 INSERTs per generation_id batch) + `mark_backup_code_used` (single-use enforcement) + `invalidate_backup_codes_for_account` (regen path — atomically marks all unused codes used). Uses `app_service` Postgres role per alembic 0006 grant inheritance. | Day 21 — Week 6 Tue (PR B) |
| `services/api/schemas/auth.py` | Extended from 1 model (Day 15 `AuthMeResponse`) to 19 models per backend-spec §4.1.1 — `SetupTokenVerifyRequest/Response`, `WebAuthnRegisterChallengeRequest/Response` + `WebAuthnRegisterVerifyRequest/Response`, `WebAuthnChallengeRequest/Response` + `WebAuthnVerifyRequest/Response` (login flow), `TotpEnrollChallengeRequest/Response` + `TotpSetupVerifyRequest/Response` + `TotpLoginVerifyRequest/Response`, `BackupCodesGenerateRequest/Response` + `BackupCodesRegenerateResponse`, `RecoverRequest/Response`, `LogoutResponse`. All models declare `extra="forbid"` per dev-guide §3 — unknown request fields → 422; unknown response fields → response_model round-trip test failure. WebAuthn JSON shapes (`options` / `credential`) typed as `dict[str, Any]` — W3C structure enforced by py_webauthn at verify time, not by Pydantic. | Day 15 (initial AuthMeResponse); Day 21 — Week 6 Tue (PR B, +18 models) |
| `services/api/routes/setup.py` | `POST /api/setup/verify-token` first-run bootstrap (backend-spec §4.1.1). Accepts the raw setup token printed once at api lifespan first-boot, Argon2id-verifies via `setup_tokens` table, marks consumed atomically, returns a `ceremony_session_id` that the WebAuthn registration flow consumes. **Day 24 ceremony fix — commit `5084d74 fix(api): verify_token must persist ceremony in CeremonyStore`:** Day-5 code minted a free-floating UUID7 and returned it as `ceremony_session_id` WITHOUT calling `CeremonyStore.create_setup_ceremony()` — Day-21's wizard endpoints (webauthn/register/challenge, totp/enroll-challenge, backup-codes/generate) all looked up the id in the CeremonyStore and rejected with 401 INVALID_CEREMONY_SESSION because nothing was ever stored. Fix: inject `CeremonyStore` via `Depends(get_ceremony_store)` and replace the UUID7 mint with `await store.create_setup_ceremony(setup_token_uuid=row.token_uuid, intended_role=row.intended_role)`. Regression test `test_verify_token_persists_ceremony_in_store` locks the handshake going forward. | Day 5 (initial verify-token + ceremony_session_id minting); Day 24 ceremony — wired into Day-21 CeremonyStore so the wizard endpoints find the ceremony |
| `services/api/routes/auth.py` | Extended from 1 endpoint (Day 15 `GET /api/auth/me` stub) to 12 endpoints per backend-spec §4.1.1: WebAuthn register/challenge + register/verify (setup wizard auth via ceremony_session_id; verify issues strong session cookie + atomically creates credential row); WebAuthn login challenge + verify (single-use ceremony_id; verify advances sign_count + issues strong session); TOTP enroll-challenge + setup-verify (wizard) + login verify (issues weak session per spec §5.4); backup-codes generate (wizard; returns 8 codes once + drops ceremony) + regenerate (re-auth gated; invalidates prior batch); recover (backup-code consumption + weak session); logout (revoke + clear cookies). Cookie issuance via `_set_session_cookies` — `__Host-trading_session` HttpOnly + Secure (in non-dev) + SameSite=Strict + 24h max-age + base64url-encoded raw session_id; CSRF cookie value freshly minted on every successful login response per double-submit pattern. `_require_recent_uv` gate on regenerate path enforces the 5-min re-auth window from dev-guide §1.5. The `WebAuthnException` base-class catch translates all py_webauthn subclasses including `InvalidJSONStructure` → 401 `WEBAUTHN_VERIFY_FAILED`. **Endpoint paths defer to spec §4.1.1 line 1700-1710** (`/register/challenge` + `/register/verify`) over IG line 376 colloquial names (`register-begin` + `register-complete`) per dev-guide §1.3 ambiguity protocol. | Day 15 (initial /me stub); Day 21 — Week 6 Tue (PR B, +11 endpoints) |
| `services/api/config.py` | Extended with Day 21 settings — `webauthn_rp_id` (default `localhost`; sops-overridden per env), `webauthn_rp_name` (`trading-system`), `webauthn_origin` (`http://localhost:3000` for dev; sops-overridden), `totp_encryption_key: SecretStr | None` (base64url-encoded 32-byte AES-256-GCM key; fails closed with 503 `TOTP_KEY_MISSING` if unset on TOTP-touching endpoints), `operator_username` (Phase 0 single-operator constant `operator`). | Day 5 (initial); Day 17 (rate-limit knobs); Day 21 — Week 6 Tue (PR B, WebAuthn + TOTP-key + operator-username) |
| `tests/unit/test_auth_helpers.py` | 35 tests across 4 `Test*` classes — `TestCeremonyStore` (8: unique key minting + get-missing returns None + expired-setup auto-prune + update persists mutations + consume pops entry + 5-min login TTL vs 30-min setup TTL + reset drops everything), `TestTotp` (13: 32-char base32 secret + otpauth URI shape + URL-encoded issuer with colon + verify round-trip + reject wrong code + encrypt/decrypt round trip with different nonces per call + reject wrong key length + reject short ciphertext + tamper detection via GCM tag + base64url/standard base64 key loading + reject wrong decoded length), `TestBackupCodes` (12: locked constants 8/10/5/5 + 8 codes per batch + AAAAA-BBBBB format + uniqueness within batch + hashes distinct from displays + verify round-trip + reject wrong code + reject corrupted hash via swallowed InvalidHashError + normalize strips whitespace/lowercase/restores hyphen + empty input → empty string + wrong-length passthrough + after-normalize round-trip realism), `TestModuleContract` (2). Pure-policy — no FastAPI app or testcontainers. | Day 21 — Week 6 Tue (PR B) |
| `tests/unit/test_auth_webauthn.py` | 15 tests across 6 `Test*` classes — `TestLockedConstants` (2: UV=required + attestation=none); `TestRegistrationChallenge` (6: challenge_bytes + user_id_bytes present + options has rp/user/challenge/pubKeyCredParams + authenticatorSelection.userVerification=required + attestation=none + exclude_credentials passthrough + supported_pub_key_algs includes ES256); `TestAuthenticationChallenge` (3: challenge_bytes + user_id None + allow_credentials passthrough + userVerification=required); `TestVerifyRegistrationFailures` (2: malformed credential raises WebAuthnException + wrong shape raises); `TestVerifyAuthenticationFailures` (1: malformed credential raises); `TestOptionsToDict` (1: round-trips registration options). Uses real py_webauthn primitives. | Day 21 — Week 6 Tue (PR B) |
| `tests/unit/test_auth_routes.py` | 26 tests across 12 `Test*` classes covering every endpoint — `TestAuthMe` (1), `TestWebAuthnRegisterChallenge` (2: invalid ceremony 401 + happy path issues options + persists state + creates bootstrap account), `TestWebAuthnRegisterVerify` (3: out-of-order 400 + invalid credential 401 + happy path inserts credential + issues strong session with last_uv_at), `TestWebAuthnLoginChallenge` (3: 404 no-account + 404 no-credentials + happy path returns ceremony_id + options), `TestWebAuthnLoginVerify` (3: invalid ceremony 401 + unknown credential 401 + happy path advances sign_count + issues strong session), `TestTotpEnrollChallenge` (1: happy path returns secret + URI + persists state), `TestTotpSetupVerify` (3: out-of-order 400 + invalid code 401 + happy path encrypts + persists secret + zeroes ceremony plaintext), `TestTotpLoginVerify` (3: account not found 401 + no secret 401 + happy path issues weak session), `TestBackupCodesGenerate` (2: out-of-order 400 + happy path returns 8 codes + drops ceremony), `TestRecover` (3: unknown account 401 + wrong code 401 + happy path marks one code used + issues weak session), `TestLogout` (1: clears cookies), `TestModuleContract` (1: all 12 routes present). Uses `api_client` + `wire_auth_overrides` fixtures with stub `AuthRepo` + fake DB + fresh `CeremonyStore` + monkeypatched `webauthn_mod.verify_registration` / `verify_authentication` / `sessions_mod.create_session`. | Day 21 — Week 6 Tue (PR B) |
| `tests/integration/test_auth_session_lifecycle.py` | 12 tests across 7 `Test*` classes — `TestCreateAndLoad` (3: row landed + load active + 404 unknown), `TestRefreshActivity` (1: bumps last_activity_utc), `TestRevoke` (2: stamps evicted_at + reason + idempotent), `TestIdleExpiry` (1: auto-revoke + reason `idle_timeout`), `TestStrengthUpgrade` (1: flips totp_bootstrap_only + stamps UV), `TestUvStamp` (1: updates last_uv_at), `TestCookieEncoding` (3: round-trip + None on malformed + 32 bytes). Real Postgres 16 testcontainer; `NullPool` engine so each test gets a fresh asyncpg connection (avoids "another operation is in progress" pool-share errors). **A22 BINDS + satisfied.** | Day 21 — Week 6 Tue (PR B) |
| `apps/web/src/lib/{api,auth,webauthn}.ts` | Day 21 frontend helpers per frontend-spec §5.1 + §5.2 + §5.3. `api.ts`: minimal typed fetch wrapper reading `NEXT_PUBLIC_API_BASE` (empty = same-origin); double-submit CSRF cookie reader + header attacher; `ensureBootstrapCsrfCookie` mints a 16-byte random token on first state-changing call before any login completes (server overwrites on first successful login response). `auth.ts`: hand-typed wrappers for every `/api/auth/**` endpoint mirroring `services/api/schemas/auth.py` (replaced by `packages/api-types/` codegen Phase 1+). `webauthn.ts`: `@simplewebauthn/browser@13.1.0` wrappers around `startRegistration({ optionsJSON })` + `startAuthentication({ optionsJSON })`; `isWebAuthnSupported()` checks `window.PublicKeyCredential`; `describeWebAuthnError` maps `NotAllowedError`/`InvalidStateError`/`AbortError` to operator-readable strings. | Day 21 — Week 6 Tue (PR B) |
| `apps/web/src/lib/{queryClient,format,toast,sse}.ts` + `apps/web/src/lib/api/{types,queries,mutations}.ts` | Day 22 frontend foundation per frontend-spec §4 + §8 LOCKED. `queryClient.ts`: TanStack Query global config — `staleTime: 30s` default, `gcTime: 10min`, retry skips 401/403/404/422/426 (else 3 retries with exponential backoff capped at 30s), `refetchOnWindowFocus: 'always'`, `refetchOnReconnect: 'always'`, `mutations.retry: 0`. Phase-1 in-memory only per §8.2 LOCKED — no IndexedDB persistence Day 22. `format.ts`: display-only adapters (`formatTimeET` + `formatDateTimeET` + `formatPnL` + `formatPnLPct` + `formatPrice` + `pnlSignClass`) using native `Intl.DateTimeFormat` with `timeZone: 'America/New_York'`; per spec §8.9 LOCKED frontend renders pre-computed strings only (no arithmetic). `toast.ts`: lightweight module-level toast store + `useToasts` hook via `useSyncExternalStore`; severity → duration mapping (P0=60s, P1=10s, P2=5s, info=4s). `sse.ts`: `@microsoft/fetch-event-source@2.0.1` connection manager + `useSSE` React hook — Last-Event-ID resume on reconnect; HTTP 426 buffer-expired path resets lastSeq + re-opens; exponential backoff with jitter `min(60s, 5s × 2^attempts) + random(0–10s)` capped at 10 attempts before transitioning to `degraded`; `session_evicted` envelope triggers toast + `evicted` terminal state; cache-invalidation per spec §8.6 SSE-event-type → `queryClient.invalidateQueries` map (14 types incl. `ping` heartbeat which is a no-op). `api/types.ts`: hand-typed mirrors of every shipped Phase-1 schema — `TodayDigestResponse` + `HealthScoreResponse` + `SystemStatus` + `PositionsResponse` + `SignalListResponse` + `FillsResponse` + `AlertsResponse` + `SSEEnvelope` + helper unions; Decimal fields typed as `string` per dev-guide §3.8 wire contract. `api/queries.ts`: 7 TanStack Query hooks (`useTodayDigest`, `useHealthScore`, `useSystemStatus`, `usePositionsCurrent`, `useSignalsPending`, `useRecentFills`, `useActiveAlerts`) with per-resource staleTime from spec §8.1 table. `api/mutations.ts`: 3 mutation hooks (`useApproveSignal`, `useRejectSignal`, `useDeferSignal`) calling the Day 15 501-stub endpoints; `SIGNAL_HANDLER_NOT_WIRED` ApiError surfaces a P2 toast pointing at the Week 4 Wed dispatcher PR; `onSettled` invalidates `['signals', {status: 'pending'}]`. Phase-1+ replaced by `packages/api-types/` codegen. | Day 22 — Week 6 Wed `[CLAUDE_CODE]` per IG line 380 |
| `apps/web/src/components/today/*.tsx` (8 files) + `apps/web/src/components/providers.tsx` | Day 22 Today-page section components per frontend-spec §2.2.2. `health-score-tile.tsx` — composite 0-100 + traffic-light color + click-expand component bars; Phase-0 renders gray "—" + tooltip "Track record under construction" because `insufficient_data=true`. `pnl-summary.tsx` — 4-column D/W/M/Y net P&L reading `useTodayDigest().pnl`; sign-class color tinting via `pnlSignClass`. `queued-signals.tsx` — table of pending signals with Approve / Reject / Defer buttons hitting the `useApproveSignal/useRejectSignal/useDeferSignal` mutations; **PR #115 — Reject + Defer now open `DecisionDiaryModal` for real tag + reasoning capture (placeholder `DecisionDiaryEntry` retired); modal stays open on mutation error so operator retries without retyping; Approve unchanged**; anomaly pill renders amber when `anomaly_reasons.length > 0`. `exposure-breakdown.tsx` — horizontal CSS bars per cluster + Gross/Net (no chart library per spec §2.2.2 D bundle-budget note); current/limit ratio + headroom-based color (emerald >50%, amber 25-50%, red <25%); placeholder cluster limits from §2.2.1 example until `/api/system/risk-envelope` wires Phase 1. `positions-table.tsx` — Market / Qty / Avg Cost / Mark / uPnL / Cluster / Strategy columns; virtualization deferred (>50 rows trigger lands Phase 1). `recent-fills.tsx` — Time ET / market / qty / price feed; `aria-live="polite"` region attribute set today, the announcer hook lands when `<LiveRegion>` ships Phase 1. `active-alerts.tsx` — P0→P1→P2 sort; severity-color pill + category + title + ET timestamp. `sse-status-pill.tsx` — connection-state indicator (Connecting/Live/Reconnecting/Degraded/Evicted/Offline) with color dot + last sequence_no when Live. `providers.tsx` — client-component wrapper hosting `<QueryClientProvider>` + Radix `<ToastProvider>` + a `<Toaster>` inner component reading `useToasts()` for the toast queue. Each section component is `'use client'` and uses TanStack Query hooks; loading/error/empty states all rendered per spec §2.2.3. Phase-0 all 7 endpoints return empty/insufficient-data so the empty-state UX is what production paints until Week 7 Mon swaps in real data. | Day 22 — Week 6 Wed `[CLAUDE_CODE]` per IG line 380; **PR #115 — `queued-signals.tsx` rewritten to use `DecisionDiaryModal`** |
| `apps/web/src/components/nav/nav-bar.tsx` + `apps/web/src/lib/routes.config.ts` (extended) | Global top nav per frontend-spec §1.3 LOCKED — single horizontal sticky bar (backdrop blur + mono text matching the Today-page header style), persistent across post-auth pages, **hidden** on `/login` / `/setup` / `/recover` (spec §1.3 "persistent across all *post-auth* pages"). App mark `●trd` (link to `/`); page links **Today / Signals / Trades / Performance / System / Calendar** sourced from `routes.config.ts` `NAV_LABELS` + `navItemsForPhase()` helpers and filtered by current phase; active route gets brighter text + underline (`aria-current="page"`); env + state pill on the right wired immediately from `useTodayDigest` (already cached app-wide via the Today-page tile, no extra HTTP cost). `routes.config.ts` extended with `/signals` (PR #113 shipped the page but never registered it) + `NAV_LABELS` lookup + `navItemsForPhase()` helper so adding a new page is a single-line registration. SSE-bound right-side TopBar elements per spec §1.6 (strategy version badge / health pill / portfolio P&L tab / agent status) deferred to Phase 1+ follow-up. Mobile breakpoint redesign (hamburger or horizontal scroll) + above-nav banners (Vacation / Convalescent / Incident-review per §1.3 stack) deferred to Phase 1+. Wired into `apps/web/src/app/layout.tsx`. | PR #114 — global nav (hot-fix whitelist; no `risk-review-approved`) |
| `apps/web/src/components/decision-diary-modal.tsx` | Real decision-diary capture per frontend-spec §3.1 LOCKED — replaces the Day-22 placeholder `DecisionDiaryEntry` hardcoded in `today/queued-signals.tsx` (`tag: 'manual_judgment'`, `reasoning_text: 'Phase-0 placeholder…'`). Tag radio with 5 locked values (`data_concern` / `regime_concern` / `size_concern` / `manual_judgment` / `other`) + reasoning textarea (10–2000 char range with live counter; submit disabled until valid; error after first blur). Kinds: `signal_reject`, `signal_defer`, `standalone` (standalone reserved for Phase 2; only the two signal kinds wired through `QueuedSignals` today). `entry_class` auto-set to `'signal_response'` for reject/defer (`'general'` for standalone). Cancel via button / backdrop click / Escape (via existing `ModalShell`). Modal **stays open on mutation error** so operator can adjust + retry without retyping reasoning_text; closes on successful submit. Lives at `components/` (not `components/today/`) since spec §3.1 calls out future standalone diary entries on Trades + other surfaces. Wired into `queued-signals.tsx` Reject + Defer; Approve unchanged. Phase-1+ follow-ups: focus trap inside `ModalShell` once Radix Dialog dep is added; component vitest once a vitest harness lands. | PR #115 — DecisionDiaryModal (hot-fix whitelist; no `risk-review-approved`) |
| `apps/web/src/app/signals/page.tsx` | `/signals` route — pending-signal table for the post-pivot pre-fill approval workflow per backend-spec §2.2.2 + frontend-spec §1.3 nav. Lists rows where `status='pending'`; operator approves / rejects / defers via `useApproveSignal` / `useRejectSignal` / `useDeferSignal` mutations from `apps/web/src/lib/api/mutations.ts`; producer-side `signal action=placed` SSE emitted at route layer post-approval (PR #107); reject / defer → `DecisionDiaryModal` (PR #115). Registered in `routes.config.ts` + gained `container mx-auto max-w-7xl p-4 md:p-6` wrapper in PR #114 (was the only post-auth page without horizontal padding). Phase 0 returns empty list per `/api/signals` until first `signal_emitted` lands post-cycle. **PR #286 (2026-05-28) — renders `<WatchingSection />` ABOVE `<QueuedSignals />`** — daily-resolution per-gate proximity view for the full V1 universe; sees the same TanStack-Query-via-SSE-prefix-invalidation refresh model (no new SSE event type per design D7); empty state pre-first-cycle: "Waiting for first LEAN cycle today (next at 21:30 UTC)" per §7.3. | PR #113 — `/signals` route + producer-side order_placed SSE emit (`risk-review-approved`); PR #107 (SSE-event emit at route layer; hot-fix); PR #114 — registered in nav + container wrapper; PR #115 — DecisionDiaryModal wiring; **PR #286 (2026-05-28) — `<WatchingSection />` mounted above pending list (frontend-only; no `risk-review-approved` per design D6)** |
| `apps/web/src/app/{layout,page}.tsx` (Day 22 rewrites) | `layout.tsx` extended to wrap `<main>` in `<Providers>` (the client-component hosting TanStack Query + Toast). `page.tsx` (root `/` Today route) replaced from the Day-20 13-line placeholder with the full 12-col grid composing the 8 Day-22 section components — Health Score + P&L Summary on row 1, Queued Signals + Exposure on row 2, Positions + Recent Fills on row 3, Active Alerts full-width on row 4 (col-span-12 below `lg:` breakpoint stacks all sections vertically per spec §2.2.1 "tablet/mobile" guidance). Page also mounts `useSSE()` once at the page root and renders `<SSEStatusPill>` in the page header. Bundle size: `/` 12.1 kB / 113 kB First Load JS (was 156 B / 87.5 kB on Day 20 placeholder — increment is TanStack Query + fetch-event-source + 8 section components). | Day 20 (placeholders); Day 22 — Week 6 Wed (Day-20 placeholder replaced with real content) |
| `apps/web/src/app/{setup,login,recover}/page.tsx` | 3 placeholder pages replaced with real ceremony components per frontend-spec §2.8.1-2.8.3 + §5.1-§5.3. `/setup` 4-step wizard — Step 1 token entry in password-style input (NOT URL + NOT browser history); Step 2 WebAuthn register via `browserStartRegistration` + nickname input (optional); Step 3 TOTP enroll-challenge → 192px QR data URI via `qrcode@1.5.4` + base32 secret display + 6-digit verify; Step 4 backup codes display once + Print/Download buttons + verbatim "I have saved my backup codes" acknowledgment field (case-sensitive, trimmed) that gates the Continue button. `/login` WebAuthn primary "Sign in with passkey" button + collapsed TOTP fallback "Use authenticator code" + backup-code link → `/recover`; browser-unsupported explainer when `!window.PublicKeyCredential`. `/recover` username + 10-char base32 backup code entry; on success → `/setup` for re-enrollment. Auth pages use `'use client'` directive + suspense-wrap `useSearchParams`; all pages call `ensureBootstrapCsrfCookie` on mount. Build sizes: `/setup` 13.7 kB / 112 kB First Load JS; `/login` 3.53 kB / 102 kB; `/recover` 2.94 kB / 99 kB. | Day 20 (placeholders); Day 21 — Week 6 Tue (PR B replaces with real ceremony components) |
| `apps/web/` | Next.js 14.2.35 App Router scaffold + 10 placeholder routes — `src/app/{page,layout,globals.css,trades/page,trades/[id]/page,performance/page,research/page,system/page,calendar/page,login/page,setup/page,recover/page}.tsx` + `src/lib/{utils,routes.config}.ts` + `src/components/ui/{button,card,table,toast}.tsx`. TypeScript strict per dev-guide §4.1 (`noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`); Tailwind 3.4.17 wired to LOCKED `Docs/frontend-spec.md` §9.1 tokens (`darkMode: 'class'` + literal hex colors per spec — NOT HSL CSS vars; `cssVariables: false` in `components.json`); shadcn/ui new-york style primitives (Button + Card + Table + Toast) installed manually since the interactive `shadcn-ui` CLI is not autonomous-session friendly; deps pinned (no `^`/`~`) — Next 14.2.35 (latest patched `next-14` dist-tag — Dec 2025 security advisory), TanStack Query 5.62.7, Recharts 2.15.0, `@microsoft/fetch-event-source` 2.0.1 (Day 22 SSE client uses this), pnpm 10.30.2 lockfile committed; `next.config.js` `output: 'standalone'` (SSR — rejected static export because Phase 1+ needs auth-gated pages + SSE proxying + dynamic params); `NEXT_PUBLIC_API_BASE` env var (zero bare-domain strings — A18 enforced); `apps/web/.env.example` documents the production `https://<your-domain>` substitution; `apps/web/Dockerfile` multi-stage (`node:20-alpine` builder + runtime; reuses the upstream `node` user at uid/gid 1000 instead of creating `trading` at the same gid — apk's `addgroup: gid '1000' in use` failure forced this fix on first attempt); build context = `apps/web/` (NOT repo root — the api + webhook_pusher containers need repo-root context for pyproject + cross-service source; this container only consumes apps/web, so context surgery is unnecessary until Phase 1+ adds `packages/api-types`). **Day 20 carryover — LIVE ON ASHBURN** at PR #66 merge commit `80a2d47`; container `trading-nextjs-1` healthy in 25s; `https://spratcapital.com/` renders the Day-20 placeholder body in dark mode (Day-19 503 maintenance page gone); two-surface smoke (loopback + external) all 5 sampled routes returned expected status codes (200 for `/`/`/trades/abc123`/`/login`/`/setup`; 404 for `/research` per `notFound()` spec); SSE + `/api/health` unchanged. **Day 24 ceremony fix — commit `a7b3e06 fix(web): theme form inputs for dark mode (@tailwindcss/forms reset fight)`:** added `@layer base` CSS in `apps/web/src/app/globals.css` to theme all form inputs + textareas + selects with `bg-bg-elevated text-text-primary` and `text-text-muted` placeholders. `@tailwindcss/forms` plugin enabled in `tailwind.config.ts` resets every input to a white-bg + dark-text default, fighting the operator's dark theme — without this fix, password-input dots rendered white-on-white and were invisible. | Day 20 — Week 6 Mon `[CLAUDE_CODE]` per IG line 375 (which the Day 20 PR also patches inline: `frontend/` → `apps/web/`); A27 satisfier = `docker compose --env-file deploy/.env.example build nextjs` + `pnpm typecheck && pnpm lint && pnpm build` in apps/web; A22 N/A; A01/A02/A04/A05/A06 N/A (pure frontend); **Day 20 carryover — VPS deployed + apex smoke PASSED; Day 24 ceremony — Tailwind forms reset fix for dark mode** |
| `docker-compose.yml` | Service inventory per backend-spec §1.4 — Day 5 `caddy`/`api`/`postgres` minimum + Day 10 `webhook_pusher` (PR #50, Phase-0-early so api stays `trading_internal`-only) + Day 20 `nextjs` (Next.js 14 frontend; `[internal]` only — Caddy already reverse-proxies apex `/` → `nextjs:3000` at `deploy/Caddyfile:95` from Day 5; bringing this service up resolves the Day-19 root-`/` 503 maintenance fallback); Phase 1+ services profile-gated (`--profile phase1`); Phase 2 IBKR + LEAN local profile-gated (`--profile phase2`). All built-image references use `ghcr.io/${GHCR_OWNER}/trading-<service>:${RELEASE_SHA:-latest}` substitution from `deploy/.env`; A18 enforced (no bare `<domain>`) | Day 5 (initial); Day 10 PR #50 (webhook_pusher); Day 20 (nextjs); **drill 6 follow-up (2026-05-25) — ib_gateway healthcheck probes BOTH external + internal ports (PR #239); new `autoheal` sidecar service block + `labels: { autoheal: 'true' }` on `ib_gateway:` (this PR)** |
| `deploy/autoheal/README.md` | Operator runbook for the `autoheal` sidecar (`willfarrell/autoheal:latest`). Drill 6 follow-up #3 (2026-05-25; `Docs/decisions-log.md` entry "ib_gateway stuck-at-login recurrence + recovery"). Pairs with PR #239's improved ib_gateway healthcheck to close the recovery loop: stuck Java process → internal-port probe fails → healthcheck `(unhealthy)` after ~3 min → autoheal invokes `docker restart ib_gateway` within 30s → IBC re-logs in (~6s per drill 6 evidence) → healthy. Worst-case time-to-recovery ~5.5 min vs. the 18h observed in drill 6. Trade-off documented: Docker socket mount = host-root-equivalent access to the autoheal container; mitigation is to pin image SHA + audit the ~150-line upstream bash script (https://github.com/willfarrell/docker-autoheal). Currently only `ib_gateway` is labeled (drill 5 + drill 6 motivated); explicit opt-in mechanism (`autoheal=true` label) so flaky healthchecks on other services don't accidentally restart-loop. Includes debug commands (force a healthcheck failure via `pkill -9 socat`), opt-out runbook (`docker compose stop autoheal`), and env-var tuning table (AUTOHEAL_INTERVAL=30 vs. upstream default 5; AUTOHEAL_CONTAINER_LABEL=autoheal vs. `all`). Hot-fix scope (`deploy/**`); no `risk-review-approved` label. | Drill 6 follow-up (2026-05-25; this PR) |
| `Makefile` | Repo-wide make targets — `lint` / `format` / `typecheck` / `test` (Python via ruff + mypy + pytest), `dep-drift-check` (api Dockerfile pip-install vs pyproject), `frontend-test` (Day 20: `pnpm typecheck && pnpm lint && pnpm build` in apps/web), `ci` (= lint + dep-drift-check + typecheck + test + frontend-test), `all` (= format + ci). `frontend-test` is wired into `ci` and `all` so future Python-only diffs don't accidentally regress the Next.js build. | Day 5 (initial); Day 8 (`dep-drift-check`); Day 20 (`frontend-test` target + wired into ci/all) |
| `.github/workflows/ci.yml` + `.github/workflows/README.md` | GitHub Actions CI gate — 7 jobs: `lint (ruff)` + `dep drift (Dockerfile vs pyproject)` + `gitleaks (secret scan)` + `typecheck (mypy --strict)` + `test (pytest --cov)` + **`frontend-test (pnpm typecheck + lint + build)` (Day 20 follow-up PR #67)** + `docker build` matrix with `include`-style for `api` + `webhook_pusher` + **`web` (Day 20 follow-up PR #67)**. Triggered on every PR + main push; `concurrency.cancel-in-progress: true` to dedupe in-flight runs. The `docker-build` matrix moved from short-form `service: [...]` to `include`-style Day 20 so `apps/web/` can specify its own `dockerfile` + `context` (other services use `context: .` for repo-root-relative pyproject paths; web uses `context: apps/web`). **PR #127 added path-filter on expensive jobs:** mypy / pytest / docker-build / frontend-test now skip when only `Docs/**`, `scripts/**`, `lean/**`, `tests/**`, `.github/**` paths change — saves ~3-5 minutes of CI on docs-only PRs. The `ci-gate` aggregate job runs unconditionally and pins required status check across both filter outcomes (always-on lint/dep-drift/gitleaks + conditionally-skipped expensive set). Branch protection on `main` then requires only `ci-gate` (per PR #128 close-out) rather than the per-job check list — keeps the required-checks UI stable as the matrix evolves. `.github/workflows/README.md` (PR #127) explains the gate pattern + the "what triggers what" matrix for operator orientation. | Day 5 (initial); Day 8 (`dep-drift-check`); Day 11 PR #50 (`webhook_pusher` to matrix); Day 20 PR #67 (`frontend-test` job + `web` in docker-build matrix via include-style); **PR #127 — path-filter + ci-gate aggregate (hot-fix scope `.github/**`); PR #128 — decisions-log close-out documenting branch-protection update to ci-gate-only** |
| `services/discord_bot/` | Discord bot service per backend-spec §6.6/§6.7 + frontend-spec §6.1 LOCKED Phase 0 surface. **LIVE in production at Day 24 carryover smoke (2026-05-15) — operator typed `/status` + `/positions` + `/halt Day 24 ceremony smoke test` in the operator's dev guild; all three returned the expected embeds (status ok green, empty-state info blue, confirm flow → 501 KILL_SWITCH_HANDLER_NOT_WIRED warning amber special-case).** Bot logs `discord_commands_synced command_count=3` + `discord_bot_ready bot_user=Trading System Bot#5325`. **Hot-fix whitelist (§2.3); `services/api/**` extension also on whitelist.** 9 modules: `__init__.py` (20 public exports — 4 color tokens + ApiClient family + BotSettings + 6 embed builders); `config.py` (Pydantic-Settings `BotSettings` env_prefix=""; required `DISCORD_BOT_TOKEN: SecretStr` + `DISCORD_GUILD_ID: int` + `DISCORD_BOT_API_BEARER_TOKEN: SecretStr`; optional `API_BASE_URL: str` default `http://api:8000` + `LOG_LEVEL` + `ENVIRONMENT`); `api_client.py` (typed httpx async client over `GET /api/health` + `GET /api/positions/current` + `POST /api/system/kill-switch/invoke` with canonical-envelope-aware `ApiClientHTTPError`; User-Agent `trading-discord-bot/0.1`; `Authorization: Bearer <token>` on every request; async-context-manager protocol); `embeds.py` (4 color tokens OK/INFO/WARNING/CRITICAL mirroring frontend §9.1; 3 internal helpers `_require_aware_utc` A06 guard + `_format_et` UTC→America/New_York via stdlib `zoneinfo` + `_format_signed_decimal`; 6 public builders: `build_positions_embed` empty-state + populated-state monospace table, `build_halt_confirm_embed` warning-amber with reason truncation, `build_halt_invoke_success_embed` post-Week-4-Wed-dispatcher response, `build_halt_invoke_error_embed` 501 KILL_SWITCH_HANDLER_NOT_WIRED special case → WARNING + dispatcher pointer, `build_status_embed` color-by-status with checks list, `format_kill_switch_invoke_failure`); `commands/{positions,halt,status}.py` (3 slash-command registrars; `commands/halt.py` `HaltConfirmView` 60s timeout + invoker-only `interaction_check` + consumed-once flag + ✓/✗ buttons); `main.py` (`TradingBotClient(discord.Client)` minimal `Intents.guilds=True` matching `deploy/discord/manifest.json`; `setup_hook` runs `tree.sync(guild=...)` for guild-scoped instant propagation; `on_ready` logs structured); `entrypoint.py` (sops yaml → env-var mapping mirroring `services/api/entrypoint.py`; reads `/run/secrets/decrypted.yaml` `discord.bot_token` + `.guild_id` + `.api_bearer_token`; fail-closes exit 2 with operator-readable error if missing); `Dockerfile` (multi-stage `python:3.11-slim` mirroring webhook_pusher pattern; non-root `trading` user; tini ENTRYPOINT → `python -m services.discord_bot.entrypoint`; runtime deps: discord.py>=2.4 + httpx>=0.28 + structlog>=24.4 + pydantic>=2.9 + pydantic-settings>=2.6 + pyyaml>=6.0; pre-runtime import sanity check). **Bot ↔ api auth model:** shared sops-decrypted Bearer token per backend-spec §6.6 + §4.4. The api-side validator is `services/api/middleware.BotAuthMiddleware` running OUTERMOST in the stack (outer of CSRF + SessionStub). Phase 0 ships `/positions` + `/halt` + `/status` per spec §6.1 LOCKED. Phase 1+ adds `/pnl` + `/exposure` + `/calendar` + `/last-fills` + `/ratify` + `/health` + `/vacation start` + `/close` + `/diary` + `/audit` + `/today` + the seven channel push paths (`#daily-brief`, `#signals`, `#fills`, `#alerts`, `#critical`, `#ops`, `#audit`). | Day 23 (PR; regular review; closes Week 6 verification gate box 3 at test-and-build-contract level) |
| `tests/unit/test_discord_bot_embeds.py` | 44 tests across 8 `Test*` classes — TestColorConstants (2: distinct + 24-bit range); TestFormatHelpers (7: `_format_et` short + with_date + reject naive + `_require_aware_utc` aware + reject naive + `_format_signed_decimal` positive/zero/negative); TestPositionsEmbed (8: empty-state title format + INFO color + "No open positions" body + footer env + populated count + symbol rendering + code block + signed uPnL + env propagates); TestHaltConfirmEmbed (6); TestHaltInvokeSuccessEmbed (5); TestHaltInvokeErrorEmbed (4 incl. 501 KILL_SWITCH_HANDLER_NOT_WIRED special case WARNING + Week-4-Wed pointer); TestStatusEmbed (10: ok→OK + degraded→WARNING + checks ✅/❌ glyphs + latency + detail). Pure Python; no `dpytest`, no live Discord; A22 N/A; A06 enforced. | Day 23 |
| `tests/unit/test_discord_bot_commands.py` | 15 tests across 5 `Test*` classes — stub `ApiClient` (real instance with AsyncMock-replaced methods) + MagicMock `discord.Interaction`; `_build_tree()` constructs `CommandTree` against MagicMock client with explicit `_command_tree=None`; `_command_callback` invokes via `tree.get_command(...).callback`. TestPositionsCommand (2: happy path + api-error); TestHaltInputValidation (3: empty/whitespace + too-long + happy path sends `HaltConfirmView`); TestHaltConfirmView (5: confirm 200 success + confirm 501 special case + cancel edits message + interaction_check non-invoker + consumed-once); TestStatusCommand (2); TestRegistrationSanity (2: register without/with guild). Direct invocation pattern: `await view.children[i].callback(interaction)` (discord.py's `_ViewCallback` auto-binds view+item). Pure Python; A22 N/A. | Day 23 |
| `tests/unit/test_api_bot_auth.py` | 14 tests across 5 `Test*` classes — targets `POST /api/system/kill-switch/invoke` because handler depends only on `get_session_context` (no DB-touching repo dep — exercises full middleware chain without testcontainers). TestNoAuthHeader (2: /api/health 200 + POST gets 403 CSRF_REJECTED); TestInvalidBearer (4: 401 BOT_AUTH_INVALID + invalid bearer on POST 401 not 501 + empty bearer falls through + non-bearer scheme falls through); TestValidBearer (3: bypasses CSRF on POST → 501 stub + bad body 422 + GET /api/health 200); TestNoBotConfigured (1: no token + bearer header → 403 CSRF noop); TestProductionFailClose (2: live-small no-bearer 401 AUTH_REQUIRED + live-small valid-bearer 501 stub — proves BotAuth runs BEFORE SessionStub fail-close). Real `httpx.AsyncClient` over `ASGITransport(api_app)`; no DB pool initialized. | Day 23 |
| `services/api/middleware.py` | Day 23 extension: new `BotAuthMiddleware` runs OUTERMOST in the stack. `Authorization: Bearer <token>` constant-time-compared via `secrets.compare_digest()` against `settings.discord_bot_bearer_token`; on match injects `SessionContext(user_id="discord-bot", username="discord-bot", role="owner", auth_strength="strong", is_phase0_stub=False)` + `request.state.is_bot_authenticated = True`; on mismatch 401 `BOT_AUTH_INVALID` canonical envelope; on no header / no configured token noop pass-through. Lazy import of `SessionContext` to break the `middleware ↔ session ↔ config` import cycle. **`CSRFMiddleware.dispatch` extended with `not getattr(request.state, "is_bot_authenticated", False)` skip** — bot has no cookies; double-submit pattern doesn't apply; forcing CSRF would mean two secrets to manage. **`register_middleware` ordering after Day 23: `BotAuth → SessionStub → RequestContext → RateLimit → CSRF → routes`** (BotAuth is added LAST in `main.py` per Starlette LAST-ADDED = OUTERMOST convention). | Day 5 (initial — CSRF + RequestContext); Day 17 (RateLimitMiddleware + register_middleware ordering); Day 23 (BotAuthMiddleware + CSRF skip-if-bot) |
| `services/api/session.py` | Day 23 extension: `SessionStubMiddleware.dispatch` extended with `if getattr(request.state, "session", None) is not None: return await call_next(request)` skip — preserves the bot's strong-auth context in production envs (`live-small`/`live-scale`) which would otherwise hit the fail-close branch before BotAuth's session reached the route. Module docstring unchanged (the stub-only behavior is unchanged for the human session path). | Day 15 (initial); Day 16 (`/api/sse/events` exemption removed); Day 23 (skip if bot session already injected) |
| `services/api/config.py` | Day 23 extension: `APISettings.discord_bot_bearer_token: SecretStr | None = Field(default=None, ...)`. Sourced from sops `discord.api_bearer_token` per `deploy/discord_bot/README.md`. When unset, BotAuthMiddleware degrades to noop (bot path not served until operator wires the secret). | Day 5 (initial); Day 17 (rate-limit knobs); Day 21 (WebAuthn + TOTP); Day 23 (`discord_bot_bearer_token`) |
| `services/api/entrypoint.py` | Day 23 extension: adds `API_DISCORD_BOT_BEARER_TOKEN` env-var mapping from sops `discord.api_bearer_token`; `_looks_like_placeholder` guard mirrors the watchdog mapping; degrades gracefully when missing (BotAuthMiddleware degrades to noop, bot-path not served). | Day 5 (initial); Day 23 (`API_DISCORD_BOT_BEARER_TOKEN` mapping) |
| `deploy/discord_bot/README.md` | Operator runbook (A27 satisfier per dev-guide §6.8 alternative (b)) for `services/discord_bot/`: Step 1 generate bearer via `python3 -c 'import secrets; print(secrets.token_urlsafe(32))'`; Step 2 add `discord.api_bearer_token` to `secrets/paper.enc.yaml` via `sops` (encrypted-edit + re-encrypt + git commit + push); Step 3 VPS deploy (SSH-delegated: `git pull --ff-only` + sops decrypt refresh + `docker compose build api discord_bot` + `docker compose up -d api discord_bot` + watch logs for `discord_bot_ready`); Step 4 live smoke (`/status` → operator-readable embed; `/positions` → empty-state UX; `/halt test ...` → confirm view → cancel + re-issue + confirm → 501 KILL_SWITCH_HANDLER_NOT_WIRED special-case embed); token rotation procedure (quarterly per spec §6.6); 8-row troubleshooting table. **Closes Week 6 verification gate box 3 live-smoke half at Day 23 carryover OR Day 24 Fri ceremony per PR #71 deferral.** | Day 23 |
| `infrastructure/`, `packages/`, remaining `services/*` (signal, execution, agent, monitoring, observability) | Scaffolded directories with `__init__.py` only | filled Week 3+ per `implementation-guide.md` §3 |

### Rows added in the 2026-05-28 refresh

| Path | Purpose | Status |
|---|---|---|
| `scripts/operator_tools/bootstrap_live_account.py` | One-time operator tool for the live-money cutover: registers an `accounts` row + initial `balances` row to enable capital-event invocation per cutover-plan §7 (the prereq for the first deposit's `apply_capital_event` call). Idempotency guard: refuses to run when an active account already exists (the cutover-plan workflow only fires this ONCE per env). Sourced from sops for IBKR account-id + initial-funding parameters; emits an `account_bootstrapped` audit event + `balance_snapshot` audit event in a single ordered batch. Pure operator tool (`scripts/operator_tools/**` — NOT on forbidden whitelist; pre-merge linter doesn't gate). | Pre-2026-05-28 — landed alongside the cutover-plan B2/B3/B5/B7 deploy infrastructure (PR #269) |
| `scripts/operator_tools/recovery_agent.py` | 60-second systemd timer that polls the `alerts` table for `worker_failure` rows + auto-replays the orphan execution via a transient subprocess call to `replay_executions.py` (the canonical mechanism for backend-blind-fills recovery from the 2026-05-18 drill 5 retrospective). Audit-first ordering preserved — emits `RECOVERY_ACTION_TAKEN` audit row BEFORE the replay subprocess fires; on subprocess failure emits `RECOVERY_ACTION_FAILED` + posts to Discord `#critical`. Closes manual-step 5/7 from the drill 5 punch list. Wired into systemd via `deploy/systemd/recovery-agent.{service,timer}` (timer fires every 60s; service drops privilege + runs as `trading` user). Locks `alerts` row via `SELECT ... FOR UPDATE SKIP LOCKED` so multiple recovery-agent instances safely race without double-replaying. Pure operator tool. | 2026-05-25 — landed as the drill 5 follow-up #5 codification PR; see `Docs/decisions-log.md` 2026-05-25 entry "ib_gateway stuck-at-login recurrence + recovery (drill 6)" subsection on the recovery agent |
| `scripts/operator_tools/trigger_v1_cycle.py` | Operator tool to fire a V1 LEAN signal cycle off the daily 21:30 UTC schedule — mirrors LEAN's `_post_event("signal_emitted", ...)` shape. Defaults to `--dry-run`; requires explicit `--confirm` to actually emit. Risk-state gate built in: refuses to fire when `risk_state.state != 'NORMAL'` unless `--ignore-risk-state` is passed (the only callers that need it are the operator manually probing convalescent-mode behavior). Dedup against the `signals` table by `(account_id, market, signal_id_hint, generated_at_utc_minute_bucket)` so re-running within the same minute doesn't double-emit. CLI args mirror the `master_client_id_probe` envelope (`--env`, `--allow-non-paper`). Useful for: (a) one-shot intraday signal injection during manual ratification tests; (b) reproducing strategy behavior at known parameter snapshots; (c) ad-hoc V1 cycle smoke tests outside the scheduled window. | 2026-05-26 — landed as PR #248; see operator-profile memory note "Trigger V1 cycle tool" |
| `scripts/operator_tools/replace_protective_stop.py` + `tests/integration/test_replace_protective_stop_end_to_end.py` (script row; integration-test row already exists at line 95) | Operator-coordinated recovery tool for the case where a protective bracket stop has been cancelled (manually in TWS OR via cross-clientId cancel side-effect like the 2026-05-27 evening `reqGlobalCancel` from clientId=86 that took out the /M2K stop). Reads the position from the `positions_current` table + the entry order's `client_order_id` + entry direction; computes the replacement stop_price = `entry_avg_price * (1 - V1_TRAILING_STOP_PCT)` (or `_PCT` above for short); audit-first ORDER_PLACED commits BEFORE the orders row pre-INSERT; places via the operator clientId (default 99); subscribes + ack-waits before returning so the post-place DB UPDATE lands `status='working'` + `acknowledged_at_utc` (PR #276 follow-up fix). Idempotency guard: refuses to run when a working bracket stop already exists for the position (operator overrides with `--force`). Risk-state bypass: succeeds even from HALT_NEW (the recovery path explicitly does NOT consult risk_state — operator-initiated stop replacement is always allowed). Forbidden-path-adjacent (calls services/execution + services/risk) but the script itself is in `scripts/operator_tools/**` which is not on the whitelist. | PR #258 (initial); PR #262 — race-window guard + advisory tests; PR #267 — integration tests (already indexed at line 95); PR #276 — subscribe + ack-wait so post-place DB UPDATE lands cleanly |
| `Docs/flexquery-template-1505530-add-fut-section-runbook.md` | Step-by-step IBKR Account Management portal runbook for adding the FUT asset class + `underlyingSymbol` column to FlexQuery template 1505530's Open Positions section. Five phases: pre-flight (sanity-check system health + 02:00-04:00 UTC trough timing), edit (IBKR portal navigation), verify (download XML + manual grep for `assetCategory="FUT"` rows), watch next EOD cycle (post-22:30 UTC validation), document outcome (append a line to `Docs/decisions-log.md`). Rollback path documented. Pre-fix the recon planner saw `broker_view={no futures}` while the backend's tracker had real futures positions, triggering false `position_qty` breaks; PR #275 (already deployed) downgrades these to non-actionable but the recurring P2 alert noise persists until the template is fixed at IBKR — this runbook closes that loop. | PR #279 (2026-05-28); operator action queued for tonight 02:00-04:00 UTC |
| `Docs/signal-proximity-design.md` | 551-line design doc for the 3-PR signal-proximity feature, signed off + COMPLETE 2026-05-28. Mirrors the structure of `Docs/exit-pipeline-design.md` (COMPLETE 2026-05-27). Covers: motivation (operator wants at-a-glance per-gate proximity above `/signals`'s pending list); architecture diagram (strategy module → LEAN heartbeat → api persistence + endpoint → frontend table); proximity definition per gate (Donchian / Trend / Hurst — directional vs direction-agnostic + PASS/CLOSE/FAIL classification with operator-tunable threshold constants `DONCHIAN_CLOSE_BAND_PCT=0.01` / `TREND_CLOSE_BAND_PCT=0.005` / `HURST_CLOSE_BAND=0.02`); §3.3 sort key (fired-today PASS at top, then CLOSE ascending by gap on closest_gate, then FAIL by smallest gap to passing, then warming-up grouped at bottom); locked operator decisions D1-D7 (single source of truth = strategy module; daily refresh cadence only; entry-side gates only; dedicated `signal_proximity` table not JSONB; per-gate score in `[fail, threshold, pass]` shape; PR-A/B label requirements; match existing refresh model — no new SSE event type); resolved open questions Q1-Q6 (both directions surfaced; no audit_log; no shadow computation; decommissioned still emits; PR-only threshold tuning V0; no historical backfill); risks + mitigations; out-of-scope deferrals (exit-side proximity / intraday / runtime threshold tuning); per-PR file-touch summary §13; PR-A/B/C kickoff prompts §15-17. | 2026-05-28 — PR #283 (created + signed off); referenced by close-out entry "Signal-proximity 3-PR plan SHIPPED end-to-end" in `Docs/decisions-log.md` 2026-05-28 (afternoon) |
| `strategies/v1_trend_following/proximity.py` + `tests/unit/test_v1_signal_proximity.py` | Pure-Python proximity module — zero LEAN imports, pytest-runnable from project root. Owns the per-gate state classifier + headroom math + threshold constants. **Operator-tunable thresholds (PR-only in V0):** `DONCHIAN_CLOSE_BAND_PCT=Decimal("0.01")` (within 1% of breakout = CLOSE), `TREND_CLOSE_BAND_PCT=Decimal("0.005")` (within 0.5% gap = CLOSE), `HURST_CLOSE_BAND=Decimal("0.02")` (within 0.02 of threshold = CLOSE). `GateState(StrEnum)` = `PASS/CLOSE/FAIL`; `GateProximity(state, headroom: Decimal\|None, detail: str\|None)`; `MarketProximity(market, long_donchian, short_donchian, long_trend, short_trend, hurst, last_close, overall_state, closest_gate: 'donchian'\|'trend'\|'hurst'\|'history', gate_status: 'ok'\|'warming_up'\|'decommissioned')`. Public `compute_market_proximity(*, market, last_close, donchian_high, donchian_low, ma_fast, ma_slow, hurst_value, hurst_threshold, decommissioned=False)` — warming-up path emits sentinel FAIL gates with `headroom=None` + `closest_gate='history'` + `gate_status='warming_up'` (or `'decommissioned'` when the kill switch is on). `_classify_donchian` / `_classify_trend` / `_classify_hurst` are pure-policy + side-effect-free. `_worst` / `_better` helpers via `_STATE_RANK` (PASS=2, CLOSE=1, FAIL=0); overall_state = worst-of-gates per design §3.3. Lives in `strategies/v1_trend_following/**` (forbidden-path scope; `risk-review-approved` required) NOT `services/signal/**`. **Bit-identical promise:** PR-A's gate-evaluation logic in `_evaluate_market` is UNCHANGED — this module exposes already-computed snapshot values + classifies them; no new gate, no threshold change, no scoring change that affects what fires. Test coverage: state classification with exact-threshold ties, headroom math, insufficient-history sentinel, decommissioned still emits, same-direction-position preserves existing reject reason while still emitting proximity, property-based `overall_state==worst_gate_state`. | PR #283 (2026-05-28) — signal-proximity PR-A (`risk-review-approved`; `strategies/v1_trend_following/**` forbidden-path scope); see `Docs/signal-proximity-design.md` §4 + `Docs/decisions-log.md` 2026-05-28 (afternoon) entry |
| `alembic/versions/2026-05-28_signal_proximity.py` + `services/api/repos/signal_proximity.py` + `services/api/schemas/signal_proximity.py` + `tests/integration/test_signal_proximity_persistence.py` + `tests/integration/test_signal_proximity_endpoint.py` | PR-B persistence + endpoint surface for the signal-proximity feature. Migration creates `signal_proximity` (BIGSERIAL PK + `cycle_ts_utc TIMESTAMPTZ` + `session_date_et DATE` + `market TEXT` + per-gate `_state TEXT NOT NULL` + `_headroom_pct/_gap_pct/headroom NUMERIC` + `overall_state TEXT NOT NULL` + `closest_gate TEXT NOT NULL` + `gate_status TEXT NOT NULL` + 8 raw snapshot NUMERIC columns: `last_close donchian_high donchian_low ma_fast ma_slow hurst_value hurst_threshold atr`) + 2 indexes (`idx_signal_proximity_market_cycle(market, cycle_ts_utc DESC)` for latest-per-market lookups + `idx_signal_proximity_cycle_ts(cycle_ts_utc DESC)` for global cycle queries). Retention: keep all rows (~10/day, negligible storage; aligns with Q2 — the table IS the durable audit trail). Repo: `SignalProximityRow` dataclass + `insert_signal_proximity_rows(rows, conn)` (best-effort write — logs + 202 on failure, NOT 5xx; per §6.3 a DB issue doesn't take down the LEAN cycle) + `fetch_latest_proximity_per_market(env, conn)` (latest-row-per-market subquery + ordered for response). Schemas: `GateView(state, headroom)` + `MarketProximityView` (nested + sorted at api edge) + `SignalProximityResponse(as_of_cycle_ts_utc, markets)`. `GateStateLiteral='pass'\|'close'\|'fail'`, `GateStatusLiteral='ok'\|'warming_up'\|'decommissioned'`, `ClosestGateLiteral='donchian'\|'trend'\|'hurst'\|'history'`. `row_to_view` maps the flat repo row to the nested API shape per design §6.4. Endpoint `GET /api/signals/proximity` added to existing `services/api/routes/signals.py` (the api-routes file is on the §2.3 hot-fix whitelist; same auth pattern as `GET /api/signals`). Heartbeat handler in `services/api/routes/internal/lean.py` persists rows when `market_evaluations` is present (audit-first ordering NOT required — these rows are observational, not state-change; per Q2 + design §6.3 audit-first ordering note). 959 integration test lines (heartbeat-with-evaluations → rows landed + endpoint returns latest-per-market + sort + ordering + warming-up handling). | PR #284 (2026-05-28) — signal-proximity PR-B (`risk-review-approved`; `alembic/**` forbidden-path scope); see `Docs/signal-proximity-design.md` §6 + `Docs/decisions-log.md` 2026-05-28 (afternoon) entry |
| `apps/web/src/lib/api/signals-proximity.ts` + `apps/web/src/components/signals/WatchingSection.tsx` | PR-C frontend surface for the signal-proximity feature — renders the "Watching" section above the existing pending-signals list on `/signals` per design §7.1. `signals-proximity.ts` (194 lines): TypeScript types mirror `services/api/schemas/signal_proximity.py` field-by-field (`GateStateLiteral`, `GateStatusLiteral`, `ClosestGateLiteral`, `GateView`, `MarketProximityView`, `SignalProximityResponse`); pure helpers `bucketOf` (PASS+ok=fired / CLOSE+ok=watching / FAIL+ok=inactive / gate_status≠ok=warming) + `closestGateAbsGap` (min abs(headroom) across long/short for directional gates; abs(headroom) for Hurst; +Infinity for history-bucket warming-up) + `sortProximityMarkets` (4-bucket sort per design §3.3 with localeCompare tie-break); `useSignalsProximity` TanStack Query hook keyed `['signals', 'proximity']` so the existing `signal` SSE event in `lib/sse.ts` (`INVALIDATE_KEYS.signal=[['signals'], ...]`) prefix-invalidates this view when a market fires mid-session (D7: NO new SSE event type — CLAUDE.md locked rule). `staleTime=60_000` (daily-resolution data; matches `useHealthScore`'s cadence). **Decimal handling:** Decimal-on-the-wire stays as `string` end-to-end; `parseFloat` is used ONLY at the display/sort layer where values are fractional + bounded to ~[-1, 1] (well within IEEE 754 precision; no rounding in transit). No new `bignumber.js` / `decimal.js` dep — matches `lib/format.ts`'s existing Number-at-display convention. `WatchingSection.tsx` (321 lines): Card-wrapped table; per-row three gate chips (PASS=`bg-health-green/20 text-health-green`, CLOSE=`bg-health-yellow/20 text-health-yellow`, FAIL=`bg-health-red/20 text-health-red` — matches `severity-p*` chip pattern in `queued-signals.tsx`); directional gates display the BETTER of long/short (other direction stays in wire payload for future hover-expansion); closest-gate column; last-close + cycle-ts footer; pre-first-cycle empty renders verbatim "Waiting for first LEAN cycle today (next at 21:30 UTC)" per §7.3; warming-up markets group at bottom with "warming up" badge + 60% opacity row tint; decommissioned markets render with `title="Strategy decommissioned — view-only"` per Q4. Wired into `apps/web/src/app/signals/page.tsx` above the existing `<QueuedSignals />`. **No test framework** — §13 strict file-scope; logic extracted into pure helpers in `signals-proximity.ts` for V1 follow-up when frontend test infra lands. | PR #286 (2026-05-28) — signal-proximity PR-C (frontend-only; no `risk-review-approved` per design D6); see `Docs/signal-proximity-design.md` §7 + `Docs/decisions-log.md` 2026-05-28 (afternoon) entry |
| `strategies/v1_trend_following/exit_proximity.py` + `tests/unit/test_v1_exit_proximity.py` | The exit-side mirror of `proximity.py` — pure-Python, zero LEAN imports. Owns the per-position exit-trigger classifiers + the `ExitState(StrEnum)` = `HOLDING/NEAR/TRIGGERED` enum (valence INVERTS vs entry `GateState`: exit-green = "safe", exit-red = "closing", per design Q4). Tunable NEAR bands (PR-only, observation-only): `EXIT_TREND_NEAR_BAND_PCT=0.005`, `EXIT_STOP_NEAR_BAND_PCT=0.01`, `EXIT_HOLDING_DAYS_NEAR_DAYS=3`. Public: `classify_stop(*, last_close, stop_price, direction)` (mark-vs-stop band; NULL-state HOLDING when no stop), `classify_reversal` + `directional_reversal_entry_state` (derive reversal from opposite-direction entry proximity, ED4), `compute_position_exit_proximity(...)` (one held position → `PositionExitProximity`), and `combine_overall_exit(*, trend_flip, stop, reversal, decommission) -> (ExitState, str)` (the §3.3 overall/closest combiner — **extracted in PR-B** so the LEAN compute + the api stop-enrichment share one combiner). `risk-review-approved` (forbidden-path `strategies/v1_trend_following/**`). | PR #308 (2026-05-31) — exit-proximity PR-A (classifier + LEAN emission); `combine_overall_exit` extracted in PR-B (2026-05-31); see `Docs/exit-proximity-design.md` §3-4 + `Docs/decisions-log.md` 2026-05-31 entries |
| `alembic/versions/2026-05-31_exit_proximity.py` + `services/api/repos/exit_proximity.py` + `services/api/schemas/exit_proximity.py` + `tests/integration/test_exit_proximity_{persistence,endpoint,migration}.py` | PR-B persistence + endpoint for the exit-proximity feature. Migration creates `exit_proximity` (BIGSERIAL PK + `cycle_ts_utc TIMESTAMPTZ` + `session_date_et DATE` + `market`/`direction TEXT` + `held_days INTEGER` + `last_close`/`stop_price NUMERIC` + per-trigger `{trend_flip,stop,reversal,decommission}_state TEXT NOT NULL` + `trend_flip_headroom`/`stop_headroom_pct NUMERIC` + `held_days_headroom INTEGER` + `overall_state`/`closest_exit`/`gate_status TEXT NOT NULL`) with CHECK constraints pinned to the `ExitState`/`gate_status`/`closest_exit`/`direction` alphabets + 2 indexes (`idx_exit_proximity_market_cycle(market, cycle_ts_utc DESC)`, `idx_exit_proximity_cycle_ts(cycle_ts_utc DESC)`); no FK to `accounts` (universe-scoped, like `signal_proximity`). Repo: `enrich_exit_evaluation(ev, *, stop_price)` (pure Q1 = (B) transform: re-classify stop via `classify_stop` + RE-DERIVE overall/closest via `combine_overall_exit` + derive `held_days_headroom` from locked `MIN_HOLDING_DAYS`), `_fetch_latest_working_stop_price` (market-only join on latest WORKING `stop_market` order), `insert_rows` (best-effort batch; per-position stop join then INSERT), `fetch_latest_per_market` (`DISTINCT ON` + Python closest-to-closing sort: TRIGGERED→NEAR-asc→HOLDING per §3.3). Schema: `ExitTriggerView` + `PositionExitProximityView` + `ExitProximityResponse(as_of_cycle_ts_utc, positions)` + `row_to_view`; Literals imported from `schemas/lean.py` (one source). Heartbeat handler (`routes/internal/lean.py`) persists rows when `position_exit_evaluations` is present — best-effort (logs + 202, never 5xx; no `audit_log`, Q6). Endpoint `GET /api/today/exit-proximity` in `routes/today.py` (Q3 = Today page; same `get_session_context` auth as the other Today endpoints). | PR #309 (2026-05-31) — exit-proximity PR-B (`risk-review-approved`; `alembic/**` + combiner extraction); see `Docs/exit-proximity-design.md` §6 + `Docs/decisions-log.md` 2026-05-31 entries |
| `apps/web/src/lib/api/exit-proximity.ts` + `apps/web/src/components/today/ExitWatchingSection.tsx` (+ `apps/web/src/app/page.tsx` + `apps/web/src/components/today/active-alerts.tsx` wiring) | PR-C frontend surface for the exit-proximity feature — the "Exits" section on the **Today page** (Q3), rendered full-width directly below the Positions/Recent-Fills row (co-located with the held-positions context). The exit-side mirror of `signals/WatchingSection.tsx` (#286) with two deliberate differences. `exit-proximity.ts`: TypeScript types mirror `services/api/schemas/exit_proximity.py` field-by-field (`ExitStateLiteral='holding'\|'near'\|'triggered'`, `ClosestExitLiteral`, `ExitGateStatusLiteral`, `DirectionLiteral`, `ExitTriggerView`, `PositionExitProximityView`, `ExitProximityResponse`); pure helpers `exitStateChipClass` (**INVERTED-valence** state→token map: HOLDING=green/"safe", NEAR=yellow, TRIGGERED=red/"closing" — a SEPARATE map from the entry-side `GateState` to avoid painting a closing position green, design Q4), `isStopUnprotected` (stop HOLDING + `stop_price===null` ⇒ latent POSITION_UNPROTECTED), `isDecommissionArmed`, `closestExitTrigger`; `useExitProximity` TanStack Query hook keyed `['positions', 'exit-proximity']` so the existing `position`/`fill` SSE events (`INVALIDATE_KEYS` ⊃ `['positions']`) prefix-invalidate it (ED8: NO new SSE event type — CLAUDE.md locked). `staleTime=60_000` (matches `useHealthScore`). **NO client-side sort** — the endpoint returns rows already ordered closest-to-closing (PR-B §3.3); rendered in receipt order (the explicit divergence from signal-proximity PR-C, which sorted client-side). `ExitWatchingSection.tsx`: Card-wrapped table — per row symbol · direction badge · held-days-vs-MIN_HOLDING (from `held_days_headroom`) · one state-chip per trigger (Stop / Trend-flip / Reversal / Decommission) · a "Closest exit" summary chip (named trigger + its headroom, colored by `overall_state`) · last-close + cycle-ts footer; the "no stop on record" case renders a distinct warning chip cross-linking `#active-alerts` (POSITION_UNPROTECTED, §3.4/§11 — hence the `id="active-alerts"` + `scroll-mt-20` added to `active-alerts.tsx`); decommission-armed renders a section banner "Strategy decommissioned — closing next cycle"; warming-up rows tint 60% + show a muted trend-flip chip; **two distinct empty states** disambiguated via the shared `usePositionsCurrent()` cache ("Waiting for first LEAN cycle today" when open positions exist but no proximity row yet, vs "No open positions" otherwise). Wired into `apps/web/src/app/page.tsx` as a `col-span-12` row. **No test framework** in the web workspace — logic extracted into pure helpers for the V1 follow-up when frontend test infra lands; gate = `make frontend-test` (pnpm typecheck + lint + build, all green). **⚠️ DEPLOYED to paper 2026-05-31 ~21:22 UTC (A+B+C bundle) but the "Exits" section is INERT** — the live exit decision (`generate_exit_candidates`, called only at `lean/v1_strategy.py:693`) reads LEAN's always-empty PaperBrokerage portfolio, so `position_exit_count=0` every cycle despite real api positions; the V1 indicator-exits (trend_flip/reversal/decommission) are dormant (stops-only). Needs follow-up **PR-A2** (feed api positions into the exit cycle — risk-engine change, operator sign-off + risk-review + backtest). See `Docs/decisions-log.md` 2026-05-31 (evening) entry. | PR #310 (2026-05-31) — exit-proximity PR-C (frontend-only; **no `risk-review-approved`** per design ED7); see `Docs/exit-proximity-design.md` §7 + `Docs/decisions-log.md` 2026-05-31 entries |

See `Docs/claude-dev-guide.md` §2 for the canonical full-repo layout.
